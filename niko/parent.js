// Parent space (behind a math gate) + BOOT. Dashboard/report land in a later increment. HANDOFF §4, §8
function renderParent(){ /* TODO: math gate + per-child dashboard + report export */ }

// ── BOOT — must stay LAST (HANDOFF §2) ──
state = load();
boot();
