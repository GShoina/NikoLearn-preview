# NikoLearn — Bug + Systemic Fix Tracker (live, owner-tracked)
**Updated 2026-07-23 · LIVE = v1.373 (`928f407`, §6c-verified live on nikolearn.com: NB-96/97/98 learn-screen nav — duplicate in-content 🏠 removed from the shared `learnNav()`, arrows re-centered around the dots; live probe cardHomeBtns=0, arrow gap diff=0, 0 console errors, screenshot looked-at. FIRST AUTONOMOUS HOTFIX under Amendment f two-track model — bug fix shipped straight to live, no owner GO required).** Prior: v1.372 (`972c227`, NB-94 drawing-studio mobile layout — clean 2-row header + scrollbar-free emoji strips; §14 sweep = INSTANCE-only).** Prior: v1.371 (`df47411`: NB-93 word-search framed + NB-92 slower dayq voice).** 2026-07-22 W7 day-question SHIPPED LIVE v1.369 (roadmap build item, not a bug row; §14 all-home class-sweep ran CLEAN, gate-fix voiced the dayq world card + bumped age5 world-count 8→9, §6c live-verified). Prior wave: **NB-72/73/74/75 all SHIPPED LIVE in v1.368 `d293a02`** (§14 class-swept + §6c live-verified on nikolearn.com + npm test/visual-gate + full pre-push QA gate green; merged to main, deployed via Pages). SSOT for every owner-reported bug + the systemic class behind it. New owner
reports get inserted here in priority order; nothing is asked until the list reaches the bottom.

## 🧾 NB INTAKE LEDGER (§14 owner-report intake — append here FIRST, echo "დაფიქსირდა NB-<n>")
One row per reported case, BEFORE analysis. Closes only with: class-vs-instance verdict + fix/DEFER reason + §6c live-verify.
| NB | Symptom (1 line) | Source | Status | Class sweep |
|---|---|---|---|---|
| NB-99 | **[MEDIUM · localization/i18n leak · owner-reported] A message/notification surfaced in ENGLISH (non-Georgian) on nikolearn.com — iOS Safari auto-triggered its Google-Translate bar (GEORGIAN ↔ ENGLISH), which fires when it detects English text on a Georgian-locale page. Exact leaking string to pinpoint (candidate: the feedback-notify worker's notification copy, or a residual EN UI string behind the „მიღებულია, მადლობა" modal). Owner's SHARPER point = the COVERAGE question: how many similar i18n leaks exist untested, and do we even KNOW what is tested vs not?** | owner 2026-07-23 (IMG 8c15…jpeg, live nikolearn.com, iPhone Safari): „შეტყობინება ჩაგვივიდა არ არის ქართული. რამდენი მსგავსი შეცდომაა, გაგიტესტია? ან პირიქით იცი რა არის გატესტილი რა არა? მსგავსი ბაგების ფიქსი რომ გააკეთებინო დაბალ მოდელს". | **OPEN — intake only, not yet analyzed.** | pending — §14 localization-leak CLASS: enumerate ALL customer-facing strings, flag any non-ka on the ka locale; check feedback-notify worker + shared overlays. Meta finding to confirm: whether ANY automated i18n coverage exists [TO VERIFY] = the real gap behind his question. |
| NB-96 | **[HIGH · UI/nav consistency · owner-reported] A 🏠 „home" button (platform EMOJI in a white card) sits in the in-content nav row on learn/flashcard screens (letter-tracing, წინადადება/sentence, ანბანი) jammed far-LEFT next to the ← arrow — while the bottom footer ALREADY has a home button (house-outline SVG). Two DIFFERENT home icons on the same screen = „home იმიჯის 2 ვერსია"; the emoji one is out of place. Owner RED-CIRCLED it in IMG_2008.** | owner 2026-07-23 (IMG_2008.jpeg / IMG_2011 / IMG_2012, live nikolearn.com, iPhone Safari): „დამატებულია home იკონა უადგილო ადგილას ... home იმიჯის 2 ვერსია არსებობს". | **✅ CLOSED — SHIPPED LIVE v1.373 (`928f407`), §6c live-verified on nikolearn.com 2026-07-23 (first autonomous HOTFIX under Amendment f).** Live probe (390px, cache-busted): served APP_VERSION=1.373, learn card `cardHomeBtns=0`, single topbar back, 0 console errors, screenshot looked-at (ვ card, 6/33) = clean, no emoji home. ONE-line fix in `niko/alpha.js` `learnNav()`: removed the per-screen emoji `<button class="abtn home" onclick="goHome()">🏠</button>`. Home is now owned GLOBALLY by the one shared footer (`index.html .bottomnav`, house-outline SVG) which already shows on every learn screen (`render(...,'slim')`, styles.css „slim hides nothing"). §6c app-run at 390px: all 6 learn screens render `inAppHome=0`, single footer home — screenshot looked-at (ა card) = clean, no emoji home. | **ONE §14 class with NB-97/98 — see NB-98 for the class table (SWEPT: instance existed only on the 6 learn screens; NO siblings).** |
| NB-97 | **[HIGH · UI/layout · owner-reported] The ← back / → forward arrows in that same in-content nav row are visually unbalanced/distorted — the misplaced home card pushes the arrows off-center, asymmetric spacing („დამახინჯებულია წინ და უკან ისრების ლოკაცია გვერდზე").** | owner 2026-07-23 (IMG_2008/2011/2012, live nikolearn.com): „დამახინჯებულია წინ და უკან ისრების ლოკაცია გვერდზე". | **✅ CLOSED — SHIPPED LIVE v1.373 (`928f407`), §6c live-verified.** Live probe: `.alpha-nav` holds exactly `[← | dots | →]`, measured `leftGap=155, rightGap=155, diff=0` = perfectly symmetric, screenshot looked-at = `←  6/33  →` centered. Direct consequence of the NB-96 fix: with the home card gone, `.alpha-nav` (`justify-content:space-between`) now holds exactly 3 children `[prev-arrow | dots | next-arrow]`. §6c app-run measured `leftGap=0, rightGap=0` on all 6 learn screens = perfectly symmetric; screenshot looked-at = `←  1/33  →` centered. | **same §14 class as NB-96/98 (NB-98 = class table).** |
| NB-98 | **[HIGH · UI/design consistency · owner-reported + PROCESS] New learn „themes" duplicate/degrade the established footer design — the in-content nav row (🏠 ← N/M →) re-implements navigation instead of reusing the shared bottom-footer pattern, so every new section drifts. Owner meta-criticism (§16/§4): „ისევ და ისევ არ გაქვს ისტორიული მეხსიერება ან არ ჩეკავ რაც შენზეა" = a CKO/historical-memory failure, not just a pixel bug.** | owner 2026-07-23 (IMG_2008/2011/2012, live nikolearn.com): „რასაც ვუყურებ ახალ თემებს რომ ქმნი მანდ აკლებ არსებულ ფუთერის დიზაინს და აკვეხებ ახალს რაც არ მომწონს". | **✅ CLOSED — SHIPPED LIVE v1.373 (`928f407`), §6c live-verified.** ROOT already correct: `learnNav()` is ONE shared function rendering the in-content nav for ALL 6 learn screens (alpha/digit/read/sent/trace) — the defect was that this single source itself carried a duplicate home. Removing it there fixes every learn screen at once (consolidation confirmed by the app-run: all 6 identical). Home = globally the shared footer; no per-screen home may be re-added (comment locked in `alpha.js`). | **§14 CLASS (pattern language): „in-content home re-implemented per-screen instead of rendered from ONE shared source, so a screen shows a home that DUPLICATES the always-visible shared footer home." Discriminator: duplicate = footer shown (`nav='slim'/'home'/'dock'`) AND an in-content home icon. SEARCH SURFACE = every `render()` screen (grep of all `niko/*.js`) + APP-RUN at 390px (§14: static reading can't prove a UI class). CLASS TABLE \| screen \| footer shown \| in-content home \| verdict \|: alpha-ka/en·digit·read·sent·trace (6 learn) · YES(slim) · 1 emoji🏠→**0 (FIXED)** · duplicate REMOVED, arrows symmetric \|\| game-shapes · YES(slim) · 0 (only back-arrow abandonRound) · OK \|\| subject-menu · YES(home) · 0 · OK \|\| talk-pick · YES(talk) · 0 home-icon (only top-left back-arrow→home, conventional) · OK, not a duplicate \|\| results (win/lose) · YES(home) · 0 · OK \|\| dayq · NO(hidden) · 1 own🏠 · OK — footer hidden, own home is the SOLE nav, no duplication possible \|\| talk-card · NO(hidden) · 1 own🏠 · OK — same self-nav pattern. **RESULT: the defect (duplicate home while footer visible) existed ONLY on the 6 learn screens and is fully consolidated + fixed. dayq/talk-card intentionally roll their own home because they HIDE the footer = a different, valid pattern, NOT an instance. No sibling of the real class survives.** Verified: npm test green · visual-gate green · §6c screenshots looked-at. |
| NB-94 | **[HIGH · UI/layout · owner-reported MOBILE] Drawing studio („ხატვის სტუდიო", `niko/draw.js`) is visually broken on a real phone: the top toolbar is scattered (back `‹` button + the 2×3 tool grid + „შენახვა" save button are NOT aligned into one clean header bar), the sticker picker below the canvas overlaps itself (two cramped rows) and overflows horizontally (cut off at the right edge), and sections generally overlap. Owner also asks the CLASS question: is mobile-first respected HERE and in EVERY new section?** | owner 2026-07-23 (IMG_1996.png, live nikolearn.com on iPhone Safari): „ხატვის სტუდიოს გვერდში ბევრნაირი ბაგია მობილურ ვერსიაში: ჰუტერი და ჰიდერი მოუწესრიგებელია, გადაფარვა ხდება სექციების და აშ. ასევე First mobile დაცულია აქაც და ყველა ახალ სექციაში?" | **✅ CLOSED — SHIPPED LIVE v1.372 (`972c227`), §6c live-verified on nikolearn.com 2026-07-23.** Live re-verify (raw Pages fetch + rendered LOOK at 390×660, SW/cache cleared): served APP_VERSION=1.372, served styles.css carries all 3 fixes, rendered draw = tools single 44px row on row 2, back x22 / save right x369 aligned, tmpls/stamps/draw-screen scrollbar bar=0, 0 console errors, screenshot looked-at = clean organized studio. Fix in `niko/styles.css` `.dw-*`: (1) header restructured — `.dw-top` `justify-content:space-between` + `.dw-tools` `order:2;flex-basis:100%;flex-wrap:nowrap` → back(left)+save(right) align on row 1, the 6 tools sit in ONE clean centered row 2 (8 buttons never fit a 390px row, so a deliberate 2-row header, not a wrap-blob); (2) both emoji strips (`.dw-tmpls`/`.dw-stamps`) get `scrollbar-width:none`+`::-webkit-scrollbar{display:none}` (matching the already-correct `.mv2 .rail`) so the ugly grey native scrollbar-through-the-thumbnails is gone; (3) `flex-shrink:0` on the non-canvas rows + `overflow-y:auto`(scrollbar hidden) on `.draw-screen` so nothing clips/overlaps on a short phone. Local render 390×660: toolsRow h44 single-row, back x22 / save right x354 aligned, tmpls/stamps bar=0px, no frame overflow, screenshot looked-at = clean. | **§14 SWEEP RESULT — INSTANCE-ONLY (draw), NOT a section-wide class. Mobile-first IS respected everywhere else.** Two methods: (a) GREP (exhaustive, code-level): only TWO `overflow-x:auto` strips in the entire CSS lacked scrollbar-hiding — both in draw (`.dw-tmpls`,`.dw-stamps`); `.mv2 .rail` (movement) is the correct reference; every other option row uses `flex-wrap:wrap`+`justify-content:center` = reflows, never overflows. (b) LIVE RENDER at 390×660, audio muted — CLASS TABLE \| section \| scrollbar-strip \| frame-overflow \| tall-header \| verdict \|: **draw** · dw-tmpls+dw-stamps · scroll-children only · dw-top h93/8btns · **BROKEN(fixed)** \|\| talk · clean · clean · none · OK \|\| dayq · clean · clean · none · OK \|\| math-topics · clean · clean · none · OK \|\| kings-exam · clean · clean · none · OK \|\| menu-math · clean · clean · none · OK \|\| game-math-add · clean · clean · none · OK \|\| game-shapes · clean · clean · none · OK \|\| alpha-build · clean · clean · none · OK \|\| placement · clean · clean · none · OK. `docOverflowX=none` on all 10 screens = no page-level mobile-first breach anywhere. Draw was uniquely broken because it is the ONLY section with a multi-button toolbar (8 controls) + horizontal emoji strips; every other section has a single back button + reflowing option rows. **ROOT-CAUSE PREVENTION (class in only 1 module, so no cross-module refactor triggered, but the *test gap* is the real root): `qa/visual-gate.mjs` only renders home/menu/game — it NEVER exercises the section screens (draw/dayq/talk/kings-exam/alpha/placement/wordsearch/figures), which is exactly why a broken draw layout shipped unseen. Logged as prevention → extend visual-gate to render each section screen at 320/360/390px and assert 0 unhidden horizontal scrollbars + 0 frame overflow. Owner's question answered: YES, mobile-first holds in every other section; draw was the one exception and is now fixed + consolidated to the shared swipe-strip pattern.** |
| NB-95 | **[MEDIUM · a11y/tap-target · SELF-DISCOVERED during the NB-94 §14 prevention] Five interactive controls in section toolbars render below the 44px accessible tap-minimum (WCAG 2.5.5 / Apple HIG): draw `.dw-stamp` 42×42, `.dw-color` 36×36, `.dw-size` 48×38 (height 38); dayq `.dayq-say` 30×30, `.dayq-dot` 42×42. Small fingers (the actual users) mis-tap.** | self-discovered 2026-07-23 while extending `qa/visual-gate.mjs` to render section screens (the NB-94 root-cause prevention); the new per-control tap audit surfaced these. | **OPEN — BASELINED, deferred to a design pass (not shipped this session).** Baselined in `qa/visual-gate.mjs` as `TAP_DEBT` (the 5 controls demoted from gate-failures to non-blocking `◦ known tap-debt (NB-95)` warnings) so the gate stays honest without red-flagging every run; a NEW sub-44 control still fails the gate. Fix deferred because resizing live controls shifts layout = a design-pass concern (bundle with W1/W2 redesign), and per the NB-65 TYPE gate a control-resize on shipped UI is a PROPOSAL, not an autonomous rewrite. TYPE: measurable a11y breach (DEFECT-leaning) but layout-touching → scheduled, not silently dropped. | **§14 SWEEP — INSTANCE set fully enumerated by the new gate (not a hidden class).** The extended `visual-gate.mjs` now audits EVERY interactive control across all rendered section screens (entry/home2/menu/game/draw/talk/dayq/eng) at 320/360/390px for a <44px hit box. Result: exactly these 5 controls under 44px, all in draw + dayq; every other section's controls ≥44px. No further siblings. Prevention is self-enforcing: the audit is now permanent, so any future sub-44 control is caught pre-push. |
| NB-93 | **[MEDIUM · UI/layout consistency · owner-reported] The „სიტყვების ძებნა" (word-search) screen renders full-bleed across the whole browser window — no phone-frame card like the other themes/sections. Root [FACT `niko/wordsearch.js:124`]: `#wsscr{position:fixed;inset:0}` escapes the `.device` app frame.** | owner 2026-07-23: „სიტყვების ძებნა ... რატომ აქვს სხვანაირი დიზაინი და გადაჭიმულია მთელ ეკრანზე, ანუ არ აქვს ჩარჩო როგორც სხვა თემებს. შეამოწმე Class ტიპის ბაგია თუ მხოლოდ ამ სექციაშია." | **✅ CLOSED — SHIPPED LIVE v1.371 (`df47411`), §6c live-verified on nikolearn.com 2026-07-23.** Fix in `niko/wordsearch.js`: `#wsscr` `position:fixed;inset:0`→`position:absolute;inset:0` + `transform:translateZ(0)` so it fills the `.device` frame and its `position:fixed` children (`.ws-owl`/`.ws-dock`) anchor to the frame, not the window. Live re-verify (raw Pages fetch + rendered LOOK, cache/SW cleared): served CSS = the fix, wsscr rect == device rect (430px), dock contained (295→705 within 285→715), 0 console errors, screenshot looked-at = word-search sits inside the phone bezel like every other theme. | **SWEEP RESULT — INSTANCE-ONLY, not a class.** Grep of every section screen (dayq/draw/talk/kings-exam/alpha/placement/figures + shared niko/ overlays): ONLY `#wsscr` used viewport-pinned `position:fixed;inset:0`; `pwa-install.js:52` (install banner) is a deliberate full-window exception, correct as-is. **ROOT-CAUSE PREVENTION logged:** convention „a section screen fills `.device`, never the viewport" — future screens follow the framed pattern so the class cannot recur. |
| NB-92 | **[LOW · audio/UX polish · owner-reported] The „დღის კითხვა" Bubu voicing (shipped LIVE v1.370) is fine for a first pass but speaks slightly too fast; owner wants it a little slower and asked me to determine the value.** | owner 2026-07-23 (heard it on nikolearn.com): „ნიკოს დღის კითხვა გამოყენებული ხმა ნორმალურია პირველივე მცდელობისთვის. ოდნავ სწრაფად ხომ არ საუბრობს, ცოტა ნელა ვასაუბროთ უკეთესი იქნება. განსაზღვრე ეგ ცოტა ნელა რა უნდა იყოს." | **✅ CLOSED — SHIPPED LIVE v1.371 (`df47411`), §6c live-verified on nikolearn.com 2026-07-23.** Agent-determined „ცოტა ნელა" = edge_tts `rate=-10%` (≈10% longer, clearly calmer, not sluggish; baked into `tools/_gen_dayq.py` `RATE` so every future regeneration keeps the child-pace). 12 clips regenerated (`dqq/dqa_1-6`, all grew ~10%), manifest wiring byte-identical (unchanged keys). Live re-verify: dqq_3=26208b / dqa_1=85392b served from Pages = the slower files. Audio kept muted per no-loud-audio rule; verified by size, not playback. | **INSTANCE-scoped by design.** Only dayq's 12 clips re-paced this pass; the other recorded KA clips (alpha/wordsearch/games) are NOT re-paced without owner word — logged as a possible global „young-voice pace" lever (§4 product line, not shipped). |
| NB-91 | **[HIGH · content/Georgian-quality · owner-reported] Non-native Georgian shipped LIVE in the „დღის კითხვა" science questions: „რატომ გვეთქმევა ნერწყვი" (გვეთქმევა is not a Georgian word) and „რატომ ისვრის ხე ფოთლებს" (a tree does not „throw" its leaves) — the owner caught both on nikolearn.com and asked whether this passed the Gemini test.** | owner 2026-07-22 (reviewing live day-question): „გვეთქმევა არ არის ქართული" · „რატომ მოგვდის ნერწყვი — ეს ქართულია" · „რატომ სცვივა ხეს ფოთლები — ესე უფრო სწორი არაა? გაასწორე ყველა ტექსტი და გახმოვანებაც" | **✅ CLOSED — SHIPPED LIVE v1.370 (`918a5b3`), owner-confirmed BY EAR on nikolearn.com 2026-07-23 („ხმა ნორმალურია პირველივე მცდელობისთვის"). Follow-up voice-pace tweak = NB-92 (v1.371).** Text: 2 hard + 1 soft fixed in `niko/dayq.js`. Voicing (owner „გახმოვანებაც"): 12 EkaNeural build-time clips (`dqq/dqa_1-6`, `tools/_gen_dayq.py` fed by drift-guarded `tools/dayq-extract.mjs` so TTS text = manifest key = playClip lookup, byte-identical) — Bubu auto-reads each question on open + 🔊 replay, speaks the answer on the reveal tap; English reward stays silent (PI-104 gate untouched). `npm test` + `qa/visual-gate.mjs` green; §6c rendered on local server + screenshots looked at (button tasteful, answer settles at opacity 1). | **§14 CLASS (pattern language): „non-native Georgian shipped in CONTENT that never passed the §6f native-Georgian gate — the gate runs per-NEW-batch, never retroactively over the whole live corpus."** Sibling of the roadmap „Georgian-quality class" (NB-13 `ვკრებავთ`) and NB-90 (trust). **ROOT CAUSE [FACT]: the dayq questions shipped on VISUAL approval only (v1.369), never through Gemini §6f** — exactly why „გვეთქმევა" reached live. TYPE (NB-65 gate): DEFECT (child-visible wrong-language text vs the app's core „genuinely Georgian" claim). **SEARCH SURFACE = all 1336 live sentence-like KA strings** (`niko/*.js`, scanned this session); the un-gated CONTENT subset (dayq · talk · kings-content · tutor · games · data) is the risk zone, NOT the reviewed UI-chrome/i18n/audio-key strings. **SWEEP THIS PASS — dayq fully native-audited (6 Q + 6 A):** 2 HARD defects fixed (გვეთქმევა→მოგვდის; ისვრის ხე ფოთლებს→სცვივა ხეს ფოთლები, + its SVG aria-label sibling) + 1 SOFT tell fixed (tree answer „ახლებს გამოიყვანს"→„ახალ ფოთლებს გამოიღებს"). Q1/Q2/Q5/Q6 = native, clean. **CLASS TABLE — \| file \| method \| non-native found? \| fixed? \|: dayq.js · full native read (6Q+6A) · YES (2 hard+1 soft) · YES · | talk/data/tutor/games/kings-content · NOT yet native-swept · UNKNOWN · NO (→ roadmap P0).** **ROOT-CAUSE ESCALATION (§14, class in the whole corpus, not the copies): a Georgian-native QA gate that runs over ALL live content (not per-batch) = the fix, logged as P0 in the PO roadmap („my version", 2026-07-22). The corpus-wide non-native count is currently UNMEASURED — that unmeasured state IS the finding.** |
| NB-89 | **[MEDIUM · audio/brand · owner-reported] The English voice on the „1 apple"/vocab deliverable sounds DIFFERENT from the app's embedded English voice — tempo, clarity, tonality all read as a second, separate voice identity.** (owner rendered 2026-07-22: apple VISUAL is acceptable/good, but „ინგლისური ხმა არ მომწონს, ჩემი შეფასებით განსხვავებულია აპში ჩაშენებული ხმისგან — ტემპი, სიცხადე, ტონალობა".) | owner 2026-07-22 (reviewing Bubu-repeat + 1-apple deliverable; source = his message, exact KA in prior-session transcript) | OPEN → gate SPEC'd; deliverable re-render pending | **§14 SWEEP DONE 2026-07-22.** ROOT CAUSE [FACT, `niko/core.js:278-298`]: the app's English voice is NOT a fixed clip — it is runtime Web Speech (`pickVoice` warm list Samantha/Karen/Moira/Google-US/Aria/Jenny; rate 0.72, young ×0.86; pitch 1.1; vol 0.85). Georgian alone is pinned (recorded clips). The „1 apple" clip was generated by a different engine/settings → off-profile. **CLASS (pattern language):** „NikoLearn has no PINNED English voice identity, so any pre-generated English audio can diverge from the app's runtime voice" (brand/consistency). Surface swept = runtime `speakOne`/`pickVoice` (source of truth), + every created-audio deliverable (1-apple/vocab, W3 hero, Bubu-repeat, video-tale). FIX = `docs/VOICE_CREATION_GATE.md` (PI-104): §1 canonical profile + a pass/fail gate every non-runtime clip clears (A/B vs app + `voice-eval.mjs`). Architectural pin (record-EN vs lock-TTS-voice) = owner fork in gate §4, default = lock the §1 target. Instance re-render (Bubu-repeat EN + 1-apple) = do it THROUGH the new gate before ship. |
| NB-90 | **[HIGH · process/CKO · owner-reported] Owner cannot tell which issue is the MAIN one across many reports in many files („დავიკარგე") — no single linked entry-point; the scattered output/ reports actively lower his trust in the work.** | owner 2026-07-22 (#13, rendered: „ბევრი საკითხი ბევრ რეპორტში — რომელია მთავარი? ამის გამო არ მომწონს. სასურველია ერთ დოკუმენტში, ლინკებით გადავიდე, თორემ დავიკარგე") | OPEN → FIX THIS SESSION (build ONE linked BLUF home-doc + prune answered reports, #14) | *(pending §14 sweep) class candidate: „knowledge shipped as many peer files with no index/entry-point, so the owner must reconstruct priority himself" — the CKO SSOT-map class (sibling of NB-71 container-vs-content, NB-76 owner-reading-channel). ROOT FIX = one owner-home doc that links to the live SSOTs (BUGFIX_TRACKER / ROADMAP / IDEAS), not another parallel report; + §14 #14 cleanup of answered files. |
| NB-66 | **[HIGH · process · DEFECT, 2nd occurrence] A subagent signals `idle` without delivering its result, and the work is gone.** | observed 2026-07-15 22:11:37 (`factcheck-landing`, 8 verdicts lost permanently) + again 2026-07-16 04:32:06 (same agent, re-spawned) | OPEN | **CLASS: „a completion SIGNAL is treated as the DELIVERABLE."** `idle` means the process stopped, not that it produced anything. Recurrence proves it is systemic, not a fluke. Cost is asymmetric: the lead notices only when reading the result, by which time the agent's context is gone. **Passes the NB-65 TYPE gate as a real DEFECT** (observable behaviour vs a stated expectation, with measured loss), not an opinion. **Interim: PULL, never assume push** — on any idle, immediately demand the table, and accept partial-delivered over complete-lost. **Root fix (owed, not done): a result is not accepted until it is written to the ledger; §10 Agent-Team Standard should require the deliverable to land on DISK, not in a message** (same class as NB-64: a message is not a durable store). |
| NB-65 | **[HIGH · process · owner-reported] „NB" has inflated to mean „anything an audit disliked", so the ledger no longer separates a real defect from an opinion — and the agent is rewriting product colours/text on the strength of that blur.** | owner 2026-07-16: „უკვე იმდენი bug-ად წოდებული ცვლილებები შეიტანე რომ ვეღარ ვარჩევ ბაგი რა არის, არა ბაგი რა არის? პროდუქტის ფერების და ტექსტის გადაწერას ისე ახდენ, უკვე რეალური საფრთხეა მნიშვნელოვნად შეიცვალოს ყველაფერი." | OPEN | **CLASS: „an intake channel with no TYPE gate — every finding enters as a defect, so severity is inherited from whoever reported it rather than from harm to a child."** The §14 intake rule I built says *every* owner/audit case gets an NB row; it never says what an NB *is*. So a Codex UX audit's 29 opinions became NB rows with CRITICAL/HIGH labels I assigned, and a label is a licence to act. **The owner is right, and his OWN doc already said so:** `docs/V2000-DESIGN-SYSTEM.md` states DESIGN SSOT = his prototype, implementation = a coding model, and Fable5's lane = *„Georgian copy, NOT UI/CSS."* I drifted out of a lane his design system had already drawn. **FIX (proposed, needs his call — this is taste/direction, §13 gate 4, not mine to settle): a TYPE column — DEFECT** (observable wrong behaviour vs a stated expectation; agent fixes autonomously) **vs PROPOSAL** (a11y/UX/copy/colour judgement; goes to the owner, never self-approved). Audit findings default to PROPOSAL until they name the child-visible harm. **Immediate, unilateral action taken now: STOP all colour/text rewrites; NB-59/61/63 + audit rows touching palette or landing copy are frozen pending re-typing.** |
| NB-64 | **[HIGH · process · RECURRING, owner-reported] VS Code closes without a handoff having been written → the project's current status has no record on disk, and recovery costs a manual forensic hunt.** | owner 2026-07-16 chat: „რამდენჯერმე დამემართა რომ დამეხურა vs code ისე რომ hand off არ გაკეთებულა. რა არის პრევენცია რომ არ დამეკარგოს პროექტის მიმდინარე სტატუსი" — **owner-reported RECURRENCE („რამდენჯერმე" = several times), which per §14 makes it a P0-class process bug, not a one-off** | **OPEN → FIXED THIS SESSION (see class sweep)** | **CLASS (pattern language): „a durability rule addressed to the AGENT, with no mechanism behind it — so it fails in exactly the case it exists for."** §6e-2 says *„periodically + automatically, WITHOUT being asked, flush session state to SESSION-HANDOFF.md."* That is a **promise, not a system**: it requires the model to choose to act, and a model whose process is killed cannot act. The rule is structurally guaranteed to be absent at the moment of the crash. **This is the NB-58 promise-integrity class turned inward** (a claim nothing checks) and the NB-55 class („every bug must leave the test that would have caught it") — the s21 handoff even *documents* a prior crash-recovery, i.e. the recurrence was already logged as prose and still had no mechanism. **MEASURED, not assumed:** the 2026-07-15 session `287dc0da` ran **12:11→23:05 (10h54m, 4149 events, 17.4MB)** and died; `SESSION-HANDOFF.md` was last written **hours earlier**; the tree held **uncommitted, un-described** work (recovered + committed this session as `0b84a4c`). **CRITICAL FINDING — nothing was actually LOST: the transcript is written to disk continuously by the harness** (`~/.claude/projects/<key>/<id>.jsonl`) and the working tree survives a VS Code kill. **What is lost is the INDEX, not the data** — the cost is forensic re-derivation, and the search surface is booby-trapped: the project key derives from the **launch cwd**, so `e88ed5b5` (launched from the parent dir) does not appear under the NikoLand key at all = the **L14 container-vs-content class, 4th recurrence.** **FIX (mechanical, at the harness layer where the model cannot fail to run it): `.claude/hooks/niko-session-pulse.ps1`** on the **already-existing Stop hook** — writes `docs/.session-pulse.md` every turn (session id · exact `--resume` command incl. the launch dir · branch@sha · uncommitted list · last owner message verbatim). **`niko-handoff.ps1` (SessionStart) surfaces it, and the signal that makes it work: pulse mtime NEWER than SESSION-HANDOFF.md ⇒ the previous session did work the handoff never recorded** → warn + hand over the resume command. Written by the **harness, not the model** → survives what the model cannot. **HONEST SCOPE: it cannot recover a turn killed mid-flight (≤1 turn stale), and it does not make the model write better handoffs — it makes the handoff's absence VISIBLE and recovery one command instead of an investigation.** |
| NB-68 | **[HIGH · tooling · DEFECT] The NB-64 crash-recovery mechanism shipped DEAD ON ARRIVAL — `recovery-checkpoint.ps1` threw before writing its checkpoint, so the system built to stop losing session state could not itself run.** | discovered 2026-07-16 while fixing the pre-push QA gate (the rewritten `qa/session-pulse.test.mjs` exercised the writer end-to-end for the first time and it crashed) | ✅ FIXED THIS SESSION | **CLASS: „the NB-64 promise-integrity class turned one layer deeper — a mechanism everyone BELIEVED ran, that never did."** NB-64 built a Stop-hook checkpoint so session state survives a crash; the checkpoint itself carried **five distinct PowerShell-5.1 bug sub-classes**, each fatal before the write: **(1)** `.TrimEnd('\\','/')` — `'\\'` in single quotes is TWO chars but `TrimEnd(char[])` needs single chars → throws (**6 instances** across `recovery-checkpoint.ps1`, `recover.ps1`, `repair-git-objects.ps1`). **(2)** `$ErrorActionPreference='Stop'` + native git stderr → a benign CRLF/detached-HEAD warning becomes a TERMINATING error even with `2>$null` (PS 5.1 gotcha) → aborted the whole checkpoint on any git warning. **(3)** `@($genericList)` on a `List[object]` → „Argument types do not match" in PS 5.1 (must use `.ToArray()`). **(4)** `[IO.File]::Replace($tmp,$path,$null)` — `$null` coerces to `''` for the `[string]` backup param → „path is not of a legal form" (must use `[NullString]::Value`). **(5)** stdin read via `[Console]::In.ReadToEnd()` decodes UTF-8 as the OEM console codepage → a Georgian owner message is double-mangled beyond recovery (must read the raw stream as UTF-8). **TYPE (NB-65 gate): DEFECT** — observable: the tool throws and writes nothing, vs its stated contract of a checkpoint every turn. Not an opinion. **FIX:** all 5 sub-classes fixed + robustness hardening in the NB-64 spirit („a recovery aid must never break the session it protects"): `Write-AtomicUtf8` now removes its temp on failure (no orphan `.tmp` litter), and the untracked-snapshot block is wrapped so a snapshot failure degrades to a `snapshot_error` note in state instead of aborting the CORE `current.md`/`current.json` write. **GUARD (NB-55):** `qa/session-pulse.test.mjs` rewritten to drive the REAL mechanism end-to-end against a throwaway git repo — the parse-guard now covers the writer too, and owner-message ENCODING-SURVIVAL is asserted (the bug that hid sub-class 5). 23 checks green. **The lesson is NB-55 exactly: a test that never ran the mechanism let a whole recovery system look installed and be dead.** |
| NB-67 | **[LOW · design-note] The NB-64 crash-vs-clean discriminator axis retired when checkpointing became continuous — recorded so the change is deliberate, not silent drift.** | design evolution noted 2026-07-16 during the `session-pulse.test.mjs` rewrite | INTENTIONAL (documented in the test) | **What changed:** the original one-shot pulse fired ONCE per Stop, so „pulse newer than SESSION-HANDOFF.md ⇒ the session worked past its last flush then died" was a real crash-vs-clean signal. The mechanism now checkpoints on EVERY prompt/tool/stop event, so `mtime(checkpoint) > mtime(RESUME.md)` is true even on a clean exit → that heuristic can no longer tell a crash from a clean close-out. **The read-half (`niko-handoff.ps1`) now discriminates on SESSION IDENTITY instead:** a DIFFERENT prior session's checkpoint fires `[RECOVERY]`; the CURRENT session's own checkpoint is silent (self-resume). That is still a genuine discriminator (proves not-always-on), just on a different axis. **Not a defect, not silently dropped.** **Revive trigger:** if `[RECOVERY]` noise on ordinary multi-session days becomes annoying, restore crash-vs-clean by having the SessionEnd hook stamp `event: session-end` into `current.json` and firing the alert only when the prior session did NOT end cleanly. |
| NB-70 | **[HIGH · process · owner-reported REPEAT-TEACH] Owner operating guidance (abundance / learn-then-build-our-own lens) was explained to an agent in this project once before and LOST — on 2026-07-17 he had to re-teach it after I framed Marble's open taxonomy extraction-first („ლეგიტიმური მალსახვევი").** | owner 2026-07-17: „ეს ძლაიან კრიტულლია და ერთელ უკვე განვმარტე დავაზუსტე ამ პროექტში აგენტან მაგრამ დაიკარგა." | ✅ FIXED THIS SESSION | **CLASS: „the THIRD intake channel has no ledger — bugs get NB rows, ideas get PI rows, but owner GUIDANCE/corrections had no mandatory same-turn durable store, so a lens taught in chat dies with the session."** Same family as the PI-ledger gap (2026-07-15) and NB-64/NB-66 (a message/chat is not a durable store). **Sweep evidence:** the project memory directory was EMPTY until 2026-07-17 (no MEMORY.md, zero feedback files) despite months of owner corrections [FACT — checked by grep this session]; the corrections that DID survive (competitor lens §6b, no-em-dash §8, loop rule §12, anti-stop §13) survived ONLY because they became CLAUDE.md rules; everything short of a rule was lost. Aggravator: the abundance lens itself already existed as §6b (owner-set 2026-06-05) and the extraction framing still shipped — so (a) a later owner clarification was lost AND (b) a rule alone did not bind reasoning. **FIX (all done this session):** (1) the guidance persisted verbatim to project memory `abundance-lens-learn-then-build-our-own.md` + `MEMORY.md` index (auto-loads every session); (2) CLAUDE.md §6b competitor-lens paragraph extended with the 2026-07-17 owner clarification (partners too · learn the mechanism → take the angle → build OUR OWN way · legal/copyright NEVER a stop/blocker/headline frame); (3) standing discipline made concrete: ANY owner correction/lens/principle gets a memory-file + index line the SAME TURN it is given — the same-turn write is the mechanism, the memory dir is the store. A re-teach = intake FAILURE (§14), which is exactly what this row records. |
| NB-71 | **[MEDIUM · process · owner-surfaced] The Stage-1.5 and 2026-07-17 twin-audit Marble assessments were built WEB-FIRST while the project's OWN deep Marble corpus (5 docs, 07-13/14, + `content/graph/` v0) sat unread — the owner had to ask „ეგენი შეფასებისას წაიკითხე?".** | owner 2026-07-17 mid-turn question | ✅ FIXED THIS SESSION (mechanism) | **CLASS: „a new analysis of a subject launched WITHOUT sweeping the project's own prior conclusions on that subject — so later versions silently re-derive or contradict earlier verdicts."** L14 container-vs-content family, next recurrence: the knowledge existed but its CONTAINER was invisible — in-repo `01_NikoLearn_Strategy_drafts/` is NOT in the docs/README.md CKO map; only the EXTERNAL `../NikoLearn_Strategy_drafts/` docx folder is indexed [FACT — grep this session]. A map-following session structurally could not find the corpus. **Damage check (2026-07-17 reconciliation):** the web-first picture and the internal verdicts turned out CONSISTENT — no shipped contradiction; one discrepancy = founder background (ex-Lingumi vs ex-Google) + funding figures → [TO VALIDATE], presented as fact nowhere. **FIX:** (1) all 5 docs' conclusions extracted (ctx_execute, titles+verdicts only) and reconciled; (2) fed to audit-engine-2 as MUST-READ with an explicit verify-against-code + never-silently-contradict obligation; (3) `01_NikoLearn_Strategy_drafts/` registered in the docs/README.md map the same turn; (4) standing CKO rule: before ANY new analysis of a named subject, grep the repo for that subject FIRST — prior verdicts are built on or explicitly superseded, never re-derived blind. |
| NB-72 | **[MEDIUM · UI/pedagogy · audit A-1, lead-verified] Georgian-flow shapes TEACH card shows the English word „square" — exactly at the corrective-teaching moment.** | 2026-07-17 twin audit A (max-effort UI/UX); lead-verified against code same day: `games.js:839` passes `o.en` into the answer pipeline; the WIN path `:855` localizes (`SHAPES.find(s=>s.en===cor)` → `sh.ka`) but the WRONG path `:858` calls `reQueueWrong(cor,null)` with the raw en id | ✅ FIXED THIS SESSION (branch `fix/nb-72-75-audit-wave`) — ✅ SHIPPED LIVE v1.368 `d293a02` 2026-07-22 (§6c live-verified nikolearn.com). §14: the asymmetry is at the shapes **young-ka call-site**, NOT in `reQueueWrong` — all 14 `reQueueWrong` callers were checked (math=numbers · phrases/alpha=ka-labelled · english=en-labelled), so the shared fn is language-agnostic **by design** and the DEFER note's "fix the shared fn" hypothesis was wrong. FIX `games.js:883` mirrors the win path (`SHAPES.find(s=>s.en===cor).ka`, voiced `ka-GE`, clip-first). §6c live (Playwright): a young-ka wrong pick now teaches „წრე"/`ka-GE` (was raw „circle"/silent); en child unchanged; `isYoung` fires on age-4. `npm test` + `visual-gate` green. | **CLASS: „the corrective path consumes the internal English id where the win path localizes" — one deck, two rendering paths, only one of them translated.** TYPE (NB-65 gate): DEFECT — observable child-visible wrong-language text vs the app's own ka-flow contract; not an opinion. **Sweep surface: money and clock strands use the same `reQueueWrong` mechanism — fix at the shared function, not the shapes call-site (§14 root-cause escalation).** |
| NB-73 | **[MEDIUM · UX · audit A-6, lead-verified] Stale owl hint bubble surfaces on worlds-home AFTER the child left a Kings task — the owl talks about a word from the abandoned task and covers the home screen.** | 2026-07-17 twin audit A; lead-verified: `owl.js:55-57` coach `helpTimer→openHint`; `clearIdleHelp()` at `owl.js:66` clears ONLY `idleTimer`, never `helpTimer` | ✅ FIXED THIS SESSION (branch `fix/nb-72-75-audit-wave`) — ✅ SHIPPED LIVE v1.368 `d293a02` 2026-07-22 (§6c live-verified nikolearn.com). FIX `owl.js:69` `clearIdleHelp` now also cancels `helpTimer` (was clearing only `idleTimer`) + `abandonRound` (`games.js:81`) calls `closeHint()` so an already-open `#aiov` can't survive the leave. §14 sweep: `idleTimer` + `helpTimer` are the ONLY per-round owl timers; `_mvTimer` is the movement-break timer, cleared by `closeBreak()` (separate feature, N/A). Every `clearIdleHelp` exit caller (record/results/abandon) now clears both. §6c live (Playwright, coach role): armed `helpTimer` → `clearIdleHelp()` → `helpTimer===null`, no overlay fires after. Tests green. | **CLASS: „screen-exit paths do not clear ALL pending owl timers/overlays" — any timer armed inside a task can fire on whatever screen the child is on next.** TYPE: DEFECT (observable stale overlay vs the mascot's contract of contextual help). Sweep surface: every owl timer (`idleTimer`, `helpTimer`, any future one) must die on EVERY exit path — fix = one clear-all in the screen-transition choke point, not per-caller patches. Related family: A-8/B-D1 mascot promise-vs-reality thread. |
| NB-74 | **[LOW · consistency · audit A-4, lead-verified] Talk nest: hub badge says „71 ბარათი", the player says „1/54" — two different numbers for one deck.** | 2026-07-17 twin audit A; lead-verified: `talk.js:188` renders full `TALK.ka.length`; `talkPool` `:198-201` filters by min-age | ✅ FIXED THIS SESSION (branch `fix/nb-72-75-audit-wave`) — ✅ SHIPPED LIVE v1.368 `d293a02` 2026-07-22 (§6c live-verified nikolearn.com). FIX `talk.js:188-189` badge now counts `talkPool('ka'|'en').length` (the same age-filtered deck the player uses). §14 sweep of other hub count-badges: WORDS/PHRASES topic badges (`screens-menu.js:219,256`) are HONEST — the play builder consumes `WORDS[c]`/`PHRASES[c]` UNFILTERED (`games.js:7,16`), `byLevel` age-gating applies ONLY to Kings YLE pools (`:1091/1140/1187/1242`), never to general topics → badge==play set → N/A. §6c live (Playwright, age-4 profile): badge shows 54/44 = `talkPool` (was raw 71/49); age-8 shows 71/49 correctly. Tests green. | **CLASS: „one fact computed from two different sources" — the NB-58 one-fact-one-number family, non-promise variant.** TYPE: DEFECT (self-contradicting UI). Fix = hub badge counts the SAME age-filtered pool the player uses. Sweep: any other hub badge that counts a raw array while the player filters (check all hubs' count badges once, same pass). |
| NB-75 | **[LOW · trust · audit A-7, lead-verified] First-day parent card shows „სიზუსტე 0%" after a PERFECT first-run — the parent's first data point about their child is false.** | 2026-07-17 twin audit A; lead-verified: `firstrun.js:117` records only `st.shields++` (no answer records); `parent.js:221` `acc = cor+wr ? … : 0` renders the empty state as a measured 0% | ✅ FIXED THIS SESSION (branch `fix/nb-72-75-audit-wave`) — ✅ SHIPPED LIVE v1.368 `d293a02` 2026-07-22 (§6c live-verified nikolearn.com). FIX `parent.js`: `hasAcc=(cor+wr)>0` guard → `accTxt='—'` when no scored answer, applied at dashboard header (`:228`), stat scard (`:235`, neutral grey when no data), parent-tip prose (`:280`, now gated so it never claims a %), and text export (`:457/461`). §14 sweep: per-math-op acc (`:290`) is safe (rendered only when that op has ≥1 entry); no other dashboard metric has a `:0` fallback that renders as a measured value. §6c live (Playwright): first-run child (shields=1, 0 answers) shows „სიზუსტე —"; control child (8/2) shows „80%" green. Tests green. | **CLASS: „an empty zero-state rendered as a measured value" — no-data and scored-zero are different facts shown identically.** TYPE: DEFECT (first-impression trust break on the parent dashboard). Fix options (pick at fix time): record first-run answers like normal ones, OR render „—/ჯერ ცოტა მონაცემია" when `cor+wr===0`. Sweep: every dashboard metric with a `: 0` fallback gets the same no-data check. |
| NB-76 | **[LOW · process · owner report] Owner could not open the private-origin GitHub link to the pending-changes brief on his phone („ლინკი ვერ გავხსენი").** | 2026-07-18 chat; link = github.com/GShoina/NikoLearn = PRIVATE repo, so an unauthenticated browser/in-app webview gets 404 — device must be logged in as GShoina | OPEN (mitigated same turn: content retold in chat + local HTML copy in `output/`) | **CLASS: „owner-reading channel depends on GitHub auth" — every private-origin phone URL (this brief AND the 100_Final_Docs corpus README) fails identically on a logged-out device. §6e-2 already names the robust channel: HTML in `output/`. Standing rule from now: every owner brief ships BOTH (output/ HTML + private URL); close when phone GitHub login is confirmed working. |
| NB-77 | **[MEDIUM · UI · owner-reported] დათვლა/„ისწავლე ციფრები" (digitLearn) ეკრანის footer-ში home ღილაკი არ არის — ბავშვი მთავარზე ვერ ბრუნდება.** | owner 2026-07-20, preview v1.318 ტესტი 5 წლის პროფილით + სქრინშოტი `Downloads/foodnotermissing.png` · ⚠️ reported on v1.318 — CONFIRMED still present on current main (v1.366) | **FIXED (branch `fix/nb77-nb80-learnnav-tutorlabel`, commit 58cdd25) — pending live GO** | **§14 CLASS: „learn/lesson screens rendered via the slim shell omit the home affordance that tabbed screens carry." ROOT-CAUSE fix (2+ screens → architecture, not copies): one shared `learnNav(prev,dots,next)` helper (`alpha.js:29`) injects a leftmost 🏠 `goHome()` button; all 5 sites replaced. CLASS TABLE — \| screen \| check \| instance? \| fixed? \|: alphaLearn(:62)/render·yes·yes; digitLearn(:151)/render·yes·yes; readLearn(:240)/render·yes·yes; sentLearn(:299)/render·yes·yes; traceLearn(:586)/render·yes·yes. §6c: Playwright 60/60 assertions @412px+360px, first(prev-disabled)+last(CTA) cards, home leftmost + goHome + aria „მთავარი" on every screen, overflow=0. |
| NB-78 | **[TO VALIDATE · audio · owner-reported] ხმის ტემპი 5 წლის ბავშვისთვის საეჭვოა („არ ვარ დარწმუნებული რომ სწორი ტემპია").** | owner 2026-07-20, preview v1.318 | **OPEN → owner tuning call (not a code defect)** | TYPE per NB-65: TUNING/PROPOSAL. FACT — current per-age rate map (`isYoung`=age<=5): ka word `alpha.js:19` 0.5 young / 0.6 else · en letter+word :22 0.66/0.78 · digit `digitSay` :125 0.6/0.7 · reading `readSay` :187 0.58/0.7 · default `core.js:289` ka 0.6 en 0.72 ×0.86 when young. NOT a defect — no silent rewrite. Proposal (needs owner/child listen): the young-ka floor 0.5-0.6 may feel slow; offer a per-profile speed toggle (slow/normal) rather than hard-changing the global rate. |
| NB-79 | **[MEDIUM · UX/pedagogy · owner-reported] სწორი პასუხის შემდეგ პაუზა სტანდარტს აკმაყოფილებს? + სწორ პასუხზე არ ისმის შექება („ბრავო, ყოჩაღ").** | owner 2026-07-20, preview v1.318 დათვლის მოდულში | **ALREADY FIXED on current main (v1.366), post-dates the v1.318 the owner tested — no new change needed** | §14 CLASS: „correct-answer praise + child-paced advance inconsistent across quiz modes." SWEEP vs current code: `answerCount` (`games.js:1027`) AND `answerDigit` (`alpha.js:191`) both call `sayThenPraise(numWord(cor),vCode,…)` → `winStep` (`games.js:1636`), same gate alpha already had. CLASS TABLE — count·read·yes-has-praise; digit·read·yes; alpha·read·yes; math modes·read·yes (winStep shared). No instance survives on main. Owner saw a ~48-version-stale bench (see NB-81). Pause: winStep sequences say→praise→advance, meets the child-paced standard. |
| NB-80 | **[HIGH · tutor · owner-reported] ტუტორის ბაგი: პირველ დახმარებაზე შედარების ლოგიკურ რჩევას იძლევა, მეორეზე ინგლისურის მასწავლებელ ტუტორზე გადადის.** | owner 2026-07-20, preview v1.318 + სქრინშოტები `Downloads/error seccond option.png`, `error seccond option2.png` | **FIXED (branch `fix/nb77-nb80-learnnav-tutorlabel`, commit 58cdd25) — pending live GO** | §14 CLASS: „a game mode not mapped in `gameSubject()`/`Tutor.build` falls through to the English-teacher default persona" — THIRD recurrence of this class (fixed 2026-07-01 kings-math, 2026-07-04 reading). The counting content-map was already fixed on main; the residual was the label ternary (`tutor.js:269`) missing a 'counting' branch → coach-mode counting showed „ინგლისურის მასწავლებელი". FIX: added 'counting' → „ბუ · დათვლის მასწავლებელი" + EN mirror. ROOT-CAUSE (recurring class): added QA guard `qa/tutor-mode-map.test.mjs` (NB-81) that FAILS if any `game.mode` silently maps to the English default. CLASS TABLE — full mode→subject→label sweep run (23 modes); count/digit→Counting fixed; 2 LATENT mislabels found → NB-82 (cal), NB-83 (exam), whitelisted in guard pending owner call. §6c: npm test EXIT=0. |
| NB-81 | **[HIGH · process · owner-reported] „ყველა მნიშვნელოვანი გეითი გაიარა ამ რილიზმა? გააუმჯობესე check and validation ისე რომ მსგავს ბაგებს მე აღარ ვპოულობდე."** | owner 2026-07-20 | **ANSWERED — gate strengthening shipped (guard + refresh recommendation)** | NB-55/NB-58 family. ROOT FINDING: the owner tested `gshoina.github.io/NikoLearn-preview` = commit `5e4d0ff` ≈ **v1.318**, while LIVE/main = **v1.366** (~48 versions ahead). Of the 4 reports: NB-79 (praise) + NB-80 content were ALREADY FIXED on main; only NB-77 (home) + NB-80 label survived. So 2 of 4 „escapes" were bench-staleness, not gate failures. WHY the 2 real ones escaped: no render-assertion for the home affordance on learn screens, and no per-mode label guard. GATE STRENGTHENING: (1) `qa/tutor-mode-map.test.mjs` now fails on any unmapped-mode→English-default; (2) §6c render-assertion covers home on all 5 learn screens; (3) RECOMMEND (owner-gated infra, §15b): refresh the preview bench to the main tip so owner tests current code — the deeper process gap. TODO per-PROMISE test (NB-58) still the larger prevention system. |
| NB-82 | **[LOW · tutor · incidental, found during NB-80 sweep] `cal` (calendar) mode falls through to the English-teacher tutor persona — should be a calendar/content-aware Georgian tutor.** | 2026-07-20 NB-80 mode→subject sweep (fixer agent) | OPEN (latent, whitelisted in `qa/tutor-mode-map.test.mjs` so it is tracked, not silent) | Same CLASS as NB-80, now its LAST open instance (sibling NB-83 closed 2026-07-21). Larger than NB-83: needs a NEW `'calendar'` subject in `gameSubject()` + a new label branch (`ctx.subject==='calendar'?'ბუ · კალენდრის მასწავლებელი'`) in tutor.js AND a full `ctx.subject` audit so the new value degrades gracefully in every hint/message switch (customer-facing copy). Fold into the next dedicated tutor pass. |
| NB-83 | **[LOW · tutor · incidental, found during NB-80 sweep] `exam` (Kings-math mock) falls through to the English-teacher persona — should be the Math teacher.** | 2026-07-20 NB-80 sweep | **FIXED 2026-07-21 (branch `fix/nb77-nb80-learnnav-tutorlabel`, not live)** | CLOSED: `gameSubject()` (niko/owl.js) now routes `exam`→'pattern' (the reasoning-strand subject) → "ბუ · მათემატიკის მასწავლებელი" label, reusing an already-shipped-safe subject value (zero new code path). Guard regression trip-wire `isMapped('exam')` added; `node qa/tutor-mode-map.test.mjs` + `npm test` green. Live owl-label render on an exam = the pre-deploy §6c step, rides the next app-render session before any live push. |
| NB-84 | **[HIGH · brand/visual · owner-reported] W8-B prototype draws a NEW, off-brand owl (tan/brown minimal face) instead of the ONE canonical Bubu — the owl is our LOGO and has an existing tutor visual. A third, different Bubu breaks brand identity.** | owner 2026-07-20, `output/2026-07-20-გაიმეორე ბუბუსთან W8-B პროტოტიპი by Niko.html` (GI-50 pt 1-2) | **OPEN → FIX THIS PASS (prototype, no live gate)** | **§14 CLASS (pattern language): „every deliverable RE-DRAWS the mascot from scratch instead of reusing the single canonical brand asset" — a brand-lock the standard never enforced.** TYPE (NB-65 gate): DEFECT — observable off-brand mascot vs the existing brand system. Canonical assets EXIST [FACT]: `assets/logo.svg` (vector owl, palette #F2894C/#D9703A/#F2B441 + „ნიკო ბუ 🦉" wordmark), `02_brand-preview/owl-amber-master.png` (master render + 5 palette variants owl-amber/blue/teal/violet/navy), `favicon.svg`, and the in-app tutor SVG (`owl.js`). W8-B's owl matches NONE. ROOT-CAUSE (§14 escalation): consolidate — ONE embedded canonical mascot asset reused everywhere; kid-visual-standard #10 upgraded to a hard mascot-LOCK (never redraw; reuse the anchor). SWEEP SURFACE: every output/ deliverable that renders a mascot (W1/W2/W4 prototypes, W7 v2, W8-B) — check each reuses the canonical asset, not a fresh drawing. |
| NB-85 | **[MEDIUM · visual · owner-reported] W8-B „apple" hero image does not read as an apple to a human („ვერც მივხდები რომ ვაშლია სურათზე") — a flat hand-drawn SVG that fails its one job: communicating the word.** | owner 2026-07-20, same file (GI-50 pt 3) | **OPEN → FIX THIS PASS (prototype, no live gate)** | **§14 CLASS: „hero/teaching imagery is hand-authored as flat SVG with no recognizability gate, so it can ship looking unlike its subject" — kid-visual-standard #1 (purposeful) + #2 (quality floor) had no PASS/FAIL recognizability test.** TYPE: DEFECT — the image's purpose is to name the word; if a human cannot name it in <2s it has failed. METHOD ANSWER (GI-50 pt 3, „is there a better standard"): YES — the canonical owl was a GENERATED, style-locked rich render, not a flat SVG; 2026 best practice [FACT, web-verified 2026-07-20 + the shared awesome-ai-pulse-georgia media-gen tools] = anchor/reference-image generation for recognizable, consistent art. FIX: add a RECOGNIZABILITY GATE to kid-visual-standard (blind-name test) + a generate-vs-draw decision rule (icons = crafted SVG that passes the gate; hero/teaching imagery = generated recognizable art, style-locked, embedded). Re-do the W8-B word images to pass the gate. |
| NB-86 | **[HIGH · promise-integrity · incidental, found answering owner „დავიჯერო რომ 1 მშობელმაც არ მოიწერა?"] Parent form-feedback IS captured server-side (worker `/v1/feedback`, KV `fb|` rows: msg + optional name/phone/email) but the owner had NO way to READ it — the stats viewer only fetched `/v1/stats` (which excludes `fb|`), and its note wrongly claimed feedback „mailto/WhatsApp-ით მოდის, დაშბორდი ვერ ხედავს, საკონტაქტო არსად ინახება". So a real parent message would sit unread and the in-app promise „შენი შეტყობინება ჩაგვივიდა. მალე გიპასუხებთ" was structurally unkeepable.** | discovered 2026-07-20; direct `GET /v1/feedback?k=` read = **1 row total, and it is the owner's own test** (name „Test", his phone +995593255385 / nikolearn@outlook.com, empty msg) → real parent form-messages = 0, but the dashboard could never have shown him that either way | **FIXED THIS PASS (owner-local viewer, no live gate)** | **§14 CLASS: „a consented inbound channel is captured + receipt-confirmed to the user, but never surfaced to the human who must act on it" — NB-58 promise-integrity family, feedback variant, aggravated by a stale viewer note asserting the OPPOSITE of the code (§16 CKO contradiction).** SWEEP of inbound channels: (1) form → worker `/v1/feedback` → **now readable in the viewer's new 💬 უკუკავშირი tab** (fetches the endpoint, flags owner-test rows, shows real ones); (2) manual `wa.me/995593255385` + `mailto:nikolearn@outlook.com` links → land in the owner's WhatsApp/Outlook, OUTSIDE any store BY DESIGN → must be checked there, cannot be surfaced in the dashboard (stated in the corrected note). FIX: viewer 💬 tab + note rewritten to match the code; live-app promise now keepable once the owner reads the tab. REMAINING (owner call, not built this pass): a proactive notify (e.g. weekly digest) so a future real message is not missed. |
| NB-87 | **[MEDIUM · promise-integrity · owner-directed verification „უზრუნველყავი რომ იყოს გამართული როგორც whatsapp ასევე ემაილ"] The two MANUAL feedback fallbacks (WhatsApp + email) are correctly WIRED and live-verified, but email DELIVERABILITY cannot be self-verified — whether `NikoLearn@outlook.com` is a real, monitored mailbox is an owner-only fact.** | owner 2026-07-20 (channel-integrity task, separate from „did a parent write") | **✅ VERIFIED WORKING 2026-07-20 — owner sent a live test to nikolearn@outlook.com and RECEIVED it; all 3 feedback channels now positively proven end-to-end** | **§14 CLASS: „a consented outbound-to-owner channel whose LINK is correct but whose destination monitoring is unproven" — NB-58 promise-integrity family, deliverability variant.** LIVE-VERIFIED THIS PASS on gaшვებულ prod (not just working copy): **(1) FORM→worker** `https://nikolearn.com/niko/parent.js` carries `/v1/feedback` [FACT]; live POST→read round-trip today = HTTP 200, row landed `ts 2026-07-20T15:20:29Z` → the reliable primary pipe WORKS end-to-end. **(2) WhatsApp** live prod carries `wa.me/995593255385` [FACT]; `GET wa.me/995593255385` = HTTP 200 valid WhatsApp page; number = the owner's own → link CORRECT + routable. Owner already confirmed his WhatsApp received nothing = nobody sent, NOT broken. **(3) EMAIL** live prod carries `mailto:NikoLearn@outlook.com?subject=NikoLearn%20feedback` [FACT], consistent with privacy.html. A `mailto:` has NO server pipe to test — it only opens the parent's mail client pre-addressed; the sole checkable deliverability fact is whether `NikoLearn@outlook.com` is a mailbox the owner logs into + reads, which is owner-only (cannot log into Outlook from here). **CLASS TABLE — | channel | check method | wired? | delivery proven? |: form→worker · live POST+read · yes · YES(row landed); WhatsApp · live GET+number match · yes · link-valid (send needs a real device tap, inherent to any deep link); email · live grep of prod mailto + consistency · yes · UNPROVEN (mailbox-monitoring = owner-only).** **HONEST ENGINEERING NOTE (bitter truth):** WhatsApp+email are best-effort by nature — they deliver only if the parent has that app, taps, and actually hits Send; the FORM→worker is the ONLY channel that captures reliably without depending on the parent's other apps, and it is proven working + now readable in the stats-viewer 💬 tab (NB-86). **CLOSED 2026-07-20:** owner's own send→receive test to `nikolearn@outlook.com` succeeded → email deliverability POSITIVELY PROVEN. Final channel status: form→worker (live POST→read round-trip) ✔ · WhatsApp (live link + owner's number + HTTP 200 page) ✔ · email (owner send→receive) ✔. All three wired + verified working; the zero-feedback across all three is now a TRUSTWORTHY product signal, not broken plumbing. |
| NB-88 | **[HIGH · promise-integrity/UX · owner-reported] Owner submitted the in-app feedback FORM 3× from someone else's phone as a test and „received none" — he expected the feedback to REACH him, but the form only stores server-side (worker KV `fb\|`) with NO email/WhatsApp/push on submit, so nothing actively arrives; it is visible only by pulling the dashboard 💬 tab.** | owner 2026-07-20 („სატესტოდ გამოვაგზავნე სხვისი ტელეფონიდან ფორმა, არცერთი ტესტი არ მიმიღია"); он also had a Sonnet session attempt a „fix" | **✅ CLOSED 2026-07-20 — DEPLOYED + END-TO-END VERIFIED: worker now emails the owner on every submission; a live test landed in his INBOX** | **KEY FINDING (worker read 2026-07-20): the 3 submissions ARE on the worker** — `17:08:43 name=Test phone=599611611 os=ios`, `17:09:02 name=Gela email=gela.shonia@bivision.ge os=ios`, `17:12:11 phone=599611611 email=Tvitprof@gmail.com os=ios`. So the FORM→worker pipe works end-to-end from a real device. **§14 CLASS: „a consented inbound channel that is CAPTURED but has no PUSH to the human who must act on it" — the exact NB-86 „remaining: proactive notify" item, now owner-VALIDATED by a real-world test.** The owner's mental model = „feedback should reach me"; the system's reality = „feedback sits in a dashboard he must open." That gap IS the perceived bug. NOT a code defect in delivery. **FIX (real one, needs owner GO — infra/§15b + possibly money): on each new `fb\|` row, the worker EMAILs the owner** (email now proven working → `gela.shonia@bivision.ge` or `nikolearn@outlook.com`) via a Cloudflare-worker email path (e.g. MailChannels/Resend). Then feedback actively arrives instead of being pulled. Interim (zero-infra): the 💬 tab already shows them; owner just opens it. **CLOSE WHEN:** notify-on-submit shipped OR owner accepts pull-only. **BUILT 2026-07-20:** `notifyOwner()` added to the POST /v1/feedback success path (`cloudflare/telemetry-worker.js`, fired via `ctx.waitUntil` so it never blocks the parent; wrapped in try/catch so a send failure never breaks the submit). INERT until the `NOTIFY_KEY` secret is set, so it is safe to deploy immediately (verified: `wrangler deploy --dry-run` bundles clean, 15.40 KiB). Privacy: sends ONLY the consented feedback row, no telemetry/IP/child data. Provider = Brevo transactional API on the owner's existing account. **SHIPPED + VERIFIED 2026-07-20 (owner cleared the gate with „როცა იცი მიზანი/outcome/done, დამატებით პირობებს რატომ ითხოვ" = execute end-to-end):** (1) logged into Brevo (2FA code read from the info@bivision.ge inbox), minted API key „nikolearn-feedback", stored ONLY in `~/.claude/.bivision-creds.env` (never in repo/commit); (2) set worker secret `NOTIFY_KEY`; (3) **DELIVERABILITY ROOT CAUSE FOUND — the earlier „sender bivision.ge already verified" assumption was FALSE:** bivision.ge is NOT DKIM/SPF-authenticated in Brevo, so mail from it is silently dropped by receivers (DMARC misalignment) — empirically proven (Probe A bivision.ge = dropped; Probe B bihub.ge = inbox). **FIX: sender switched to `niko@bihub.ge` (bihub.ge IS authenticated). NEVER revert the sender to bivision.ge.** (4) deployed the worker (version `5b1ab168`); (5) **END-TO-END: two live POSTs to `/v1/feedback` → `{ok:true}` → email landed in the INBOX (not spam) at both `nikolearn@outlook.com` and `info@bivision.ge`, Georgian rendering intact; the owner's mailbox even auto-replied, proving real delivery.** Privacy held: the notify carries ONLY the consented feedback row (msg/name/phone/email/os/ts), no telemetry/IP/child data; inert if `NOTIFY_KEY` unset; wrapped in try/catch so a send failure never breaks the parent's submit. Maintenance note: Brevo API keys expire after ~90 days of inactivity — low feedback volume could let it lapse; re-mint if sends stop. |
| NB-69 | **[LOW · repo-hygiene · incidental] `git fsck` still reports broken pre-repair objects, so the recovery checkpoint stamps „Git health: FAILED" every turn.** | discovered 2026-07-17 while verifying the compact-hook FAILED flag | OPEN (DEFER until post-go-live) | **CLASS: „residue of an already-repaired failure keeps the health alarm permanently red, training the reader to ignore it" (alarm-fatigue member of the NB-58/NB-64 promise-integrity family — a signal nobody can act on stops being a signal).** Verified scope (2026-07-17, read-only): HEAD `recover/clean-2026-07-16`, `main` and every other ref INTACT (`git rev-list --objects` swept per-ref); the ONLY broken chain is stale branch `recover/s21-nb32-nb3-nb8` (+ reflog), and its missing blobs are missing in BOTH backups too (`NikoLand-git-safety-723ff31`, `git-pre-repair/20260716-145511`) — the damage predates every surviving copy; the content was already salvaged into the clean branch on 07-16. Nothing new is at risk. **FIX (deferred, owner GO per §15b asset-deletion): after the go-live milestone, prune the residue — drop the s21 ref (pointer restorable: tip `efa83c3` is intact) + `git reflog expire` + `git gc`, or fresh re-clone — so the FAILED flag becomes meaningful again. No object-store surgery before the owner's preview/go-live decision.** |
| NB-58 | **[CRITICAL · class · the audit's real headline, and the audit never saw it] PROMISE-INTEGRITY: we make a claim to a parent that the code does not keep.** The 2026-07-15 audit filed these as 6 unrelated findings across 3 categories. They are ONE class with (now) **7 confirmed instances**. | audit #4, #6, #22, #23 (fact-checked 2026-07-15) + pre-existing NB-27, NB-53, NB-57 | **OPEN — the class sweep is the NOW item.** | **CLASS (pattern language):** *a user-facing claim whose truth no test ever checks.* **Instances:** (1) **NB-57** landing promises offline in 5 places incl. an FAQ answering „დიახ." → offline the app was **unreachable** for ~45 days. (2) **NB-53** `landing.html:386` promises „PIN" → the gate is single-digit addition. (3) **NB-27** `privacy.html:246-248` promises ≤12-month retention + deletion on request → worker has neither. (4) **#4** `parent.js:163` tells a parent „შენი შეტყობინება **ჩაგვივიდა**" on a path where `localStorage` may have destroyed it (`:130` bare catch). (5) **#6** `owl.js:194` „დართე ნება (**მშობელი**)" → `:199` `grantMic(){game.micOk=true}` checks **nothing**; any child taps it. (6) **#22** the parent-visible Path milestone „trace done" (`placement.js:57`) flips from a **button tap** (`alpha.js:581`), so a child can tap → → → to the last card and finish **without drawing one stroke**. (7) **#23** coins presented as movement reward fire on a **wall clock** (`owl.js:383`), no sensor. **ROOT CAUSE (§14 escalation — 7 instances = architecture, not copies):** every one is invisible to a test suite that tests **code against code**. Not one is a crash. They are only catchable by testing **code against PROMISES**. **THE SYSTEM THIS DEMANDS = a per-PROMISE check**: enumerate every claim we make to a parent (landing.html, privacy.html, in-app copy, i18n EN mirrors) and pin each to a test that fails when the claim goes false. NB-55's guard is per-BUG; `qa/ship-scope.mjs` is per-DEPLOY; **nothing is per-PROMISE.** This is the direct answer to the owner's 45-day challenge #3 (prevention, not fixing). |
| NB-59 | **[HIGH · class] The audit called this "missing accessibility". It is not. It is INCONSISTENCY: the helpers already exist and some sites are not routed through them.** | audit #1(tail), #7, #8, #11, #12, #13, #16, #17 — all fact-checked 2026-07-15, `factcheck-a11y` | **OPEN** | **Why the reframe matters (it shrinks the job):** `kt()` (`screens.js:364`), `aria-pressed`, `aria-checked`, `role="menu"`, focus placement **all exist and all work** — they are applied at some sites and not others (colour dots ✅ / age chips ❌; feedback form focuses ✅ / PIN gate ❌; mode grid ✅ / path strip ❌). Fix = **route the stragglers through the helpers that already ship**, not "add a11y". **Sub-classes + named survivors:** **(a) no accessible NAME** — `screens.js:253` + `parent.js:106-109,412`: **zero** `<label>`/`aria-labelledby` in either file by any mechanism (WCAG 3.3.2; the placeholder vanishes on first keystroke, so the field loses its name exactly when needed) · `draw.js:86-90` every colour = identical `aria-label="ფერი"` → a screen-reader user cannot pick a colour. **← fix these two first: highest certainty, lowest cost, no design tension.** **(b) no focus/modal lifecycle** — `core.js:210-223` `render()` has no focus contract; `role="dialog"`/`aria-modal`/`Escape`/`inert` = **0 across 9 files**; `activeElement\|restoreFocus\|lastFocus` repo-wide = **0**. **(c) keyboard survivors of NB-54** — `alpha.js:405,480` click-only undo spans, `placement.js:425` click-only path step (`git show --stat 72c1b51` proves NB-54 never touched those 2 files). **(d) #11 RUNTIME-CONFIRMED** — Enter/Space on 🗑️ **navigates into the profile and never deletes**; a keyboard parent cannot delete a child's profile from the card. **NOT-a-defect rows, recorded so they are not re-litigated:** the clock's `aria-label` withholding the time is arguably **deliberate** (naming it would leak the answer); wordsearch cells ARE real buttons (the gap is naming, not reach). |
| NB-60 | **[HIGH · class] Corrective teaching rule is cut off after a fixed 2.8s — and it contradicts our OWN owner-locked pedagogy 1,100 lines above it in the same file.** | audit #26, fact-checked 2026-07-15 (`factcheck-trust` rated it the highest-value fix in its set) | **OPEN — cheapest high-value fix on the board** | **CLASS:** *inconsistent advance-gating across strands* (not 2 instances). `games.js:1370` + `:1494` `setTimeout(…,2800)`. Payload at `:1454` is a **3-clause formula with 4 positional referents, in Georgian**: „წესი: (ზედა + ქვედა-მარცხ.) × ქვედა-მარჯვ. = ცენტრი. ანუ (7 + 3) × 2 = **20**". A 7-year-old reading at ~60-80 wpm plausibly needs **8-15s**; it gets 2.8. It severs the single highest-value moment in the app: the corrective explanation after a 2nd miss. **`games.js:272` is owner-locked 2026-06-13 and says verbatim: "Advancing happens ONLY on a tap, never on a timer."** The audit cited that block as evidence FOR its complaint — it is the fix. **Fix = route both timers through the existing tap-gated pattern at `games.js:268-269`.** Scope: Kings pattern + reasoning strands only, not the core loop — which is why Sev-2, not 1. |
| NB-61 | [MED · class] Tap targets under 44px on the controls a 4-year-old taps most | audit #24, fact-checked 2026-07-15 | **OPEN — scoped, the audit's list was padded** | **CLASS:** *no shared tap-target token, so compliant and non-compliant rules sit side by side in one file* (`styles.css:754` `.dw-tool` 44 ✅ / `:765` `.dw-color` 36 ❌). **Real:** `.dw-color` 36px (`:765`, palette dots, `padding:0`, 7px gaps) — **tapped constantly by 4-year-olds**, and a mis-tap picks the **wrong colour** rather than missing · `.dw-size` 38px · `.dw-stamp` 42px. **Padded (recorded so the count is not re-inflated):** `.app-lang`/`.cb-x`/`.limit-chips` are rare or **adult**-operated; **`.talk-back` is DEAD CSS** — named check: `grep -rn talk-back --include=*.js` hits only `./www/` + `./android/` build mirrors, **not** `./niko/talk.js`. **Owed runtime:** `.dw-color` has `border:3px solid transparent`; box-sizing unverified → rendered box is 36px (border-box) or 42px (content-box). **Under 44 either way, so the verdict is safe** — measure only to quote an exact number. |
| NB-62 | [MED] SW install swallows every per-asset error, then `skipWaiting()`s a possibly half-populated cache to live clients | audit #9, verified 2026-07-15 | **OPEN** | All 4 mechanical sub-claims true: per-asset `.catch(()=>{})` `sw.js:63` · `skipWaiting` `:64` chained after a `Promise.all` that **can never reject** · unconditional wipe of every non-current cache `:67` · reload `index.html:133-137`. **One correction to the audit's picture:** the reload IS guarded (`if(reloaded||!hadController) return;`) so it **cannot loop**. **Runtime owed:** force an asset failure against a running SW and see whether a half-populated cache ever reaches a client. |
| NB-63 | [MED] `lang=en`: first-run headings translate, the only tappable things do not | audit #5, §6c-verified 2026-07-15 | **OPEN** | The audit merged two things needing **different fixes**, and that merge was its real miss. **First-run = a FALLBACK, not hard-coding** → fix is **DATA**: headings („Who learns through play?") translate, all three age buttons stay „3-5 წელი / 6-8 წელი / 9-12 წელი" — untranslated **exactly where a parent must tap**. **`pwa-install.js` = a genuine BYPASS** → fix is a **CALL**: it never invokes `applyLang`, so 0 of 10 strings translate. Welcome screen is **fully fine** — the English button is NOT broken. Screenshots: `output/2026-07-15-აუდიტი #5 *`. **`pwa-install` half still owed** (needs the install prompt to surface). |
| NB-57 | [CRITICAL · child-facing] Opening the installed app **with no internet** served the marketing landing page instead of the app, and every way out led back to it. LIVE on nikolearn.com since `8ed8f55` (v1.320). | audit finding #10 (Codex UX/UI adversarial 2026-07-15), rated Sev-3 by the audit → **re-rated Sev-4 by us**: the audit rates the code, we rate the harm to a child. Independently re-verified by me against the real files before any fix (§10 lead duty). | ✅ FIXED + §6c RUNTIME-VERIFIED 2026-07-15, commit `3fb170a` · **LIVE v1.366 2026-07-20** (`ignoreSearch` marker verified on live sw.js). **What it did to a child:** parent opens `landing.html` → the SW writes that marketing page over the app's cache slot → child later taps the icon offline → gets the ad page → taps „დაიწყე უფასოდ" → the ad page again. **ჩიხი.** Offline, the app was unreachable. **Two mechanisms, both required, each fixed separately:** **(a)** `sw.js:90` wrote EVERY successful navigation to the literal key `'./index.html'`, so any other document overwrote the shell — and `niko/screens.js:132` redirects an unauthed visitor to `landing.html` **by design**, which makes the poisoning the NORMAL path, not an edge case. **(b)** `manifest.json:5` start_url is `index.html?app=1&src=pwa`; an exact `caches.match` against a key stored without a query **always** missed, so every offline launch fell through to the fallback — landing straight on the poisoned key. Sweep found `landing.html` carries **5** CTAs to `index.html?app=1`, so this was the ordinary route, not a corner. **FIX:** cache each document under its own url (`'./index.html'` is now written only by the precache, which cannot be anything but the shell) + `{ ignoreSearch: true }` on the offline navigation match. **§6c PROVEN BOTH WAYS in a real browser** (a cache class cannot be proven by reading — static reading would have shown me the same two lines and told me nothing): served over http, installed the SW, ran the identical sequence on OLD and NEW code. OLD → key `./index.html` = 36117 bytes, title „NikoLearn: ისწავლე თამაშით" = the landing page, `POISONED: true`; offline launch → landing; CTA → landing again (loop). NEW → key = 9311 bytes, title „NikoLearn", `isAppShell: true`, `POISONED: false`, landing cached under its own key; offline launch → **the app** („მოგესალმები", v1.365). Both screenshots looked at, kept: `output/2026-07-15-NB-57 ოფლაინ *`. | **CLASS: „a cache write whose KEY is a string literal instead of the request that produced it."** Sweep of all 4 `.put` sites in `sw.js`: `:90` navigate = the ONLY literal → **fixed**; `:63` precache keys on `u` from ASSETS (the url IS the request, correct by construction); `:75` audio and `:106` js/css already key on `e.request` ✔. Repo-wide grep for `.put('` → one hit, `cloudflare/telemetry-worker.js:236` = **N/A, named reason**: a KV namespace, not the Cache API, with a deliberate composite key. **SECOND, SEPARATE CLASS (survives fixing the key, so swept on its own): „a request carrying a query matched against a key stored without one."** Surface = every `caches.match` in `sw.js`, each with a named check (absence ≠ clean): `:101` navigate → **the instance, fixed**; `:74` audio → no query (grep of `audio-manifest.js` urls, 0 hits); `:116` js/css → no query (every `src`/`href` in `index.html` + `landing.html` is a plain path; `opentype.min.js` likewise); `:122` fonts/images → no query. The only queried local urls in the tree are `index.html?app=1` navigations = the fixed path. **ROOT-CAUSE:** not patched in N places — the literal is gone, so the class cannot recur through this door. **GUARD (NB-55 rule: every owner-visible bug leaves the test that would have caught it):** `qa/qa-check.mjs` check 8 `sw-cache-key`, discriminator-proven 3 ways (fixed → pass · literal key injected → P1 exit 1 · `ignoreSearch` removed → P1 exit 1). **The guard's own first version was an instance of the class it exists to catch** — it grepped for the WORD „ignoreSearch" and my own explanatory comment satisfied it, so it passed a file where the real call was gone. It now strips comments and matches the CODE, and that mistake is written into the check as a comment so it cannot recur. **⚠️ THIRD CLASS, found last and the most important — NB-57 is ALSO an NB-27 promise-integrity instance:** `landing.html` promises offline in **five** places, one of them an FAQ that asks „მუშაობს ინტერნეტის გარეშე?" and answers **„დიახ."** (`i18n-landing.js` ships it to English parents as „Works offline"). For ~45 days that answer was **false** — offline, the app was not merely degraded, it was unreachable. Same shape as NB-53 (public copy promised a PIN the code never implemented) and likely audit #25. **The product signal (§4), and it is bigger than the fix:** offline is a headline promise on the landing page and **no test, CI gate, or QA step has ever launched this app offline**. The bug did not survive 45 days because it was subtle — it survived because the one thing we promise loudest is the one thing we never checked. NB-55's guard is per-bug; this asks for a per-PROMISE check: every public claim needs a test that can fail. Logged as the strongest input to the NB-27 class sweep (roadmap NOW) — not fixed here, and not silently dropped. |
| NB-56 | [HIGH · epistemic/measurement] The rebuilt preview bench deployed green and served the app correctly, but **nothing in the build identified it as a preview**: footer read `v1.365` = byte-identical to nikolearn.com; the cache-first SW was keyed on that unchanged version; and the build POSTed to the **production** telemetry Worker. | agent-discovered 2026-07-15 (§6c render pass on the preview I had just shipped — found by LOOKING at the screenshot, not by reasoning) | ✅ FIXED + VERIFIED 2026-07-15, commit `89867d8` (preview repo only; covered by the owner's PI-87 „Go" — this is that bench being made honest, not a new feature). **VERDICT: HIGH, and the severity is about EPISTEMICS, not pixels.** The app worked; the *bench* lied. Three mechanisms, one class: **(a)** the footer version is the owner's own deploy-landed signal (archive 2026-06-02: „he checks the footer to confirm a deploy landed") and branch work never bumps it → the one surface built for him to confirm a change could not tell him WHICH build he held. **(b)** `sw.js` is cache-first keyed on that version (`CACHE='nikolearn-1.365'`), so a SECOND preview push would be served from the FIRST one's cache → he tests old code while I call it new. This is the failure that matters: it does not look like a bug, it looks like a passed test. **(c)** `analytics.js:102` → `nikolearn-t.bivision.workers.dev` → every tap on the preview writes into the real numbers and burns the KV daily write budget the 06-28 batching exists to protect: **testing would move the metric being tested**, and PI-47/measurement is the exact thing the owner is angry about. **FIX:** `deploy-preview.yml` stamps the commit SHA into APP_VERSION + landing footer + SW cache name, and repoints telemetry to an inert same-origin path — each stamp **asserted**, build dies on a silent no-op. **DISCRIMINATOR PROVEN BOTH WAYS** (locally, before pushing): stamps apply (`v1.365-preview-a73f76e`, `nikolearn-preview-1.365-a73f76e`, endpoint replaced) AND the asserts FAIL on unstamped input — a guard never seen to fail is not a guard. | **CLASS: „a non-production build is indistinguishable from production — by eye, by cache key, and by backend — so anything confirmed on it is unanchored, and worse, testing it mutates production state."** Sweep = every axis on which preview could impersonate production, enumerated from the build, not guessed: **identity** footer/APP_VERSION → stamped ✔ · landing footer → stamped ✔ · **staleness** sw.js CACHE (the only `nikolearn-` occurrence, grep-verified) → SHA-keyed ✔ · **backend writes** telemetry ENDPOINT (only outbound POST in the tree, grep `fetch\|sendBeacon`) → inert + asserted absent from the whole `_site` ✔ · **domain** CNAME → already asserted absent + live-verified 404 ✔ · **search** noindex + robots → already in the build + live-verified ✔. **ROOT-CAUSE NOTE:** the honest fix is that a preview should differ from production **by construction**, not by a list of seds a future file can slip past. Logged OPEN, not built this pass: the two builds share one allowlist maintained by hand in two files — a single build script parameterized by target would make drift impossible. Recommended, deliberately not done under a same-turn deadline. **PROCESS SIGNAL (the real lesson):** I shipped this bench and called the deploy green. It WAS green. Green meant „the workflow exited 0", not „the owner can trust what he sees" — §14 „absence ≠ clean" one level up: the CI was clean about the thing it checked, and silent about the thing that mattered. Only the §6c „render it and LOOK" step caught it, on my own work, minutes after I declared it done. |
| NB-55 | [CRITICAL · deploy integrity] `actions/checkout` died on the preview runner — **`File name too long`** — before any build step, so nothing deployed. Two owner strategy .docx added by `b9f0b9e` **in this same branch** had 299- and 269-byte filenames. | agent-discovered 2026-07-15 — **found BY the rebuilt preview, 16 seconds after it came back to life** | ✅ FIXED + GUARDED + VERIFIED 2026-07-15, commit `a73f76e`. **VERDICT: CRITICAL, and it was aimed at production.** `main` does not carry those files, so nikolearn.com is fine TODAY — it breaks **on merge**, and this branch is the thing waiting to merge. **ROOT:** `NAME_MAX` on ext4 is **255 BYTES, not characters**; Georgian is 3 bytes/char in UTF-8, so a Georgian filename dies at ~85 characters — a length that looks perfectly normal in Explorer. That asymmetry (legal on the NTFS box we author on, illegal on every Linux runner we deploy from) is exactly why it shipped unnoticed. **FIX:** both files renamed (299→109, 269→161 bytes; also fixed a Latin `i` typo in one). **ROOT-CAUSE ESCALATION per §14 — the rename is the instance, the guard is the fix:** new release-blocking `repo-portability` check in `qa/qa-check.mjs` (check 7) walks every tracked path for (i) basename > 255 bytes and (ii) case-only collisions (`Foo.js` vs `foo.js` collapse on a case-insensitive checkout — same class, other mechanism). **DISCRIMINATOR PROVEN BOTH WAYS:** long name restored → `[P1] repo-portability: filename is 299 bytes… ⛔ GATE FAILED`, exit 1; short name back → `✅ all checks passed`, exit 0. **METHOD NOTE (my own bug, kept because it is the interesting part):** my first measurement reported 1128 bytes for a 299-byte name — two compounding errors, `awk length()` counting CHARACTERS in a UTF-8 locale, and `git ls-files` **octal-escaping** non-ASCII (`\341\203\236`) so I was measuring the escaping, not the filename. A measurement that is wrong in the same direction as the bug still „finds" it and teaches you nothing true. The shipped check uses `git ls-files -z` + a Buffer + `Buffer.byteLength()`, and **that mistake is written into the code as a comment** so it cannot recur. | **CLASS: „the repo holds a path that is legal on the authoring OS but ILLEGAL on the CI runner → `checkout` itself fails, so the build dies before any check runs and every commit after it is undeployable."** Sweep = all 1897 tracked paths, measured in real UTF-8 bytes: **2 instances**, both in `01_NikoLearn_Strategy_drafts/` (the folder NB-51 had just un-ignored — i.e. NB-51's fix is what exposed this; the files were always this long, they were simply never tracked before), both fixed ✔ · case-only collisions: 0 across 1897 ✔ · other Linux-illegal path shapes (`<>:"|?*`, trailing dot/space, reserved names) = **NOT swept this pass**, logged OPEN below · path-length total (>4096) = 0, checked ✔. **OPEN (logged, not built):** the guard covers byte-length + case collisions only; Windows-reserved names and illegal characters are the same class and would fail the same way in reverse. Cheap to add, deliberately not bundled into a fix that needed to ship in-session. **§14 „ABSENCE ≠ CLEAN" — DEMONSTRATED ON OURSELVES, which is why this row exists:** the pre-push gate **ran on the broken commit and printed „all checks passed"**. It was green about the app while the repo could not be cloned. Nothing in 25 QA files looked at paths at all, so no check was failing — there was no check. **This is the first instance of the standing rule the owner's 45-day challenge demands: every bug found must leave behind the test that would have caught it.** |
| NB-53 | [HIGH · child-safety + promise-integrity] `landing.html:386` promises unconditionally „მშობლის სივრცე PIN-კოდითაა დაცული, **ბავშვი ვერ შევა**", but a PIN exists only if the parent set one; the default door was `7+5` — an addition **this app itself teaches its 4-12 year olds** — and the „forgot PIN" BACK door (`14×7`, above the teaching ceiling) was HARDER than the front door. | Codex UX/UI adversarial audit 2026-07-15, finding #2 (Sev-4) — fact-checked by me | ✅ **SAFETY HALF FIXED + §6c-VERIFIED 2026-07-15 · LIVE v1.366 2026-07-20** (owner GO deploy; live-verified: multiplication-gate marker `ri(11,19)` present in nikolearn.com/niko/parent.js). **VERDICT: CONFIRMED, Sev-4 upheld.** The code convicts itself — `parent.js:11` comment read „kid-proof for toddlers **but not a real secret**", i.e. the gap was known and shipped anyway. Harm was concrete, not theoretical: behind that door sit the **daily screen-time limit** and the PIN setup, so the child the limit is FOR could switch it off. **FIX (root, at the chokepoint):** `openGateMath()` — `hard` now selects the RECOVERY COPY only, never the difficulty; BOTH paths use the 2-digit × 1-digit above-ceiling problem the owner already set as the bar on 2026-06-14. One rule, one place, no second difficulty tier to drift. **§6c GROUND TRUTH (rendered at 390px, not reasoned):** no-PIN gate → `17 × 8 = ?` with the normal „მშობლის სივრცე" copy, 3-digit answer, screenshot LOOKED at (`niko-shot/nb53-gate-nopin.png`) ✔ · PIN set → still routes to the PIN gate, no regression ✔ · recovery → keeps its own „მშობლის აღდგენა" copy ✔ · 0 pageerrors. 8/8 asserts PASS. (Method note: my first run reported 1 FAIL that was MY test bug — `window.gate` is undefined because `let`-globals never attach to `window`. A test looking at the wrong place proves nothing; fixed to a bare `gate.ans`.) **OPEN → the HONESTY half is NOT fixed:** with no PIN set the door is now genuinely kid-proof, but it is still a **math problem, not a PIN**, so the landing sentence stays literally false. Two ways to make it true — (a) reword to cover both states, (b) prompt for a PIN on first parent entry so the promise becomes true by construction. **RECOMMENDATION: (a)**, cheaper and adds zero first-run friction. **GATED: customer-facing copy (§13 gate 4) + Georgian (§6f = Gemini pass, then owner validates).** Not shipped unilaterally. | **CLASS: „public copy states a specific factual promise the code does not implement"** = the **NB-27 class, second confirmed instance** (NB-27: `privacy.html:246-248` promises ≤12-month retention + deletion on request; worker `fb\|` rows have no TTL and no deletion route). Two independent instances in two unrelated subsystems (marketing↔auth, privacy↔storage) → per §14 root-cause escalation this is architectural, not two bugs: **nothing links a public claim to the code that must honour it, so copy and behaviour drift silently and only an adversary notices.** The audit finding this class independently, while the roadmap already ranked „NB-27 promise-integrity" as NOW #1, is mutual confirmation from two directions. **FULL CLASS SWEEP = the NOW #1 item, deliberately NOT collapsed into this row:** every factual promise in `landing.html` + `privacy.html` must be enumerated and checked against code (child-safety, ads, data, offline, retention, on-device). Scope belongs to that sweep; this row fixes only its own instance and hands the class over. Guard to build there, not here: a promise↔code assertion in `qa/` so a claim without an implementation fails CI instead of shipping. |
| NB-52 | [MED · exposure/epistemic] `deploy-pages.yml:50` publishes the whole `share/` dir → `share/7731b5f78d82/` serves 6 live pages of strategy/pricing/grant/diaspora material behind nothing but an obscure URL + noindex — and our own SESSION-HANDOFF asserted this exact exposure was „VERIFIED NON-ISSUE". | Codex UX/UI adversarial audit 2026-07-15, finding #3 (Sev-3) — fact-checked by me | ⏳ **PARTLY FIXED 2026-07-15; one item OWNER-GATED (§15b CI/CD).** **VERDICT: audit CONFIRMED on the fact, re-rated Sev-3→Sev-2 on the risk, and its framing corrected.** The audit read it as accidental leakage; it is **deliberate** — `b34da67` (06-08) „share: NikoLearn strategy docs (private noindex link for owner mobile view)", and `ab4387e` (NB-24) put `share` on the allowlist ON PURPOSE so the owner's mobile link would survive the denylist→allowlist flip. **MEASURED risk, not assumed:** all 6 URLs return **200 live** [FACT, fetched] · all 6 carry `noindex` [FACT] · path is 48-bit random = not brute-forceable [FACT] · repo **PRIVATE** so the hash never leaked via git [FACT, `gh repo view`] · **no live page links to `/share/`** (checked `/`, landing, privacy, changes, validate) [FACT] · **archive.org has never captured it** (CDX empty) [FACT] · no sitemap.xml (404) [FACT]. Residual vector = the link itself being forwarded (WhatsApp/Telegram/email previews prefetch URLs) — which is precisely what the link is FOR [HYPOTHESIS, low probability, nonzero]. Blast radius = business strategy, **zero child PII** — that scoping is why this is Sev-2 not Sev-3. **DONE (autonomous, reversible, non-CI/CD):** `robots.txt` → `Disallow: /share/` (noindex stops *indexing*, not *fetching*; this closes the crawler invitation `Allow: /` was issuing) + its false comment replaced; `_config.yml` annotated as a **DEAD CONTROL** (Jekyll never runs — workflow uploads a pre-assembled `_site`, so its `exclude:` list has never executed once). **OPEN → OWNER GATE (§15b = CI/CD change):** drop `share` from `for d in niko assets movement share` in `deploy-pages.yml`. **RECOMMENDATION: retire it.** The docs are strategy v2 from 06-08/06-10, superseded by the current roadmap = 5 weeks stale; the link's job is done. **DEFAULT if the owner is silent: retire at the next merge** (deletes nothing — the files stay in the private repo, only the public serving stops). Only reason to keep: he still reads them on his phone. | **CLASS: „owner-private business content is served from the PUBLIC production origin, access-controlled only by URL obscurity + honor-system `noindex` — and a config file that cites a mechanism which never executes makes the control look real."** Sweep = every entry the allowlist publishes (that IS the full exposure surface — enumerated from `deploy-pages.yml`, not guessed): `share/` **6/6 files flagged** (15-30 sensitive terms each) → FIXED/gated ✔ · `niko/` 1517 files, 30 text-scanned → 2 hits **verified false positive by reading them** (`kings-content.js` = kids' money word-problems „25 ლარი, 8 ლარი დახარჯა"; `i18n-strings.js` = a future-paid-plan UI string) ✔ · `assets/` 15 files → 0 ✔ · `movement/` 1 file → 0 ✔ · 5 root HTML (index/landing/privacy/changes/validate) → 0 sensitive terms each ✔ · `_config.yml` = dead control, annotated ✔. **Method note (§14 „absence ≠ clean" caught live):** my first sweep printed „share: NONE" — a **false clean** caused by a missing regex `g` flag returning only the first match. Re-run with `/gi` surfaced 6/6. A sweep whose method is broken reports clean, not broken — the exact trap that produced the s20 claim this NB retracts. **ROOT-CAUSE ESCALATION:** third recurrence of the L14 container-vs-content class (PI-sweep → NB-51 `.gitignore` rename-orphan → this). Recommended guard, NOT built this pass (logged OPEN): a `qa/qa-check.mjs` assertion that greps every allowlisted path for sensitive terms, so a future „internal file on the public origin" fails loudly in CI instead of surviving as a confident sentence in a handoff. |
| NB-51 | [HIGH · repo/backup integrity] The owner's strategy drafts silently lost their git backup: `0694e4e` added `01_NikoLearn_Strategy_drafts/` + `*.docx` to `.gitignore`, reversing `5b07582` „Backup: track strategy drafts". The .docx exist nowhere else, so one `git clean -xdf` would have destroyed them permanently. | agent-discovered 2026-07-15 (s21 crash recovery) | ✅ FIXED 2026-07-15, commit `b9f0b9e` (internal/reversible → autonomous per §7). **ROOT:** top-level folders were renamed with sort prefixes (`backups/`→`00_backups/`, `brand-preview/`→`02_brand-preview/`, `auditbyagents/`→`05_auditbyagents/`) but their `.gitignore` patterns still named the OLD path, so each pattern silently stopped matching. The repair guessed at the new names and over-corrected, ignoring a folder that was deliberately TRACKED — and did it inside an unrelated a11y commit, so it got no review. **No owner instruction for this exists in any session transcript** (grepped all of 2026-07-15) = unrequested agent decision reversing a deliberate one. **FIX (root-cause, not per-copy):** patterns are now prefix-tolerant (`*backups/`, `*brand-preview/`, `*auditbyagents/`) so a future rename cannot orphan them; strategy drafts un-ignored + tracked, with an in-file comment recording why they must stay tracked. **VERIFIED:** `git check-ignore -v` confirms each renamed folder matches again; 21 files / 644,846 bytes committed, 0 empty; 12 .docx that were NEVER tracked (s18's „untracked = 0 backup" flag) now have backup; publish-safety re-verified first — `deploy-pages.yml` is an allowlist („publishes only if explicitly named here"), so tracking can never publish these; NB-33 secret-scan guard re-self-tested (exit 0 clean / blocks a planted secret). | **CLASS: "a path-shaped config entry (.gitignore pattern) keeps naming a path that a rename has moved → the rule silently stops applying, and nothing fails loudly."** Cuts BOTH ways: ignore-meant folders became exposed, tracked-meant folders became ignored. Sweep of all 5 renamed top-level folders: `00_backups/` orphaned→re-pinned ✅ · `01_NikoLearn_Strategy_drafts/` wrongly ignored→un-ignored+tracked ✅ · `02_brand-preview/` orphaned→re-pinned ✅ · `04_competitor-profiles/` rename unstaged→staged as rename ✅ · `05_auditbyagents/` already re-pinned s20 ✅. Same class checked in other path-shaped configs: `deploy-pages.yml` allowlist = names only live paths, verified by the 21-file publish count ✅ N/A. **OPEN (logged, not blocking):** no guard detects a dead ignore pattern — a `git check-ignore` assertion in `qa/qa-check.mjs` would make this class fail loudly instead of silently; recommended, not built this pass. |
| NB-50 | [MED · visual/legibility] Font size „ძლიან დახუნტავს" (shrinks a lot) — text is not stable; owner wants it pinned at the default readable size | owner 2026-07-15 chat | ✅ FIXED + §6c-VERIFIED 2026-07-15 (CUSTOMER-FACING → SHIPPING LIVE v1.365, autonomous per Policy Amendment d = reversible CSS stabilizer, non-financial/non-reputational). **ROOT:** neither production stylesheet ever set `text-size-adjust`, so mobile browsers were free to auto-inflate/shrink text off the author default (iOS text-inflation + orientation-resize) → type read as "unstable / shrinking". Base sizes themselves are fine (app body 17px, landing 16-17px, responsive `clamp()` on headings). **FIX:** pin `-webkit-text-size-adjust:100%; text-size-adjust:100%` at the `html` root of BOTH `niko/styles.css` (app) and `assets/landing.css` (landing). §6c: rendered app (seeded age-8) + landing at 390px mobile, asserted computed `text-size-adjust=100%` on html+body and stable default `font-size` (app 17px / landing 16px), before/after screenshots LOOKED at (`scratchpad/font-app.png`,`font-landing.png`) — grid + hero render at correct stable sizes, zero pageerror. | **CLASS: "font size is not pinned to the author default → the browser's own text-size-adjust algorithm can rescale it (unstable type)."** §14 sweep of every page that ships text: the deploy allowlist (deploy-pages.yml) publishes index.html→`niko/styles.css` + landing.html→`assets/landing.css` (privacy.html reuses landing.css) = BOTH now pinned at root ✔; `*{}` reset carried no size-adjust before ✔ fixed; non-published demos (writing-demo/v2-prototype) = N/A (never deployed). Root-fix at the html chokepoint (inherited app-wide), not per-component. |
| NB-49 | [P1 · audio/trust] v1.362 profile-add / entry screen (`screen home`, „ვინ სწავლობს?"): a Georgian voice autoplays „მოძრაობა" (the Movement subject word) on load — an errant/mis-wired AUDIO_MANIFEST clip firing on the profile chooser; wrong word on the FIRST trust surface a parent/child hits | owner 2026-07-14 chat (live v1.362) | ✅ FIXED + §6c-VERIFIED 2026-07-15 (branch `fix/nb49-unlock-silent`; CUSTOMER-FACING fix → SHIPPING LIVE v1.363 via tools/live-push.mjs, autonomous per Policy Amendment d = reversible + non-reputational). **ROOT:** the NB-20 first-gesture AUDIO UNLOCK (`audio.js` `_primeAudio`) played a REAL voice clip (`clip_294` = „მოძრაობა") at `a.volume=0`, intending an inaudible unlock. **iOS Safari IGNORES programmatic `.volume` (read-only there)**, so on a phone the very first tap on the profile chooser played „მოძრაობა" ALOUD (desktop, which honours volume, was silent → why it slipped). **FIX:** unlock with an in-code all-zero-PCM silent WAV (data URI) + `muted=true`, inaudible on EVERY platform; `clip_294` stays in the manifest for legitimate Movement voicing. §6c GROUND-TRUTH: rendered local profile chooser, spied `HTMLMediaElement.play`, fired the first gesture → the ONLY play was the silent data URI (muted:true, volume:0), ZERO `clip_294.mp3` fetch; screenshot LOOKED at (`docs/nb48-shots/nb49-profile-after.png`, profile renders normally on warm bg). npm test v1.362 GATE PASSED, 0 findings. | **CLASS: "a 'silent' autoplay/unlock that relies on `a.volume=0` to mute is NOT silent on iOS, where `.volume` is read-only → the audible asset leaks."** §14 sweep of every audio-play path (grep `.volume` / `play(`): `_primeAudio` = the ONLY site using volume=0-for-silence → FIXED at the chokepoint (every later clip routes through the same unlocked element) ✔; all other `.volume` sets = 0.85 (intended audible) ✔; profile/home render (screens.js) calls NO `speak()`/`playClip()` = silent by construction ✔; Movement break auto-voices BY DESIGN (owl.js, not the profile screen) = N/A. Root-fix = a genuinely silent asset, not a volume trick. |
| NB-47 | [MED · visual/UX] v1.360, 7yo profile: card TEXT sits inside a **dark frame/plate** (new in v1.360) — owner likes the cards + text but strongly dislikes the dark plate; wants a UI/UX-designer challenge on it | owner 2026-07-14 chat (live v1.360) | ✅ FIXED + §6c-VERIFIED 2026-07-14 (branch `fix/nb47-worldname-darkink`; CUSTOMER-FACING → SHIPPING LIVE v1.361 via tools/live-push.mjs). **ROOT:** the dark plate is `.subj.wld .w-nm{background:rgba(22,15,5,.55)}` = MY OWN NB-40 fix (the world-name needed a contrast surface on the saturated tiles; white-on-tile measured 1.79-2.77 FAIL). **Design-challenge result (what a UI/UX designer arrives at):** DROP the plate, switch the name to DARK warm-ink (#241708) + a light lift-shadow. WCAG calc over each hue's gradient-mid: dark-ink = **5.92-9.13 on ALL 8 world hues** (BEATS the plate's own 6.29-8.31) while removing the frame, and unifies with the subject-card `.s-name` dark-ink pattern. §6c: rendered the live world-home on a 7yo profile, before/after screenshots LOOKED at (`output/nb47-before-plate.jpeg` / `nb47-after-darkink.jpeg`) — plate gone, every name crisp on its tile. | **CLASS: "world-name = the only white-on-saturated-tile label needing a per-screen contrast patch."** §14 sweep of every card-name surface: `.w-nm` world names = NOW dark-ink ✔; `.s-name`/`.s-sub` subject cards ALREADY dark-ink-on-light (#8F3A16, no plate) ✔ = the pattern NB-47 unifies toward; `.m-name` math-inner dark ✔; kings-gold reuses the same `.w-nm` rule ✔. No other white-on-tile label survives. **The DEEPER class is shared with NB-48** (redesign hardcoded hex) — see that row. |
| NB-48 | [HIGH · class-level] Bar/dock COLOR picker (🎨, cycles sunlit/playground/calm) does NOTHING visible anywhere — owner „არაფერს აკეთებს არსად"; establish intended behavior + how it should work now | owner 2026-07-14 chat (live v1.360) | **✅ REVIVE IMPLEMENTED 2026-07-14 (Opus direct — owner reversed the Sonnet delegation: „მოდი არ გარისკო და ისევ opus"). SCOPE = chrome-only: dock re-tokenized (added `--dock-bg`/`--dock-idle`/`--dock-active` to sunlit/playground/calm blocks; 5 dock hex decls → `var()`). Sunlit anchored BYTE-IDENTICAL (rgba(255,255,255,.82)/#B8A88E/#FF8A00); playground = cool glass + coral-red active; calm = cool glass + blue active. Subject/world card hues + NB-47 ink kept FIXED as identity (theming them would alter the look owner likes) → deliberately NARROWER than „home/worlds/dock". §6c live-local VERIFIED across 3 themes (computed-style + render + cycle 🎨 + screenshots `docs/nb48-shots/`); QA gate 0 findings; commit `9124971` on `fix/nb48-theme-tokens`. **AWAITING: owner live-push gate + confirm chrome-only vs bolder full re-skin.** My earlier default (remove) is SUPERSEDED.** INTENDED: the 🎨 dock button (index.html:44 → `cycleTheme()`, tweaks.js) swaps `data-theme` sunlit→playground→calm; the token system (`[data-theme]` blocks styles.css:16-94) redefines --bg/--card/--ink/--primary per theme, so the app should re-skin. ACTUAL (proven live — cycled all 3 on the 7yo world-home): only `bodyBg` shifts by an imperceptible oklch tint (0.965→0.975→0.96); card gradients + dock stayed BYTE-IDENTICAL. WHY: the v1.319 home reskin + v1.357 worlds redesign hardcoded HEX (`.subj`/`.whue-*`/dock) instead of `var(--token)`, so the FIRST + most-seen screen ignores the theme → from where the child/owner looks the control is dead. NOTE: the token system is NOT globally dead (868 var() uses; quizzes/overlays/body DO respond) — it's home/worlds/dock that were orphaned. **RECOMMENDATION (default if owner silent): REMOVE the dead in-app 🎨 control now** (a control that visibly does nothing erodes trust; reversible) and treat "real selectable app themes" as a LOW-priority future feature (re-theming the 3-4 looks the owner LIKES through tokens = real work of questionable value for a kids' app vs core learning). Alternative if owner WANTS color choice: revive by tokenizing home/worlds/dock (larger; must preserve the current look). **Owner's call MADE 2026-07-14: keep-and-build (REVIVE); executed by Opus (chrome-only) after owner reversed the Sonnet delegation.** | **CLASS: "post-redesign screen hardcodes hex instead of the var(--token) design system → the [data-theme] switch is inert on that surface (NB-48) and contrast must be re-patched per-screen (NB-47 plate)."** §14 sweep — every screen's primary surfaces, tokens vs fixed-hex: home cards `.subj` (styles.css:333-443) = FIXED HEX → theme-dead ✗; worlds `.subj.wld`+`.whue-*` (1775-1794) = FIXED HEX → theme-dead ✗ (+drove NB-47); dock `.bottomnav.dock` = FIXED HEX rgba(255,255,255,.82) → theme-dead ✗; quiz/game surfaces (var(--card)/var(--primary) heavy) = TOKENS → responsive ✔; overlays ([data-theme] .overlay 636-637) ✔; body var(--bg) ✔ (the one thing that changed). Root-fix if revived = re-tokenize the 3 dead surfaces (ONE consolidation, not per-hue). |
| NB-44 | **[P1 correctness] Word Search („სიტყვების ძებნა") puzzle not provably solvable** — owner tried to find რძე/პური/ვაშლი/ბანანი, found ONLY რძე + ბანანი; ≥1 target word appears not to be placed/findable in the grid → a child can hunt for a word that isn't there (trust-killer) | owner 2026-07-14 chat | ✅ FIXED + §6c-VERIFIED 2026-07-14 (branch fix/qa-nextsteps-nb4-38-39-40-41; CUSTOMER-FACING → ✅ SHIPPED LIVE v1.360 2026-07-14 via tools/live-push.mjs, §6c live-looked on nikolearn.com). **Owner's hypothesis (a target word silently unplaced) DISPROVEN by simulation** (5000 builds: all 4 food words placed every time). TWO real root causes found + fixed in the rewritten `niko/wordsearch.js`: (1) **DISCOVERABILITY** — the old build could render a clue for a word it had not actually placed; the NB-44 rewrite GUARANTEES solvability: `_build` grows the grid (young ≤9 / reader ≤12) and retries `_tryLayout` (ALL-or-nothing placement, 8×/size) until EVERY clue word is placed, and clues render ONLY placed words → a child can never hunt for an absent word. (2) **SELECTABILITY EDGE** — `wsTap` had `if(cell.classList.contains('found')) return`, which made a word unselectable when it shared an endpoint cell with an already-found crossing word (24/25 builds failed the endpoint-tap test); removed the guard (selection uses only the two tapped endpoints) → 25/25. §6c: 25 food-pack builds each place + select all 4 (რძე/პური/ვაშლი/ბანანი), dock screenshot LOOKED at (all 4 clue chips present). Behavioral gate `qa/_wordsearch.mjs` 23/23 PASS; npm test v1.359 0 findings. | **CLASS: "generated puzzle/quiz ships without a proven-solvable invariant."** §14 sweep of every generator: word-search grid = NOW carries the invariant (build-until-all-placed + clue↔placement 1:1) ✔; genMath/genRebus/genModel reroll on answer-in-stem (NB-39) and their answer is the missing term (never absent) ✔; math reroll / KMX pools = answer always derivable ✔. Guard lives at the choke-point (`_build` retry loop + clues rendered from `_words` only), not per-instance. |
| NB-43 | [HIGH] Word Search: no Niko owl tutor presence + no timed hints — owner got stuck (2/4 words) with zero scaffolding; wants the tutor to coach + surface a hint after a delay | owner 2026-07-14 chat | ✅ FIXED + §6c-VERIFIED 2026-07-14 (branch fix/qa-nextsteps-nb4-38-39-40-41; CUSTOMER-FACING → ✅ SHIPPED LIVE v1.360 2026-07-14 via tools/live-push.mjs, §6c live-looked on nikolearn.com). The rewrite adds tutor scaffolding: a Niko 🦉 owl bubble AUTO-appears after a struggle window (young 18s / reader 22s, `_armStruggle`→`_owlOffer`) offering help, plus a persistent 💡 მინიშნება button that PULSES when the owl offers. `wsHint()` is graduated: 1st press pulses the first letter + plays the word audio, 2nd press flashes the whole path; hint use feeds the NB-45 skill (hint-free win → up, ≥3 hints → down). §6c GROUND-TRUTH: probed computed style of the SETTLED owl = display:flex, **opacity:1**, visibility:visible, correct KA text „გინდა დაგეხმარო?…"; settled screenshot LOOKED at (owl above dock + hint pulsing). Gate asserts BOTH the timer path (owl auto-shows after the `__wsStruggleMs` seam) AND the display logic (`__wsForceStruggle`). Timer-bug caught + fixed during build: stray completion timers wiped the struggle timer → now tracked via `_completeT` + cleared on rebuild. | **CLASS: "activity lacks tutor/hint scaffolding on struggle."** Sweep of stallable games: word-search = NOW scaffolded (struggle-timer→owl→graduated hint) ✔; match/spell/kings already teach on a WRONG answer via reQueueWrong/teachAndConfirm (INV-2/NB-38) = a different mechanism, not a silent-struggle gap; owl tutor already present across quiz modes. Word-search was the one stallable activity with no coach — closed. [ASSUMPTION: a passive-struggle timer for match/spell is a future enhancement, not a current bug.] |
| NB-42 | [MED] Word Search: no footer/bottom-dock — no shared nav to exit/switch (owner: „footer არ აქვს") | owner 2026-07-14 chat | ✅ FIXED + §6c-VERIFIED 2026-07-14 (branch fix/qa-nextsteps-nb4-38-39-40-41; CUSTOMER-FACING → ✅ SHIPPED LIVE v1.360 2026-07-14 via tools/live-push.mjs, §6c live-looked on nikolearn.com). Word-Search now renders a self-contained bottom dock `<nav class="ws-dock">` with 3 actions — 🏠 მთავარი (`closeWs`) / 💡 მინიშნება (`wsHint`) / 🔄 ახალი (`wsAgain`) — so the screen is no longer a dead-end. Dock buttons meet the 44px tap floor (`min-width/min-height:48px`, NB-4 class). §6c: dock screenshot LOOKED at (3 buttons visible, actions wired). Gate asserts 3 buttons + all three onclick handlers present. | **CLASS: shared-dock not rendered on a fullscreen activity** (NB-1/NB-36 exit-nav family). Word-search = NOW has an in-activity dock ✔. This is a word-search-owned dock (overlay is z-60, OUTSIDE the main `.bottomnav`), consistent with how the Movement/Drawing overlays own their own exit; consolidating ALL fullscreen overlays onto ONE shared dock component stays open as **NB-1** (product-scope refactor). Instance closed; the cross-activity consolidation is the NB-1 line. |
| NB-45 | [MED] Word Search: difficulty does not scale by age + capacity (owner: „სირთულის მიხედვით ასაკზე და ქეფასითზე") — same word set/grid regardless of child | owner 2026-07-14 chat | ✅ FIXED + §6c-VERIFIED 2026-07-14 (branch fix/qa-nextsteps-nb4-38-39-40-41; CUSTOMER-FACING → ✅ SHIPPED LIVE v1.360 2026-07-14 via tools/live-push.mjs, §6c live-looked on nikolearn.com). Difficulty now scales by AGE (hard band) AND CAPACITY (mastery) via a persistent per-profile `wsSkill`: `LEVELS[0..3]` map skill→{word count, direction set, grid size} — L0 (young/brand-new) = 3 words, straight-only, small grid; L3 = 6 words + both diagonals, bigger grid. `_resolveSkill` clamps to the age band (young 0-1, reader 1-3) so a reader is never trivially easy and a young child never gets diagonals-heavy; `_bumpSkill` on completion ramps UP after a hint-free win and eases BACK after ≥3 hints. §6c/gate: skill 1/2/3 → more words at higher skill, skill applied from profile, reader floored at 1 even if stored 0; young grid screenshot LOOKED at (3-word straight band). | **CLASS: activity ignores age/mastery adaptive-difficulty.** Word-search = NOW age-banded + mastery-scaled ✔. Sweep vs the age-gate family: math/shapes/alpha already gate by age (placement.js) and math has the `flatTier` mastery climb (INV-4); word-search was the one activity with a single fixed grid regardless of the child — closed. `wsSkill` persists per profile alongside the existing mastery signals. |
| NB-46 | [process/infra] weekly QA runner report is inert + mis-targeted: (i) checks STALE origin `gshoina.github.io` (old GitHub Pages) NOT live nikolearn.com; (ii) cloud sandbox blocks outbound → 0 of 4 checks run every week; (iii) findings are REPORT-ONLY, no auto-fix loop — owner wants verify-and-fix without his involvement; (iv) generation mechanism undocumented | owner 2026-07-14 chat (pasted emailed report) | **ROOT-CAUSED 2026-07-14 (read-only investigation done); fix = ONE infra decision for the owner (§15b CI/CD gate).** How the report is generated (owner asked): it is NOT a repo file — it is a **cloud scheduled routine** (RemoteTrigger-class weekly job) that runs a checker against a hardcoded origin and emails the result; the repo holds only the LOCAL twin `qa/qa-check.mjs` (`npm test`) + `.github/workflows/deploy-pages.yml` (no cron). Four confirmed defects: (i) it checks the **STALE origin `gshoina.github.io`** (old GitHub Pages), NOT live **nikolearn.com** → it is auditing a dead site; (ii) the cloud sandbox blocks outbound fetch → effectively **0 of 4 checks actually run** each week; (iii) findings are **REPORT-ONLY — there is NO auto-fix loop** (this is the owner's core ask: „გადაამოწმე და გაასწორე ჩემი ჩართვის გარეშე"); (iv) the mechanism was undocumented. **RECOMMENDATION (needs owner GO — it is an infra/CI change, §15b):** replace the inert cloud routine with a **scheduled GitHub Action** that (a) runs `qa/qa-check.mjs` against a reachable target, (b) on a finding auto-appends an NB row + opens a fix PR, (c) emails the digest — moving from report-only to a closed verify-then-fix loop. DEFAULT if owner silent: build the GH-Action loop on a branch (reversible, no live push) and show it before enabling the schedule. **✅ OWNER GO 2026-07-14** („QA ავტო-გასწორების ციკლის აშენება (NB-46) გეთანხმები") → **✅ BUILT + SELF-TESTED 2026-07-14 (commit 29fb4d7, branch fix/qa-nextsteps-nb4-38-39-40-41):** `qa/live-check.mjs` (fetch-based LIVE health check of nikolearn.com — assets 200, charset/OG/mojibake, live version consistency, stale-deploy vs repo HEAD, manifest parse; runs on GH runners where outbound works, fixing defects i+ii) + `tools/qa-triage.mjs` (fingerprint+dedupe triage → auto-appends one NB row per NEW finding, drafts the PR digest, emits triage-out.json; a persistent finding is logged ONCE = no weekly spam, fixing defect iii) + `.github/workflows/qa-auto.yml` (weekly cron + workflow_dispatch: source+live check → triage → **healthy week is SILENT**; a NEW finding opens ONE PR carrying the tracker rows + deterministic version-drift auto-fix; §13-safe — never auto-merges to prod). Self-test PASS: healthy path (v1.360, 0 findings → prNeeded=false, no PR); injected findings (3 new NB rows, in-run + cross-run dedupe both hold); YAML valid (8 steps). **HARDENED (owner red-team ask before activation, commit fdea42a):** the live stale-deploy check now guards against propagation-window false positives via retry (re-fetch until the live version converges) + grace (`--head-epoch`/`--deploy-grace-min 15` → a deploy still propagating is NOT flagged; only HEAD-old-and-still-behind = real P1). Tested: within-grace suppressed, past-grace flagged. **INERT until merged to main — that merge is the activation = a CI-infra gate (§15b) → ONE owner GO to enable the weekly cron.** | **CLASS: "automated audit that neither targets the live app nor closes the loop."** Generator located (cloud routine, not repo) ✔; live-origin repoint + report→triage→fix handoff = the recommended GH-Action loop above. Sibling: the same stale-origin risk applies to any monitor pointed at `gshoina.github.io` — the repoint fixes the class. |
| NB-36 | **[HIGH] Movement nav-trap:** after entering Movement world the session stays on the „ბუქნის შესვენება" break screen; Drawing Studio + Word Search captures show it in **18/18 combos** → both worlds have ZERO real visual coverage this run; rep counter increments across subsequent captures (Movement=1 → WordSearch=2 → Drawing=3/4) = stuck persistent state, not a coincidence | 2026-07-13 visual-QA §8 run3 LOOK phase (34/34 sheets read) | ✅ RESOLVED 2026-07-14 = **HARNESS bug, app is fine** (branch fix/qa-nextsteps-nb4-38-39-40-41). Firsthand real-browser repro: `closeBreak()` exits Movement cleanly (child's back-button works); the harness `goHome()` called `selectProfile('guest')` which re-renders home UNDER the appended `#breakscr`/`#wsscr` overlays, so every post-Movement capture shot the stale overlay → WordSearch+Drawing got ZERO coverage in all 18 combos. Fix: goHome now calls closeBreak()/closeWs() + removes the overlays before re-render. **App exit paths verified working; no app change needed.** Process signal: the harness under-covered 3 fullscreen worlds for the whole run3 — logged. | **CLASS: "back-navigation from a fullscreen activity does not complete the exit; persistent activity state hijacks subsequent screens."** Sweep surface: every fullscreen/stateful activity (movement break, drawing canvas, word-search grid, mic recording, tracing) × back-button + FAB-nav + home-dock exit paths |
| NB-37 | [MED] EN-side KA leak set (one-directional; KA UI is 100% clean): kings-math task text fully KA on EN (olympiad, practice-exam incl. timed intro, word-problems, triangle instruction) = an EN child cannot read the task; kings-eng Speaking „ჩაიწერე შენი ხმა" + mic disclaimer; kings-test instruction „წაიკითხე და აირჩიე…" + „10ჯ./20ჯ." score suffix; math-tutor hints mixed per-problem (some EN, some KA in the same combo = hints partially untranslated); age-5 items (counting „დავითვალოთ ერთად", shapes „რომელი ფიგურაა?", multiplication intro); english-Phrases topic chips; kings-math menu footnote; movement break screen; path-top „🧭 შენი გზა:" + pset-hint (probe: 39 findings, all EN-side) | 2026-07-13 run3 probes + LOOK | OPEN — SCOPED 2026-07-14, split into TWO decisions (do NOT ship a partial — see below). **(A) CHROME leaks** (instructions, score suffix, break screen, path-top, phrase chips, age-5 prompts, menu footnotes, tutor hints, mic disclaimer) = bounded, clearly-correct i18n registration via I18N_MAP + inline `useEn?` ternary (games.js:832 precedent). MUST be done as ONE complete per-zone pass, NOT piecemeal: a half-registered zone recreates the exact half-translated-UI state the 2026-06-23 owner design note removed the in-app toggle to avoid. **(B) kings-math CONTENT** (~120 hand-authored Georgian olympiad word-problems) = a genuine §4b PRODUCT + large-effort fork, NOT a bug: authoring EN versions is a multi-session project of questionable value AND in tension with „the app teaches English through content, chrome stays Georgian" (i18n.js note). **RECOMMENDATION (default if owner silent): do (A) chrome-complete as the next Lane-A pass; for (B) keep kings-math content Georgian (Option A) OR gate kings-math to KA when UILANG=en so an EN child never lands on half-translated task text (Option C) — do NOT unilaterally start a 120-problem EN translation project (Option B).** CARRIED-OPEN top NOW item. | **CLASS: NB-19 reverse-lookup i18n coverage gap** (unregistered ka strings fall through to ka). Two mechanisms confirmed: (1) `I18N_MAP` static table + `applyLang` DOM-walker; (2) inline `useEn?'en':'ka'` ternary for dynamic strings the walker can't reach. §16 CKO contradiction logged: 2026-06-23 „chrome stays Georgian" vs 2026-06-25 „app respects parent-set UI language" — NB-37(B) is where they collide. |
| NB-38 | [LOW content] kings-eng Match: untranslated entry „Dance Wiggle" appears in the GEORGIAN column (seen 7-en-390) | 2026-07-13 run3 LOOK | ✅ FIXED + §6c-VERIFIED 2026-07-14 (branch fix/qa-nextsteps-nb4-38-39-40-41). **ROOT CAUSE was NOT a bad vocab pair** — the reported „Dance Wiggle" is the i18n EN-overlay translating the GEORGIAN match column: `"ცეკვა":"Dance Wiggle"` (i18n-strings.js:706) means on the EN UI the DOM-walker rewrote every ka tile → English, collapsing a bilingual KA↔EN matching game into English-on-both-sides (the child had nothing left to match). FIX = `data-noi18n` on the ka `.match-col` (games.js:465) so `applyLang`'s walker (`closest('[data-noi18n]')`, i18n.js:55 — skips the whole subtree) never translates the Georgian column. §6c: rendered matchRound() at 390 in EN — ka column stayed 100% Georgian (ვარსკვლავი/ჩაი/ვარდისფერი/მარწყვი/ბანანი), en column English, screenshot looked at. **Class-level, not instance:** ANY ka vocab drawn now stays ka, not just „ცეკვა". | **CLASS (reclassified): "the EN i18n overlay translates a deliberately-bilingual widget, destroying content that MUST stay Georgian."** Sweep: every widget that shows ka text as CONTENT (not chrome) on the EN UI = match-col (fixed) + any future ka↔en pairing/quiz; guard = mark the ka-content container `data-noi18n`. |
| NB-39 | [MED-low content] generated math items where the ANSWER equals a visible operand: „4−2"→2, „4÷2"→2, rebus „△+4=8"→8 (~7 real of 37 probe hits; ~26 = reading passages where answer-in-stem is the game = by design; 4 = probe FP on single letters) — kid can pattern-match without computing | 2026-07-13 run3 answer-in-stem probe + manual triage | ✅ FIXED at ROOT 2026-07-14 (branch fix/qa-nextsteps-nb4-38-39-40-41). ONE predicate `answerIsShown(it)` (games.js:677 — strips HTML, matches every \d+ in the prompt, tests `nums.includes(it.a)`) now guards EVERY generated-math path: `genMath` wraps `genMathRaw` in a 12-try reroll (all of mathRound/requeue/easyFirst funnel through it); `mathRound` q0 warm-up rerolls ≤8×; `genRebus` reuses the SAME predicate as a 20-try reroll (tier-2 „N+5=10→5", tier-3 △-chain); static `MODEL_POOL` lv1 „8→3" item reauthored to „9→4" (answer no longer printed). npm test v1.359 0 findings + visual-gate clean. | **CLASS: "a generated/authored math item whose correct answer is a number already visible in the prompt."** §14 sweep of ALL math paths: genMath✔ genRebus✔ genModel✔ (fixed); genPattern/genTriangle = clean by construction (answer is the missing term, never printed); static KMX_* pools scanned (scratchpad/nb39-scan.mjs: 217 items, 94 integer-answer, **3 heuristic hits ALL coincidental non-instances** — coin/change/savings word-problems where the number legitimately recurs, 0 true instances). Consolidation: one predicate, no second definition. |
| NB-40 | [MED] worlds-home world-name chips (`w-nm`, 14px) contrast 1.87-2.88 on yellow/light-green tiles = 439 probe findings, all one widget; v1.359 worlds-redesign regression | 2026-07-13 run3 contrast probe + LOOK confirmed | ✅ FIXED + §6c-VERIFIED 2026-07-14 (branch fix/qa-nextsteps-nb4-38-39-40-41; CUSTOMER-FACING → ✅ SHIPPED LIVE v1.360 2026-07-14 via tools/live-push.mjs, §6c live-looked). Root: white label sat on pastel tiles at 54-83% chip height where no scrim reached → 1.86-2.96 FAIL on every hue. FIX = the world NAME gets its own dark rounded name-tag plate (`background:rgba(22,15,5,.55)`, `width:fit-content`, auto-centered, `padding:2px 9px`) — keeps the playful white-on-colour look, moves WITH the label regardless of icon-clamp. §6c GROUND-TRUTH: pixel-sampled the rendered plate on all hues = ~6:1 (was analytic-scrim modelling giving false-pass; switched to real pixel sampling), full-page screenshot looked at. | **CLASS: "light text on a mid/light tile with no guaranteed contrast surface."** Sweep (NB-3/VIS-2/3 family): w-nm (fixed); other white-on-colour = s-badge (NB-3, still open), world icons (decorative, N/A). Guard pattern = give text its own contrast plate rather than depend on tile bg. |
| NB-41 | [MED] 320px horizontal overflow: Movement exercise-chip row + Drawing dw-* tool row overflow the viewport (109 probe HIGH + 18 combined with tap<44) — two concrete screens, worst on smallest devices | 2026-07-13 run3 overflow probe + LOOK confirmed | ✅ FIXED + §6c-VERIFIED 2026-07-14 (branch fix/qa-nextsteps-nb4-38-39-40-41). FIX = `flex-wrap:wrap` on `.dw-top` + `.dw-tools` (+ `min-width:0`) so the Drawing tool row wraps instead of overflowing; `.mv2 .who{min-width:0}` lets the Movement title shrink instead of pushing the row wide. §6c: rendered Drawing at 320 — tool rows wrap cleanly, nothing clipped, screenshot looked at; visual-gate `overflowX=false` at 320/360/390 on every screen. | **CLASS: "a fixed horizontal row of chips/tools assumes ≥360 width and overflows at 320."** Sweep: dw-tools✔ dw-top✔ mv .who✔ (fixed); visual-gate now guards all subject/menu/game screens at 320 (0 offenders) = class-level regression guard, not a two-screen patch. |
| NB-35 | [MED measurement] PWA-install funnel structurally undercounts: (a) landing.html loads pwa-install.js but NOT analytics.js → every nudge shown on landing (= ALL first-time visitors incl. FB-in-app ad clicks, boot() routes them to landing) calls `track()` into `window.Analytics===undefined` = silently dropped; (b) funnel middle unmeasured — `pwa_nudge_dismiss` + `pwa_prompt_*` emitted by pwa-install.js:105,100 but absent from CLIENT_ALLOW (analytics.js:97) + worker EVENTS → dropped on-device; (c) iOS A2HS never fires `appinstalled` → iOS installs invisible to pwa_installed forever (real install KPI = retention_ping ctx=pwa); (d) pwa_nudge carries no ctx (landing/app × inapp/ios/android) | CKO autonomous follow-up 2026-07-13 (logged s18: "investigate why pwa_nudge barely fires"); read pwa-install.js + analytics.js + telemetry-worker.js + landing.html | OPEN — root-caused, fix NOT built (visual-QA run3 in flight owns the worktree; app-file edits would contaminate it). Verdict: "nudge barely fires" ≈ mostly a MEASUREMENT artifact layered on narrow eligibility (platform gate isInApp‖isIOS‖deferred + 4d soft-backoff + 28s/value reveal — niko:value IS live, games.js:1609). pwa_installed=0 = inconclusive until (a)-(c) fixed. FIX PLAN: ① landing.html add analytics.js before pwa-install.js (check no auto-fired session events pollute landing stats) ② CLIENT_ALLOW+worker EVENTS add pwa_nudge:['ctx','page'], pwa_nudge_dismiss:[], pwa_prompt:['outcome'] ③ pwa-install.js pass params ④ weekly-report: read retention_ping ctx=pwa as the install-base KPI. Client side = autonomous ship (Amendment d, measurement-only, reversible); worker deploy = GO, bundle with pending NB-26/27/28 hardening deploy. Feeds FDQ #1 (Play/TWA): fix measurement FIRST, then read funnel ≥1wk | **CLASS: "telemetry emitted where the facade isn't loaded / event not allow-listed = silent drop."** Sweep at fix time: grep every `Analytics.event(` caller × page script-lists × CLIENT_ALLOW × worker EVENTS for 4-way mismatches (known cousins: demo_age/kings_exam_*/feedback_send = deliberate drops, documented) |
| NB-34 | [MED app-hardening; harness-context CRITICAL] `openMenu()` renders a live mode grid with `profile=null` after `goHome()`; first mode tap throws `TypeError reading 'words'` at `levelOf` (core.js:247) → permanent crash overlay („უი, რაღაც აირია") until reload | 2026-07-13 visual-QA §8 run1 incidental (186 identical pageerror stacks; repro'd standalone) | FIX BUILT + SELF-TESTED on branch `fix/profile-null-crash-guard` (da77e6f, NOT merged — Preview→GO→Live): levelOf tolerates missing state entry (`state[p]\|\|{words:{}}`), openMenu bounces to picker when no profile. qa/_repro-guard-verify.mjs 15/15 PASS + npm test GATE PASSED. Real-user reachability LOW (bottomnav entries `profile&&`-guarded, index.html:45-46) — severity is latent: any future unguarded openMenu caller reintroduces a hard crash. | **CLASS: "profile-keyed state lookup dereferenced without existence guard."** Sweep: `levelOf` = the chokepoint (levelIdx → startDigitQuiz/startCount/alphaQuiz/startKingsExam/kxPracticeRound/startGame/results all route through it); other `state[profile]` derefs are in-round handlers where profile is guaranteed once openMenu is guarded; picker/parent-dash iterations safe via load() backfill (core.js:36-37 backfills every kid + guest). 2 guards = whole class closed at the chokepoints, no N-copy patching. |
| NB-33 | **[HIGH] live STATS_KEY credential (`nk_QcZqZ…`) in plaintext in 2 tracked archive handoff files + git history** — reads /v1/stats AND /v1/feedback (parent PII); repo private so immediate risk LOW but latent HIGH; I had PROPAGATED it into 20260712_full.md during the s18 trim (owned) | Codex Phase-2 High-finding-1, verify-first CONFIRMED 2026-07-12 (git grep = 2 tracked files + history) | ✅ FIXED + VERIFIED 2026-07-12 (autonomous under Amendment d — security remediation, reversible, non-financial/reputational; leaving an exposed live credential is the greater risk). **ROTATED** STATS_KEY (wrangler secret put on nikolearn-t): old key → 403, new key → 200 (verified). Refs updated: `~/.claude/.bivision-creds.env NIKO_STATS_KEY` + gitignored viewer. **REDACTED** literal from both tracked archives (git grep live-key = 0). Git history retains the now-DEAD old key = harmless post-rotation → NO history purge (heavy, rewrites shared history, moot once key is dead). | **CLASS: "live secret in a human-readable tracked doc"** — sweep: git grep `nk_`/`AIza`/`sk-`/token patterns across ALL tracked = only the 2 archives (now clean); creds live ONLY in gitignored `.env`/`*creds*` (verified NB-24/infra-audit). **GUARD (systemic, Amendment-c benchmark = git-secret-scanning best practice):** recommend a pre-commit secret-scan hook (gitleaks-style regex) so a credential literal can never be committed again — logged as autonomous build candidate. |
| NB-23 | `/v1/stats` telemetry READ down — HTTP 500 / CF error 1101 after ~97s (2× repro 2026-07-12; worked 07-10) exactly as the FIRST D1 retention cohort lands; write path `/v1/t` unaffected (data still accumulating); weekly-report.mjs broken; no monitoring so outage was invisible | autonomous onboarding audit s18 | ✅ FIXED + DEPLOYED + VERIFIED 2026-07-12 (owner GO „იმოქმედე"; worker version 62547d32; my stated-default plan). Fix shipped = ranked-①+③+④ combined: counters now carry value in KV metadata (collect path), /v1/stats reads list()-only (~2 subrequests) with capped get-fallback + `?prefix=` day-slice, NEW owner-gated `/v1/backfill` migrated ALL 1013 pre-fix keys in 5 chunked calls (count ≈ just over the 1,000 cap = diagnosis re-confirmed), new telemetry keys get 180d TTL (history backfilled WITHOUT TTL to preserve launch-period cohorts; fb| untouched). VERIFIED: stats 200/0.35s (was 500/97s), 1013 entries 0 null, retention_ping data readable; weekly-report.mjs end-to-end = worker OK, cf OK, gh OK. Controlled pre-test had confirmed class: /v1/feedback (tiny keyspace, same pattern) = 200/0.27s while stats failed. | **CLASS: "full-keyspace O(keys) KV reader vs unbounded no-TTL keyspace → crosses the Workers 1,000-subrequest/invocation cap as keys grow."** Evidence: `telemetry-worker.js` /v1/stats does `list()` + one `get()` PER key in a do/while; no `expirationTtl` on any put; snapshot 06-16 = 266 keys (~38-68/day) → ~1,300 by 07-12 after the 07-10 deviceType-split. 3 converging signals for subrequest-cap (1101 = thrown exception, not 1102 CPU / 522 stall; ~97s ≈ ~1,000 sequential cold gets; worked 07-10 → fails 07-12 = monotonic growth crossing a fixed cap). **Fix ranked:** ① counters → KV `metadata`, reader list()-only (~5 lines, subrequests O(keys/1000)≈2, backfill-fallback self-heals) · ② daily rollup doc (structural) · ③ `expirationTtl` 90d on telemetry puts (fb| exempt) · ④ stopgap TODAY: `?prefix=` filter + weekly-report pulls day-by-day. **Traps:** Promise.all does NOT reduce subrequest count; Analytics Engine is on our own wrangler.toml privacy deny-list. **§14 sweep:** /v1/feedback reader = same list-and-get pattern but fb| keyspace is small+bounded (voluntary submissions) = same class, low urgency — fold into the same fix pass. |
| NB-24 | internal files publicly served on nikolearn.com: `/CLAUDE.md` (agent ops rules), `/cloudflare/telemetry-worker.js` + `feedback-viewer.html` (backend source + admin UI), `/competitor-profiles/nodi.md` (competitive intel), `/share/7731b5f78d82/*` (strategy docs) — ALL verified live 200 (2026-07-12, cache-busted); NO credentials exposed (nothing secret is git-tracked, verified) | autonomous onboarding audit s18 | ✅ FIXED + DEPLOYED + LIVE-VERIFIED 2026-07-12 (owner URGENT GO „სასწრაფოდ დახუროს"): deploy-pages.yml flipped to explicit ALLOWLIST (Opus built `ab4387e`; CKO review caught + restored owner tools changes.html/validate.html; deployed `6066781`→main; Pages run 29186178385 success first try). LIVE 18/18 checks: CLAUDE.md / cloudflare/* / competitor-profiles / docs / package.json / bump.mjs = 404; index/landing/privacy/changes/validate/niko JS+audio/manifest/sw + share/ (intentional) = 200. CLASS killed: a newly-added tracked file can NEVER auto-publish. Bonus unblocked: strategy drafts now git-tracked (backup done). NOTE: this closes the INTERNAL-docs leak; the app's own JS stays publicly readable by nature of a static PWA — real code-copy protection = the open backend decision (HANDOFF §6). | **CLASS: "denylist publish model — rsync in deploy-pages.yml excludes a hardcoded list, so EVERY newly-added tracked file ships to the public site by default; any future internal file auto-leaks."** Sweep: full rsync-exclude vs `git ls-files` reviewed — `docs/ qa/ tools/ android/ v2-prototype/ scratchpad/ output/` correctly excluded (probed docs/ = 404); offenders = the listed 5 families + root clutter (`_config.yml`, `_preview_server.mjs`, `bump.mjs`, `package*.json`, dev/demo HTMLs). **Root-cause fix = flip to ALLOWLIST** (copy only known site files: index/landing/privacy/sw/manifest/niko/assets/movement/icons/CNAME/robots) — one workflow edit kills the whole class; then re-probe the 5 URLs = 404. 2026-07-12 increment (Codex RL-01): `auditbyagents/` = one more tracked-if-added leaky path; allowlist covers it. |
| NB-25 | **[HIGH] EN privacy page contradicts actual app analytics** — privacy.html:250-253 (EN) claims web analytics is "marketing landing page only" + "No analytics of any kind runs inside the children's learning environment"; FALSE: index.html:5-6 loads CF Web Analytics beacon IN the app + analytics.js sends first-party telemetry (off only localhost/DNT/owner). KA §3 (privacy.html:107 „საიტისა და აპის") is honest — EN drifted. | Codex TR-01, triage-verified + my own read 2026-07-12 | IN PROGRESS — **owner REFRAMED 2026-07-12: truthfulness fix, NOT privacy tightening; privacy = a challenge to solve right, never an insight-blocker at MVP (matches locked memory feedback_mvp_measurement_over_privacy).** **READY FOR FOUNDER APPROVAL (Execution Contract verdict, 2026-07-12):** Opus rewrote EN „Web analytics summary" (truthful: CF analytics site+app; first-party id-free/free-text-free telemetry; anti-overcommit wording; the two „არასდროს" absolutes kept deliberately = §5 hard guarantees) + §14 sweep caught the SAME false claim in the KA trailing subsection (fixed, owner-validated phrasing reused) + judged 4 other claims out-of-class with reasons + README clean. CKO acceptance evidence: diffs reviewed (v1 + v2 `38956a0` anti-overcommit refinement — insight-constraining absolutes removed from EN, child-safety "never includes a child's name or free text" kept = §5 hard guarantee); Gemini KA-QA = PASS; §6c screenshots looked at by acceptor (v1 + v2 in `output/screenshots-s18/`); qa gate green. ✅ SHIPPED LIVE + VERIFIED 2026-07-12 (autonomous under Policy Amendment d — risk-reducing truthfulness fix, reversible, fully verified = no human GO needed; owner directed the model change). Deployed cfc03f4→main, Pages run 29190205114 success. LIVE 6/6 cache-busted checks: EN+KA false claims REMOVED, truthful text PRESENT both languages, page serves. CLASS „privacy copy drifts from data flows" now zero open instances. §6f: Gemini KA-QA passed pre-ship; owner may still tweak KA parity („მხოლოდ ჯამურ" vs looser EN) any time = 1-line, low value. | **CLASS: "privacy copy drifts from actual data flows, per-language"** (same class as the HANDOFF "zero network calls" claim CKO fixed 2026-07-12). Sweep surface: privacy.html EN+KA all sections, landing badges, README, in-app parent texts. Trust/compliance exposure on a kids' app = highest-value external finding. |
| NB-26 | **[RE-REPORTED as audit #19, 2026-07-15 — this row already held it, and already named the same fix]** [MED] owner read-endpoints take the secret via `?k=` query string (telemetry-worker.js:152/186/207) + feedback-viewer.html persists it in localStorage (:52/:65) and accepts via URL hash (:66) → leak vectors: history/proxy logs/screenshots; a leak exposes parent-feedback PII | Codex SG-01, triage-verified | OPEN — fix = Authorization header + rotate STATS_KEY + stop browser persistence; worker change = infra → owner GO | secret-transport class; owner-only surface so MED not HIGH |
| NB-27 | [MED] `fb|` feedback rows have NO TTL and no deletion route (worker:236) while privacy.html:246-248 promises ≤12-month retention + deletion on request | Codex FB-01, triage-verified | OPEN — fix = expirationTtl ≈12mo on fb| puts + owner deletion workflow; NOTE: my NB-23 fix deliberately left fb| TTL-free — that call is now revised by the privacy promise; infra → owner GO | policy-vs-code retention class (sibling of NB-25) |
| NB-28 | [MED] telemetry counters = non-atomic KV read-modify-write across CONCURRENT requests (worker:286; within-request race was fixed earlier, cross-request remains) + collect POST /v1/t never REJECTS disallowed origins (cors() headers-only, worker:89-92) → counts directional + injectable by non-browser clients | Codex DA-01, triage-verified | OPEN — treat metrics as directional (already the read-rule); fix options: sharded day-keys or DO aggregation + origin reject; infra → owner GO | measurement-trust prerequisite; fold into any future worker pass with NB-26/27 (ONE deploy) |
| NB-29 | [MED-low] a11y inconsistency: subject tiles are `<div onclick>` no role/tabindex/keyboard (screens.js:380-402), avatar color-dots unnamed (screens.js:261), while worlds.js cards DO have keyboard — same control family, split semantics | Codex AC-01, triage-verified | ✅ FIXED v1.364 (2026-07-15) — added `kt()` helper (role=button+tabindex0+aria-label+onkeydown Enter/Space) to BOTH subject grids (young + 8+); color-dots got aria-label(color name)+aria-pressed. §6c-verified: 9/9 tiles pass contract, real keyboard Enter navigates, 0 pageerror, screenshot no-regression | **CLASS CLOSED: "interactive tile primitive lacks unified semantics"** — subject tiles share one `kt()` contract matching worldCard; color-dots named |
| NB-30 | **[RE-REPORTED as audit #21, 2026-07-15 — the swallow half was already tracked here]** [MED-low] silent failure surface: index.html:70 preventDefaults ALL unhandledrejection; core.js:45 empty catch on save/storage | Codex ER-01, triage-verified | ✅ PARTIALLY FIXED 2026-07-15 (`3c6038f`) · storage half LIVE v1.366 2026-07-20 — closed with NB-32: `core.js:45`'s empty catch now sets `saveHealthy=false` and the failure reaches a human via the parent banner (`saveStatus()`). **STILL OPEN (logged, not silently dropped):** `index.html:70` still preventDefaults ALL unhandledrejection, so non-storage boot/runtime errors remain swallowed. Next pass = classify expected-vs-bug rejections, keep the swallow only for the known-benign set. | error-swallowing class — storage instance closed, boot instance open |
| NB-31 | [MED-low] no CSP anywhere; heavy innerHTML + inline handlers (names sanitized → low current risk) | Codex CP-01, triage-verified | OPEN — staged: CSP report-only → inventory inline handlers → enforce; not urgent | defense-in-depth |
| NB-32 | [MED] local progress is not a durable data product: `save()` (core.js:45) returns no status, UI promises „პროგრესი შეინახება" with no read-back, no schema version, no migration, no last-known-good recovery — a kid's progress can silently vanish on quota/corruption | Codex ST-01 (recommendations), triage-verified | ✅ FIXED 2026-07-15 (`41897f4` + `3c6038f`) · **LIVE v1.366 2026-07-20** (live-verified: `save-warn` + `SK_BAK` markers on nikolearn.com). **ROOT:** localStorage treated as infallible; the LYING-quota mode (setItem accepted, value dropped) passed as success through a bare try/catch. **FIX = 3 layers:** DETECT (save() reads back, returns false; throwing/private-mode path too) · SURFACE (`.save-warn` banner in parentDash names the real consequence; ink #8F2318 measured 7.83 AA) · RECOVER (`nikolearn_p2_bak` last-known-good rotated per good save; corrupt main key restores instead of resetting; `__v` schema stamp). **§6c LOOK found what 14 assertions missed:** the privacy card under the banner still promised „პროგრესი ინახება მხოლოდ აქ" — now conditional on `saveStatus()`. Proof = `qa/_save-durability.mjs` 17/17 behavioral (dropped write · throwing store · corrupted key · healthy path), shots `niko-shot/nb32-save-warn.png`. | data-durability class; closed WITH NB-30 (same commit) |
| NB-19 | switching UI to English leaves Georgian strings untranslated across the frontend (partial translation) | owner 2026-07-08 s13 | PARTIAL — chrome fixed+verified (scanner qa/i18n-coverage.mjs + qa/_i18n-en-sweep.mjs; 31 chrome strings backfilled; owner CONFIRMED interface was the target, not content); EN sweep of home/menus/round = 0 Georgian left (only „ა ბ გ" alphabet label, intentional); content stays Georgian by design; ✅ SHIPPED LIVE v1.354 (2026-07-08, commit 926ef77, owner GO; verified on nikolearn.com: Remove PIN / Counting & Logic / Add a child translate); parent-dashboard deep screens + dynamic labels = follow-up → ✅ TAIL SHIPPED LIVE v1.358 (2026-07-11 s17, commit 8d754b7+0f2981c, owner GO, Pages run 29150361164 success first try, 8 markers apex-verified): qa/_i18n-en-sweep.mjs extended to 11 surfaces (parent dash w/ goal, goal/delete modals, admin, feedback, transfer, v1.357 worlds home age5); fixes = worlds/inner-name MAP block (24 entries, v1.357 chrome was post-backfill), delete-modal dynamic consent sentence + split ჩაწერე/ქვემოთ nodes, comma goal-title pattern (§8 em-dash removal had orphaned the old „—" pattern), GENERIC emoji-prefix retry pattern (kept LAST; class fix for „🌿 <name>" topbars), 4 runtime textContent sites wrapped in tx() (owl.js mvLbl hold/reps, parent.js იგზავნება…/✓ დაკოპირდა — §14 sweep, applyLang can't see post-render assigns), i18n.js attr loop now honours data-noi18n (delete-confirm placeholder must stay the typable «წაშლა»; was showing "Delete" while the check wants წაშლა). Verify: _i18n-en-verify +9 asserts ALL PASS, behavioral gate 5/5, qa-gate clean, §6c screenshots looked at (worlds EN home, delete modal before+after). Remaining Georgian on EN = intentional only (names, ქართული autonym, «წაშლა», ა ბ გ). | **CLASS (88% conf):** "translate-the-rendered-Georgian" reverse-lookup i18n — English is an overlay (`i18n.js` `toEn`) that DOM-walks rendered Georgian and swaps each string via a hand-kept table (`i18n-strings.js` I18N_MAP 776 + I18N_PATTERNS 116). Any Georgian string not registered (new/edited/dynamically-built) falls through and STAYS Georgian silently (`toEn` returns raw). NOT a re-render bug (core.js:197 re-applies EN on every screen) = pure coverage gap. Static-label proxy: only ~63% translate → ~135 of 369 static labels stay ka, plus large dynamic backlog; worst in games.js (15/256), screens.js (8/136), parent.js (28/168). **Fix:** full re-architecture to key-based t() = LARGE, NOT worth it (ka-first app, EN secondary). Pragmatic = backfill missing MAP/PATTERN entries + repair NB-11 drifted regexes (MEDIUM, data-only, low-risk) + add a build-guard script that fails CI when a source Georgian literal has no mapping (stops silent regrowth). **Business priority: LOW vs the q0 completion leak** — recommend deferring the backfill until after the q0 fix. |
| NB-1 | footer talk consolidation — bespoke `.talk-nav` ≠ shared dock | session-6 queue | OPEN | shared-dock class, table pending |
| NB-2 | ka tutor wording B-1…B-8 (`tutor.js`/`owl.js`) | session-6 queue | OPEN | pending |
| NB-3 | s-badge contrast | 07-02 audit | ✅ FIXED 2026-07-15 (`41897f4`, branch `recover/s21-nb32-nb3-nb8`; NOT yet live). All 7 `.s-badge` hues darkened to the name-ink level (#FF8A00→#8F3A16 · #0E9E78→#0A6B4F · #2E86DE→#184C86 · #EC4899→#B01E63 · #8B5CF6→#5B32A8 · #14B8A6→#0B6A64 · #E5A600→#7E5B0E) — swept as a CLASS (every hue), not the reported one. | contrast class CLOSED (VIS-2/3 + badge) |
| NB-4 | 44px tap targets | 07-02 audit + 2026-07-13 run3 quantified | ✅ FIXED at ROOT + browser-VERIFIED 2026-07-14 (branch fix/qa-nextsteps-nb4-38-39-40-41). Diagnosis corrected: HEIGHT was never the problem (base `.bottomnav button` already had `min-height:44px`) — the 43×44 buttons were 1px short on WIDTH, and only the `.slim` in-game bar (NOT the `.dock` home bar, which already had `min-width:44px`) lacked a width floor. FIX = add `min-width:44px` to the shared BASE `.bottomnav button` rule → covers dock + slim + every future variant in ONE place; ai-fab 42→44px. Browser-measured all bottom-bar buttons = 44/49/49/44/44 (`anyShort=[]`), ai-fab 44×44. | **CLASS: "shared bottom-bar / floating action button below the 44px tap minimum."** §14 catch this pass: fixing `.dock` alone would have LEFT the sibling `.slim` at 43px (the visible in-game bar) = the exact class-trap. Root-cause consolidation on the base rule kills the whole family; s-badge (NB-3) is a separate contrast class, not tap-size. |
| NB-5 | exam bonus cap→5 | 07-02 audit | OPEN | instance-level |
| NB-6 | skip-tier monotonic | 07-02 audit | OPEN | pending |
| NB-7 | telemetry allow-list | 07-02 audit | DEFER — CF Worker coupling | N/A (deferred) |
| NB-8 | "Screen-time" anglicism | 07-02 audit | ✅ CLOSED 2026-07-15 (`41897f4`) — the session-9 hypothesis was right: the key was DEAD (mapping existed in `i18n-strings.js`, the string was rendered nowhere), so there was no anglicism reaching a parent. Removed the dead mapping rather than translating a string that does not ship. | dead-i18n-key class |
| NB-9 | menu-i18n cluster: `screens-menu.js:100` სამკუთხედი, Premium `·`vs`—`, `screens.js` chrome, firstrun island | 07-02 audit | OPEN | untranslated-chrome class |
| NB-10 | Movement/მოძრაობა section weak — strengthen like ამოწერა; include Movement/Drawing/Talk in every sweep | owner | PARTIAL — core "weak/no-arc" already FIXED 2026-07-04 (verify-first 2026-07-06); only open piece = make it a browsable menu SECTION (owner scope/taste fork) | **Verify-first:** Movement is break-only (owl.js:221-356, ~15min or manual), NOT a menu subject (absent from screens-menu.js). NB-10 core "passive player, no arc/goal/reward = weakest section" was ALREADY strengthened 2026-07-04 (owl.js:297-300): mini-mission = finish `_mvGoal` distinct moves → celebration + `MV_REWARD` coins. MOVE_POOL=11 moves, rigged SVG, `tiny`-safe filter for 3-4yo, player UI + done overlay. **Remaining = product-scope fork (NOT a bug):** owner said "strengthen like ამოწერა" (which IS menu-browsable) → does he want Movement as an on-demand browsable tile? Which ages? = customer-facing taste, cannot infer. Default if silent = leave break-only (it now has arc+reward). **2026-07-09 — pre-reader half CLOSED (owner GO):** the move NAME was shown TEXT-only (owl.js mvRender `#mvName.textContent=m.ka`, never voiced) → a non-reader ≤5 couldn't HEAR which move to do. FIX: mvRender now voices `m.ka` on render via `playClipFor` (owl.js:368). 9 current MOVE_POOL names had no clip → generated build-time (edge_tts EkaNeural, `tools/_gen_move2.py`, clip_333-341) + manifest-wired; all 17 move names now resolve. Harness `qa/_move-voice.mjs` = 4 asserts (17 render + voiced + resolve + non-empty) ALL PASS; §6c settled screenshot verified (opacity 1, name renders). ✅ SHIPPED LIVE v1.355 (2026-07-10, owner GO, verified on apex — playClipFor in live owl.js). Menu-tile browsable-section fork still OPEN (owner taste). |
| NB-11 | stale EN-translation regexes (`i18n-strings.js:765,771`) → EN-UI untranslated titles | session-9 | DEFER — low value (ka-first) | i18n-regex-drift class |
| NB-12 | 3 listening modes (`listen`,`k-listen`,`listen-yle`) have NO text fallback if audio fails | session-9 q0 audit | DEFER — inherent+unvalidated (2026-07-06 §14 verdict; robust fix = EN clips, low-prio vs ≤5 pass) | **CLASS:** "listening-comprehension prompt carried ONLY by runtime EN SpeechSynthesis; if TTS fails there is no non-answer-revealing recovery." §14 sweep of ALL kings/listen modes: **2 real instances** = `listen` (games.js:134, EN word, emoji opts, word never shown) + `listen-yle` (games.js:1060, sentence audio, picture opts). `yesno`(q.s)/`story`(q.text+q.q)/`speak`(q.q) all SHOW prompt text → NOT in class. **`k-listen` is NOT a real mode** [FACT ~90%: absent from SUBMODES games.js:85, round-dispatch, handlers] → session-9 Playwright-label error; ledger "3" corrected to 2. Text fallback REJECTED (defeats listening pedagogy). Root cause = runtime EN-TTS dependency (`speak()`=SpeechSynthesis core.js:288; iOS stuck-paused documented core.js:285-287) — same class the app already killed for ka (runtime ka-TTS banned, all ka=AUDIO_MANIFEST). Robust fix = pre-record EN listen prompts as edge-tts clips (choke-point). DEFERRED: EN/school-age (not the ≤5 pass priority), failure UNVALIDATED (n=10-24, no evidence real kids hit it) — do not build customer-facing EN feature on unproven failure. |
| NB-13 | `ვკრებავთ`→`ვკრებთ` morphology (`tutor.js:135`) | external audit | OPEN — owner native validation | ka-grammar class |
| NB-14 | ZERO behavioral/unit tests = structural cause of dropped-bug recurrences; scope+implement minimal logic harness (tutor/games), due ≤2026-07-12 | 2026-07-05 architecture session | ✅ SHIPPED v1.354 (2026-07-08, commit 926ef77; aggregator green, all 3 harnesses PASS; wired to pre-push, verified on push) | **CLASS: "behavioral harnesses exist but are ORPHAN — no gate/CI runs them, so already-fixed bugs recur silently."** Fix (not a code bug, a process gap): (1) NEW `qa/behavioral.mjs` aggregator — spawns all 3 young-lane harnesses in sequence, exits 1 if any fails, exit 2 (chromium missing) = SKIP not block; (2) `qa:behavioral` npm script; (3) de-duplicated the shared hardcoded playwright/chromium/ROOT/server boilerplate out of `_alpha`/`_math`/`_shapes` into ONE SSOT `qa/_harness.mjs` (change a path once); (4) WIRED into existing `.git/hooks/pre-push` after the visual gate, same skip-safe contract → the 3 harnesses now actually run at every push to main. Deliberately NOT added to CI `npm test` (harnesses use local chromium/playwright paths, can't run in GitHub CI — LOCAL pre-push is the correct enforcement point). Verified: `npm run qa:behavioral` → "✅ BEHAVIORAL GATE PASSED — all 3 harnesses green". Files: qa/_harness.mjs (new), qa/behavioral.mjs (new), qa/_alpha-young.mjs, qa/_math-young.mjs, qa/_shapes-young.mjs, package.json, .git/hooks/pre-push. Reversible, uncommitted. |
| NB-15 | pre-reader (≤5) shapes quiz showed shape-NAME word options → a non-reader cannot answer | session-10 pre-reader audit #1 | FIXED v1.350 (§6c verified young+reader) | **CLASS: "quiz option format assumes the child can READ the option text — pre-readers can't."** Shapes swept + fixed (audio-in/picture-out). Sibling instances OPEN below: NB-16 (ka-alpha reading suite ungated), NB-17 (math-add unvoiced) |
| NB-16 | ka-alpha reading suite (კითხვა/წინადადება/გაგება/ააწყვე/შეადგინე) + გაგება offered to young (≤5) pre-readers; word options, some unvoiced | session-10 pre-reader audit #2 | FIXED v1.353 (qa/_alpha-young.mjs 23/23 + §6c screenshots + live-verified on apex 2026-07-06) | **CLASS verdict:** gate = modes that ASSUME reading: წინადადება (sentLearn), გაგება (rtext — no-audio by design), შეადგინე სიტყვა (shead — silent ka letter-ordering) hidden for ≤5 in ka+en menus. KEEP for ≤5 = audio-scaffolded reading ACQUISITION: ისწავლე/ტესტები/კითხვა/ააწყვე/ამოწერა [ASSUMPTION — owner can tighten]. §14 sweep found 3 sibling instances, all fixed same pass: (1) fresh young profile's „გააგრძელე" card routed into the ENGLISH menu (PATH_SUBJS fallback in homeResume) → skip empty-path subjects; (2) english path = 5 unreachable milestones capping young ჯამური პროგრესი → pathFor young english=[]; (3) tiny (≤4) math path counted unreachable add/sub → shapes only |
| NB-17 | math-add problem for young not voiced (pre-reader can't read the sum prompt); verify options ≤10 | session-10 pre-reader audit #3 | FIXED v1.352 (25 asserts, live-verified; device ear-check TO VALIDATE) | read-the-prompt class → abstract add/sub voiced via playClipSeq (sub = dative clips), visual take-away sub added; reader ≥7 stays silent |
| NB-18 | 5 praise words have NO audio clip (ზუსტად, სწორია, გენიოსი ხარ, გაიხარე, „აი, ასე") → Android TTS reads them in ENGLISH voice | owner (old report, re-surfaced 2026-07-06 s12) | VERIFIED-NOT-REPRODUCIBLE in code 2026-07-08 (~90%; residual = owner device ear-check, folded into existing carry) | **Verify-first (report is STALE — premise "NO audio clip" is FALSE):** all 10 praise-pool words (ბრავო/მართალია/შესანიშნავია/ყოჩაღ/ზუსტად/სწორია/ძალიან კარგი/გენიოსი ხარ/გაიხარე/აი, ასე) HAVE a manifest key AND a real mp3 on disk (prs_01-06 = 9.6-12KB, clip_019/095/188/198 present — byte-checked). Chain: `praise()` core.js:308 → `speak()` → `speakOne` WRAPPED by audio.js:49-60 → `clipFor(t)` finds the clip → `playClipUrl` plays the RECORDED clip before any TTS. Two independent guards make the reported "English voice" impossible in current code: (1) recorded clip plays regardless of device voice; (2) core.js:277 ka-gate — ka text + no device ka voice = stay SILENT, NEVER read ka with en/ru voice. Worst residual = silence if an mp3 fails to decode on a specific Android browser (playClipUrl onfail=null, no TTS fallback), NOT English garbage. Symptom likely fixed since the old report by the ka-gate + prs_ clips. NO build warranted (recording "missing" clips would have been wasted work — they already exist and already play). Residual <100% = device-specific, needs owner Android ear-check; same bucket as the v1.352 device ear-check already carried. |
| NB-19 | English section has many bugs too (owner report, unspecified) — pre-reader pass so far only swept ka/math/shapes; EN modes NOT audited at same depth | owner 2026-07-07 | OPEN-NARROWED (2026-07-11 s17) | DEEP AUDIT run: NEW permanent `qa/_en-audit.mjs` (wired into behavioral gate → now 6 harnesses at every push). Live-menu enumerated (english 9 tiles / kings-eng 9 / en-alpha 3); entry-diagnostic + quiz/reverse/listen/colour/engram/addlet/listen-yle/yesno/story auto-played FULL rounds by clicking the parsed-correct option + spell auto-typed → ALL complete to results, correct option always rendered, engine 0 wrong on correct-only clicks, 0 pageerrors; engram+story §6c screenshots content-checked (answer valid, distractors wrong). **Engine/flow level: owner's "many bugs" NOT reproduced.** STILL OPEN (why not closed — no premature clean verdict): match/speak/kings-exam = struct-only (interactive depth not auto-played) · en-alpha quiz not auto-played · device AUDIO quality (EN TTS voice on real phone = owner ear-check) · full item-content review. Next: owner's concrete instances would pinpoint the class fastest. |
| NB-20 | „ამ ლინკზე გადასვლით არაფერი მესმის" — no audio heard when opening the local test link | owner 2026-07-09 s15 | ✅ SHIPPED LIVE v1.355 (2026-07-10) — first-gesture audio unlock live; owner ear-check pending (deep-audit verdict: desktop silence = owner-side) | **Verify-first FACTS:** clip files serve (200, audio/mpeg) + decode (dur 2.088s, no error); NB-10 voicing call fires + resolves the right clip; qa-gate green. **CLASS found:** "recorded-clip audio has NO first-gesture unlock — HTMLAudioElement.play() is autoplay-blocked on desktop Chrome / iOS until a user gesture, so any auto-voiced audio whose play() is NOT inside a tap handler (a break/prompt/greeting that renders on its own) stays silent." Confirmed: no AudioContext/unlock/pointerdown-prime anywhere; only `speechSynthesis.resume()` (TTS path). On mobile every screen change is a tap so it self-unlocks; on desktop auto-voice can be blocked. **FIX (audio.js):** one-time `_primeAudio` on first pointerdown/touchend/click primes the reused element with an existing clip at volume 0 (unmuted, inaudible = unlocks), then restores. Verified: no JS error, no NotAllowedError after gesture (qa/_unlock-check.mjs). **STILL owner-only (cannot discover):** whether he heard ANY other app audio, phone vs desktop, and whether he reached the Movement break at all — retest needed to confirm this was the cause vs volume/device/not-reaching-break. |
| NB-21 | first-run ka-alpha task = text-only instruction („იპოვე ასო „ა"") + generic 🔤 emoji, no audio → the pre-reader it targets can't know what to do | deep-audit 2026-07-09 s15 (MED, verified) | FIXED 2026-07-10 (§6c screenshot + qa/_firstrun.mjs 7/7 PASS; ✅ SHIPPED LIVE v1.355) — CLASS FIX: firstRunTask now voices EVERY task prompt (9 edge_tts clips fr_tc/ta/te 1-3) on render + 🔊 replay (covered counting too, not just ka-alpha); ka-alpha `show` now = the TARGET LETTER as a big flashcard (`.fr-bigletter`) instead of the meaningless 🔤 → pre-reader hears „იპოვე ასო ა" + matches the shown letter. | **CLASS (same as NB-15/16): "first-run / task prompt assumes reading — no audio + no meaningful picture for ≤5."** `firstrun.js:27` task1 `{q:'იპოვე ასო „ა".',show:'🔤',opts:['ა','ო','მ']}`. FIX (audit): on render play a recorded ka clip of the prompt/target + persistent 🔊 replay, and show the target letter as a big flashcard (placement.js:329 pattern) instead of 🔤. Needs a first-run prompt clip (edge_tts). §14 sweep firstrun.js for sibling tasks. |
| NB-22 | first-run English task offers ≤5 three unreadable English word options (cat/dog/sun) with no per-option audio | deep-audit 2026-07-09 s15 (LOW, verified) | FIXED 2026-07-10 (qa/_firstrun.mjs PASS; ✅ SHIPPED LIVE v1.355) — english first-run prompt now voiced (fr_te1-3) + tapping an option speaks the English word `speak(sel,'en-US')` (en TTS permitted) so a pre-reader HEARS each word; picture (🐱/🐶/☀️) conveys meaning + first-run is guaranteed-win activation. | **Same read-the-option class.** `firstrun.js:35`; firstRunStart shows the ენა/ინგლისური tile to EVERY new profile incl ≤5 (no age gate, screens.js:297) even though the ≤5 english path is empty (placement.js:69). FIX: voice each option on tap (en voice permitted) OR picture options OR hide the english first-run tile for ≤5 (consistent with empty ≤5 english path). |
Legend: next id = NB-49. (NB-47 = v1.360 dark-plate-behind-card-text UX complaint → design-challenge result = dark-ink no-plate, ✅ FIXED + SHIPPING LIVE v1.361; NB-48 = in-app 🎨 theme-cycle inert = the v1.319/v1.357 redesigns hardcoded hex and orphaned the token system, ROOT-CAUSED, product fork revive-vs-remove pending owner call — both owner-reported 2026-07-14 on live v1.360.) (NB-42/43/44/45 = Word-Search fix batch, ✅ SHIPPED LIVE v1.360 2026-07-14 via tools/live-push.mjs — the merge also shipped the code-complete NB-4/38/39/40/41 batch; NB-46 = QA-report meta, ✅ owner-GO'd 2026-07-14, GH-Action verify→fix loop being built.) (NB-25…NB-32 = Codex external-audit intake 2026-07-12, all triage-verified vs live v1.359 before logging; duplicates NOT logged: monolith/CI-gap/orphan/precache/denylist/stale-docs = already-known s18 items; precache increment: wordsearch.js ALSO missing from sw.js precache alongside worlds.js.) Session close: every OPEN NB → SESSION-HANDOFF carried-open block. **2026-07-09 deep-audit (s15): 6 confirmed findings; 4 FIXED same session (Word-Search pre-reader text, audio-bleed on close owl.js+wordsearch.js, clip volume 0.85 audio.js) committed local; NB-21/NB-22 = the 2 pre-existing first-run ones, OPEN. Audio-silence report NB-20 = verdict OWNER-SIDE desktop audio (5-hypothesis consensus), app code clean — owner desktop ear-check pending.**
(Sections below = pre-07-05 history in the legacy `[x]/[ ]` format — kept as record; new intake goes ONLY in this ledger.)

## GOAL
Every reported bug fixed at the **choke-point** (shared layer, whole class dies), verified before it
reaches the owner. LOOP: weakest/highest-value open item → fix at choke-point → verify (logic+§6c render)
→ deploy (fix) → tick. DONE = root found · shared-layer fix · verified · live.

---

## ✅ LIVE (deployed + verified today)
- [x] pattern/reasoning owl speaks Georgian + right lesson/label — v1.298
- [x] iOS keyboard on every tap (registration) — v1.299
- [x] „Русский" dead option removed — v1.299
- [x] teaching beat on the FIRST wrong answer (grasp before retry, shared reQueueWrong) — v1.300
- [x] duplicate question in a round (deduped word/phrase pools) — v1.301
- [x] owl SPEAKS in math (fixed voiceable `say` phrases + clips) — v1.302
- [x] units/tens hint now DEFINES the terms inline (no assumed knowledge) — v1.302
- [x] READABILITY STANDARD (structured tutor text: lead+bullets+conclusion, adaptive font, measure) — v1.303
      + `docs/READABILITY_STANDARD.md`; units/tens hint restructured to match owner mockup.
- [x] adaptive fonts fanned out to ALL cards (question/sub/teach card/equation/count) — v1.304
- [x] owl speaks ka in ALL subjects (INV-3 fan-out complete: vocab/alpha/kings/count/compare/…) — v1.305
- [x] fast-reveal modes (spell/listen-yle/yesno/story) now teach-gate, no fast auto-advance — v1.306
- [x] privacy/consent copy OVER-STATED (owner 07-02): removed Clarity + inert Meta Pixel + cookie banner →
      site 100% cookieless (kept cookieless CF beacon + first-party aggregate Worker); privacy.html ka+en
      rewritten to no-ads reality; "სრული privacy"→"სრული კონფიდენციალურობა"; softened "GDPR & KIDS" — v1.307.
      Live-verified: banner gone, fbq/clarity never load, 0 errors, layout intact.
- [x] post-test recommendation was tiny (.88rem) + buried "which topic" (owner 07-02): placement results now a
      readable `.reco` card — prominent adaptive topic (clamp 1.28-1.7rem), readable lead/sub, + listen button
      (reco_start.mp3, edge-tts ka; playClip-gated) — v1.308. Live-verified: topic 27px, mp3 HTTP 200.
- [x] readability STRUCTURE fan-out beyond units/tens (owner 07-02): 4 inline-list hints → .r-lead + .r-item
      bullets (compare >/</=, shapes, clock hands, EN subject-verb) — v1.309. speakHint strips tags, csay clip
      covers voice (§6-safe). Live-verified: r-item=13, rendered as bullets, 0 errors.
- [x] INV-5 puzzle/riddle objects too small on phone (owner 07-02): pattern/math-pat numbers + rebus/model
      strand text + triangle (fluid box + %-positioned labels + clamp fonts) all clamp-fluid & BIGGER on phone
      — v1.310. Verified @390px: pattern 30→35px, rebus 24→26px, triangle val 15→18px, box 90→117px, aligned.
- [x] INV-3 owl-voice fan-out COMPLETE: listen-yle/yesno/speak/story returned from Tutor.build with no r.say
      (owl silent, TTS-fails on iOS) → added fixed ka say phrases + edge-tts clips (yle/yesno/speak/story_say.mp3)
      — v1.311. Live-verified: manifest ok, 4/4 mp3 HTTP 200, playClip true.
- [x] INV-6 talk grammar + audio drift (owner): built a diff-tool (talk.js q vs manifest key) → found 5 orphans
      (text fixed in talk.js, audio/key stale). Synced 4 prose cards to talk.js SSOT + regen clips: tlk_000
      (plural agreement ყოფილიყო→ყოფილიყვნენ), tlk_018 (უყენებდე→უწესებდე), tlk_025 (tense), tlk_026 (reworded)
      — v1.312. Verified: q→clip resolves, 4/4 mp3 200, old keys gone. tlk_045 poem left (edge). Gemini: NOT RUN (no key).
- [x] INV-1 teach-before-test (owner 07-02): multiplication primer before the first mul round (per profile,
      flag mulPrimerSeen) — teaches × = repeated addition with a visual (3 groups of 4 apples = 12) + owl clip.
      Tap „დავიწყოთ" → real round. — v1.313. Verified @390px: primer renders, listen, start-flow sets flag, 2nd skips.
- [x] INV-4 adaptive ladder for flat modes (owner 07-02): shared flatTier(mode) 1-3 climbs on a 4-correct
      mastery streak; bumpFlat in each handler. compare 10→20→100, skip ×5→×10→×2/3/4, clock +half hours at t2,
      money 2→4 items+more lari — v1.314. Verified: tier climbs+caps, wrong resets, generation scales, renders clean.
      (count/shapes left un-tiered: young foundational / small fixed pool = little headroom.)
- [x] #1 English one-tap „დაიწყე აქედან" (owner GO 07-02 after preview): results primary button launches the
      recommended session in ONE tap (startHereGo; English milestone → vocab quiz), tappable topic chip, secondary
      „ყველა თემა" for free-roam — v1.315. Preview-reviewed on desktop, GO'd, live-verified: one-tap=yes, v1.315.

## Findings (verified, no code change needed)
- INV-2 answerOutcome resolver — ASSESSED, refactor NOT done (deliberate). Fresh audit of all wrong-answer
  handlers: behaviour is ALREADY consistent via 3 appropriate primitives — reQueueWrong (9 handlers),
  teachAndConfirm (listen-yle/yesno/story/spell), rule-reveal on 2nd miss (pattern/reason). exam = no-teach
  BY DESIGN (assessment), count = retry-till-right BY DESIGN (youngest mode). The original "15 handlers, only
  9 share the contract" framing was overstated. Forcing all into ONE monolithic answerOutcome is the WRONG
  abstraction — it would break exam (must not teach) and count (must retry). User-facing value = zero; regression
  risk across 15 modes = high. Correct architecture (small shared primitives, each handler picks the right one)
  already exists. Not shipping a risky invisible refactor for elegance. [If owner still wants the consolidation
  purely for maintainability, it's optional tech-debt, do it in isolation with a full per-mode test pass.]
- "correct answer marks too fast": on the CURRENT live build a wrong math answer no longer flash-marks the
  correct one (verified via a timed DOM timeline) — it teaches (hint 1st miss, gated teach card 2nd miss).
  This was the OLD behaviour before v1.300/v1.302. Re-test on v1.303; if it persists, name the exact mode.

## 🔬 DEEP AUDIT 2026-07-02 (8-dimension Quality-Audit standard, 40-agent team, method-upgraded per-mode matrix + play-on-phone)
Ship gate FAILED — every dimension <8 (brutal bar). Scores weakest-first: **D8 improvement=3 · D7 owl=4 · D3 comprehension=5 · D4 pedagogy=5 · D6 voice=5 · D1 content=6 · D2 visual=6 · D5 human/trust=7.** 48 confirmed findings (1 CRIT, 1 HIGH, 28 MED, 18 LOW). Full result: tasks/w51l1kdx2.output.
Two buckets: (A) shippable BUGS → fixed below; (B) PROGRAM/INVESTMENT gaps (no A/B framework, no native-voice talent, no CEFR/Common-Core tagging, no WCAG style guide) = roadmap forks needing owner decision, NOT loop-fixable.

## ⏳ STAGED — v1.316 (preview, awaiting owner GO → live)
- [~] 🔴 CRITICAL owl leaks the answer (D7 AITUTOR-01): tutor.js math count-on hints printed the terminal number
      (5−2 → "…უკან: 4, 3…"). CHOKE-POINT fix in tutor.js countSeq()/mulSeq(): enumeration always stops ≥1 step
      short of the total → verified 0 answer-reveals across 674 result-revealing combos. Guardrail restored.
- [~] 🟠 HIGH 18 versions never reached returning users (D8-F1): sw.js CACHE frozen at nikolearn-1.297 while app=1.315
      (cache-first SW never re-installed). FIX: ran bump.mjs (single-writer) → APP_VERSION + sw CACHE + landing footer
      all synced to 1.316; this deploy delivers the whole v1.298→1.316 backlog to returning/PWA users. QA gate PASSED.
      HARDENING DONE: installed `.git/hooks/pre-push` — any push to `main` now runs `npm test` and is BLOCKED if the
      gate fails (the gate already flags APP_VERSION↔CACHE↔footer drift as release-blocking); verified it fires on a
      main-ref push. The drift class can no longer ship silently. (Hook is local to this clone, not git-tracked.)

## ✅ v1.317 — audit safe-batch (accessibility + accuracy), deployed
- [x] VIS-2 shield-chip contrast: --sun-d (2.48:1, fail) → oklch(0.52 0.14 72) = **5.55:1** on card (headless-measured).
- [x] VIS-3 movement-break .mv2 --faint 0.640→0.540 (proven main-theme value) = 3.04→**4.61:1**.
- [x] VIS-6 .m-sub + .section-label .71/.72rem → clamp(0.82rem,3.4vw,0.95rem) (readable on phone; rendered @360px OK).
- [x] VIS-1 .topbar flex-wrap:wrap (chips no longer overflow narrow screens; computed wrap confirmed).
- [x] H5-3 privacy.html „ბოლო განახლება"/„Last updated" → 2026-07-02 (matches the v1.307 no-ads rewrite).
Verified: QA gate 0 findings @1.317, 0 console errors, home render clean @360px.

## ⏳ OPEN — audit remainder (queued, honest categorization)
**Fable5 owns (parallel session):** tutor.js answer-leak AITUTOR-01/06 + tutor parts of D3-F6/AITUTOR-03/05.
**Bucket B — investment/asset, NOT code-loop-fixable (owner forks):** CEFR/Common-Core tagging (C2/C1), A/B
framework (D8-F2), per-item discrimination logging (PED-5), native-voice recording + loudnorm (VOICE-3/4/6, C4),
font self-hosting (H5-2), per-unit sign-off ledger (C5).
**Deferred — needs care/coupling:** telemetry events needing Cloudflare Worker redeploy (AITUTOR-02, D8-F4/F6/F7,
D8-F8 submode-in-abandon); wrong-answer-handler routing (D3-F1/PED-6/AITUTOR-04 — regression risk + overlaps
Fable5's tutor.js); PED-3 (count=retry-till-right is BY DESIGN, do NOT consolidate); exam gating PED-7; H5-4
i18n regex (verify vs live emit-strings first); shapes mastery tier D3-F5; readability rubric into gate D3-F7.

## ⏳ OPEN — priority order (owner firehose 2026-07-02; plans: docs/FABLE5-ACTION-PLAN.md · LEVELED-CONTENT-ROADMAP.md · ANALYTICS-ABANDONMENT-2026-07-02.md)
0. [x] 🔴 **Owl answer-leak choke-point + commit** — already done pre-session (v1.316 / commit 6b23c2c): countSeq/
       mulSeq stop >=1 short, 0 reveals over 674 math count-on combos. Staged, NOT live. (Residual all-modes audit -> #4.)
0b.[x] **v1.317 accessibility fixes committed** (were uncommitted, interrupted) -> commit a2361f2. Staged, NOT live.
1. [ ] **Talk cards -> OUR card design** (owner APPROVED the 20). theme/char/emoji/subs/min + grade by age +
       edge-tts clips tlk_051+ + dedup vs 50-deck -> preview -> GO -> live. **(NEXT ACTION.)**
2. [ ] **Leveled content integration** (Math 17 + English 17 self-verified; Kings next). Owner+Gemini QA ->
       wire behind band gates (isYoung/isBig/kingsLevel) -> clips -> preview -> GO. Review HTMLs sent.
3. [ ] **q0-abandonment fix (DATA-DRIVEN)**: 53% of abandons are on the FIRST question; math 65% / kings 59% worst.
       Warm-up/primer + gentle first item + inviting first-screen visual, math & kings first.
4. [ ] **Tutoring = explain the PRINCIPLE** (owner #3): Fable band-aware explanations (x-table, 1-2 unknown
       equations, place value, fractions) + teach/primer flow. Tier-2 moat. Includes all-modes answer-leak audit.
5. [ ] **Owl (bu) upgrade** (owner #4): richer/varied/band-aware persona + warm round-entry intro (ties #3/#4).
6. [ ] **Georgian voice eval** (owner #5, NOT Fable): edge-tts voice/SSML/rate; child intelligibility; options+cost.
7. [ ] **Letter-tracing (centerline) polish** — parked, verified-good; finish later (owner: needs polish).
8. [ ] **Design polish + "wow"** (v2) — last, rides on 1-6.
- ANALYTICS finding: retention ("do they return") NOT measurable by design (no persistent ID = child PII);
  q0 abandonment is the lever. Full read: docs/ANALYTICS-ABANDONMENT-2026-07-02.md.

Legend: `[ ]` open · `[~]` partial · `[x]` DONE + live.
Architecture SSOT: `docs/INTERACTION_CONTRACT.md`. Branch note: hotfixes linear on main; F2 feature branch
(jelly cards, vivid counting, landing telemetry) to be rebased onto main before its own ship.
