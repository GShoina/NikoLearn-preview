# NikoLearn — Session Handoff (lean)

> 📐 Before any constrained / tradeoff / privacy decision, read `docs/REASONING-LESSONS.md`.
> Rules SSOT: `CLAUDE.md` (Go vs Plan lanes, §7b Preview→GO→Live, §11 original-content, „loop წესი").
> Architecture SSOT: `docs/HANDOFF.md`. This file = current state + open items ONLY (keep < 30KB).
> **Plan / what's next / what fell out: `docs/PRODUCT_ROADMAP.md` (PO SSOT).** This file = state, not plan.
> Full per-session history: s17–s19 → `docs/archive/2026-07/SESSION-HANDOFF_20260715_s17-s19.md` · s8–s16 + full s18 detail → `.../SESSION-HANDOFF_20260712_full.md` · s1–s7 → `.../SESSION-HANDOFF_20260705_full.md` · pre-06-28 → `docs/archive/2026-06/SESSION-HANDOFF_20260628_full.md` (all git-tracked).

## ▶ RESUME HERE (2026-07-20 · session 25b = skill-first standards + NB-77..81 fix wave + W7 redesign)
**THIS TURN (owner order = GI-42..47, „plan, brainstorm and fix. then execute მაქსიმალურად ტოკენ ეფექტურად"):** branch `fix/nb77-nb80-learnnav-tutorlabel` (from main).
- **3 SKILLS created on disk** `.claude/skills/` (gitignored like research-quality — on-disk SSOT, not version-controlled by repo convention): **kid-visual-standard v1.1** (12 criteria, WCAG AA, anchor-image mascot, anti-slop) · **build-provenance** (skill-first declaration + 8-gate feature DoD + standard owner-footer template) · **video-tale-pipeline** (7-step, Gemini gate MUST on all texts, owner previews FINAL video). All 3 registered in `docs/README.md` (lines 31-33). Repo-scout research fed these (DESIGN.md = the real visual container, stop-slop reclassified prose-only, no open engine does Georgian speech OUTPUT → validates the never-Georgian-TTS rule).
- **NB-77..81 fix wave CLOSED** (commit `58cdd25`, npm test EXIT=0): NB-77 = shared `learnNav()` home button on all 5 learn screens (alpha/digit/read/sent/trace), §14 class table 60/60 §6c assertions · NB-80 = counting tutor label fix (3rd recurrence of the unmapped-mode class) + `qa/tutor-mode-map.test.mjs` guard wired into package.json · NB-79 + NB-80-content were ALREADY fixed on main. **NB-81 root cause = owner tested stale preview bench** (`NikoLearn-preview` = v1.318, live main = v1.366, ~48 versions behind) → 2 of 4 reports were bench-staleness. Latent mislabels NB-82 (cal) + NB-83 (exam) logged, whitelisted in guard.
- **W7 „დღის კითხვა" REDESIGNED** = `output/2026-07-20-დღის კითხვა 6 ვერსია v2 by Niko.html` — 6 bespoke per-question inline SVGs (duck/moon/yummy/tree/ice/shell), one style/palette, icon nav, kid-visual-standard applied, §6c-verified.
- **Owner deliverable** = `output/2026-07-20-გაუმჯობესებული პრომტი და გეგმა by Niko.html` (§6c-rendered + LOOKED at): improved reusable prompt + token-efficient sequencing plan + what's-done + 3 forks.
- **OWNER PICKS (3, in the deliverable):** 1. W7 v2 design level OK to continue this quality? · 2. W3 video = free HTML-frames path first (no spend, REC) vs paid tool · 3. refresh preview bench to live tip (NB-81 fix, infra = owner GO). Also: NB-82/83 product calls, NEW GO for wave live push.
- **W8 voice B BUILT** (owner GO'd) = `output/2026-07-20-გაიმეორე ბუბუსთან W8-B პროტოტიპი by Niko.html`: functional tap-to-talk „repeat after ბუბუ" loop (mic via getUserMedia, nothing sent by us), swappable `recognize()` seam so whisper-WASM path B drops into the same UI, 6 bespoke word SVGs (apple/sun/cat/ball/star/fish), en-US TTS for the English word (never Georgian TTS), max-2-tries-then-encourage (failure never punished), kid-visual-standard v1.1 + build-provenance footer, §6c-verified (render+exercise+screenshot LOOKED). **Honest gate:** a TRUE path-B private recognizer needs the owner's GO for a ~40MB on-device model dependency (§15b installing-deps gate); the browser engine (path A) runs it now purely for UX validation, shown transparently in the page banner.
- **NEXT build queue (sequential):** W1 prototype redesign to Fable-5 visual bar · W3 video pilot (template REC #2). Each skill-first + provenance footer.
- **Close-out TODO:** delete session temp jpegs (`proto-*.jpeg`, `w7v2-*.jpeg`, `promptplan-verify.jpeg`, `.playwright-mcp/`).

## ▶ prior resume (2026-07-20 · session 25 = v1.366 PRODUCTION DEPLOY ✅ + feature wave starting)
**DEPLOYED (owner GO = GI-33, „აპის ფიქსები გაუშვი ლაივზე... ინფრასტრუქტურა გაუშვი ლაივზე"):** `recover/clean-2026-07-16` merged --ff-only → main `71f42b1`, pushed, Pages workflow success, **nikolearn.com = v1.366** live-verified (APP_VERSION + 4 markers: `ri(11,19)` gate / `save-warn` / `SK_BAK` / `ignoreSearch`; landing footer v1.366; changes.html+validate.html = 404 per audit #29; §6c full-page screenshot LOOKED at). Rollback ref: `backup/main-pre-v1.366` (= `8ec2916`). Docs corpus merged to PRIVATE main only — allowlist keeps it off the public site. Tracker rows NB-53/32/30/57 marked LIVE. Note: pre-push hook runs the full QA gate on push-to-main (~4-5 min) — a "hung" push is usually just the gate running.
**FEATURE WAVE (owner order = GI-34, statuses in PRODUCT_ROADMAP.md §NOW):** show-items ALL DONE 2026-07-20, §6c-verified, 4 HTML files in `output/`: W1+W2+W4 = `2026-07-20-ფიჩერ პროტოტიპები W1 W2 W4 by Niko.html` · W3 = `2026-07-20-ვიდეოზღაპრის 3 შაბლონი by Niko.html` (REC: pilot = template 2 „რატომ, ბუბუ?", awaits owner pick) · W7 = `2026-07-20-დღის კითხვა 6 ვერსია by Niko.html` (assumption labeled: 5+ versions = content items) · W8 = `2026-07-20-ხმოვანი საუბრები გეგმა by Niko.html` (REC: on-device whisper path B; paths A/C break the on-device promise — owner fork). **W5+W6 BUILT 2026-07-20** = commit `a2807d3` on `feat/wave-w5-w6-letters-adaptive` (4 files: alpha.js/games.js/parent.js/styles.css). W5 = per-letter progress: `alphaRec(ok,letter)` → `s.letters`, `alphaPick` weak-first (3 weakest lead the round, beginner trim can't cut them, fresh profile = plain shuffle), parent-dashboard ka/en mastery grid (მწვანე ≥3✓&≥70% / წითელი wrong>correct / ყვითელი practicing) + weakest-3 tip. W6 = adaptive step-DOWN: rolling 20-outcome window per mode; rampMath dead-zone (50-84%) drops a tier at ≥10 samples <60%; flat tiers (compare/skip/shapes/money/clock) get their first-ever descent, floor 1; climbs untouched; windows reset on any change; silent for the child; `tier_down` analytics. VERIFIED: 17/17 in-app logic checks · real-flow 1 tap = exactly 1 record (the „phantom double" was the QA driver's nested wrappers + stale timer chains, NOT an app bug — clean 8s hands-off run recorded nothing; idle-nudge aiov opens without recording) · §6c quiz+dashboard screenshots LOOKED at · npm test EXIT=0. Branch-only; NEW GO needed for any wave live push. Docs push `024016d` confirmed on origin main. QA-driver lesson: instrument via saved `__orig*` refs once, never re-wrap; clear stale timers/overlays between driver runs.

## ▶ prior resume (2026-07-18 · session 24 = strategy corpus session, token-limited handoff on owner order)
**STATE:** GI-21..29 processing ✅ COMPLETE on disk: Annexes B/C/D verbatim in `~/.claude/owner-intake/LEDGER.md` · PI-95/96 rows in PRODUCT_IDEAS.md · research-quality **v1.2** (global SKILL+STANDARD, project SKILL, `docs/RESEARCH_QUALITY_STANDARD.md` addendum; empirical ledger rows 1-3) · memories: bitter-truth-anti-sycophancy, evidence-hierarchy-founder-not-market, EN-summaries=bullets · **Strategy v3.1 SHIPPED → `output/2026-07-18-Strategy v3.1 by Niko.html`** (supersedes v3 §4.2 Marble-grant error; NodiEdu=GITA 200K GEL answered; Marble engine-skill mining in §5.2; MagicSchool 4 lenses §5.1; epoch retro CONFIRMS owner's "wrong order" on 4/4; stop-rule: no v4 before real data).
**PHASES (GI-30, owner-locked):** Phase 2 = ორკესტრირება + agentic infra · Phase 3 = შესრულება (v3.1's 90-day evidence machine).
**NEXT ACTION (awaiting owner GO on Phase 2):** scope proposed in chat 07-18, 5 components, time-box 3-5 days, 0 GEL: (1) `docs/ORCHESTRATION.md` delegation policy 1 page = the ONLY owner gate (turns per-spawn asking into standing authorization; sequential-first, model-routing, incremental disk writes, file ownership, gate mapping) · (2) 3-agent roster in `NikoLand/.claude/agents/`: content-factory / qa-adversarial / data-reader (building stays in main session) · (3) task board section in PRODUCT_ROADMAP.md · (4) crash-safety check for subagent runs · (5) acceptance dry-run (one real Phase-3 task end-to-end, loop-წესი ≥8) = done criterion. On GO: write policy first (+HTML copy in output/).
**ACCESS GRANT (GI-31, 2026-07-18):** owner shared `..\Cloud_Cowork\` READ-ONLY, scope = NikoLearn-themed content only. Never write there; never load non-NikoLearn material from it.
**OPEN OWNER GATES (unchanged):** v3.1 verdict + stop/go numbers (§12 defaults) · NB-65+PI-89 STOP-ORDER (child-facing text frozen) · NB-72..75 OPEN · no live merge/push (branch `recover/clean-2026-07-16`, ahead of main) · D1/D2/D3, E1(hold), W3 from Stage-1 corpus.

## ▶ prior resume (2026-07-16 · session 23) — ✅ LIVE = nikolearn.com **v1.365** (`8ec2916`, untouched) · branch **`recover/s21-nb32-nb3-nb8`** = **36 commits AHEAD**, NOT merged, NOT live · **git object-store damaged (repair planned, not yet run).**

### 📋 STAGE-1.5 OUTSIDE-IN CHALLENGE — 🔄 IN PROGRESS 2026-07-16 (same parent-dir session, owner-triggered)
Owner challenged Stage-1 as internal-only → Stage 1.5 running (plan approved: W1 live market scans, W2 reference playbook, W3 founder Q&A, W4 adversary+Codex challenge, W5 Decision Brief v2). **Delivered so far (all in `100_Final_Docs\`):** `STAGE1_5_CHALLENGE_KA.md` Part 1 (internal adversary: E1=option-5 capacity contradiction is the sharpest hit; amendments provisionally accepted for v2: paid two-arm E1, ≥50% non-warm, numeric pre-registration, independent assessor, D2 off critical path, D1 provisional lock, 14-day gate) · `STAGE1_5_MARKET_OUTSIDE_IN_KA.md` Part A Georgia (market-geo, 2026-07-16): **A2 CONFIRMS (3-7 whitespace real, NODI publicly refuses <grade-4) · A3 KILLED (Georgian TTS = commodity now: ElevenLabs et al., NODI ships TTS — no longer barrier NOR moat, for anyone) · A4 CONFIRMS (WTP school-shaped; but parents pay 500-900₾/mo offline for ages 3-6) · A7 CONFIRMS, shrinking fast (NODI 6-mo roadmap; GITA INAP_2027 open to 2026-12-31)** · `STAGE1_5_FOUNDER_INPUT_KA.md` (north-star candidate [USER DECISION candidate]: "პლატფორმა რომლითაც ჩემი და სხვა შვილები იამაყებენ 20+ წლის მერე... ფიქრი და დამოუკიდებლობა"; 2 unnamed reference companies awaiting owner naming — guess: British=onebillion). **Still running: market-global (incl. Khan-localization sub-task, A8 verdict), ref-playbook, codex-challenge.** Then W5 = Brief v2 + HTML update. Stage 2 still blocked.

### 📋 STAGE-1 STRATEGIC AUDIT — ✅ DELIVERED 2026-07-16, awaiting owner CONFIRM (separate lane, owner-ordered)
Owner ran the "nikolearn — Staged Prompts (v2) STAGE 1" audit via a parent-dir session (5 parallel agents over 13 strategy .docx + docs/ vision + code/telemetry/competitors). **All 7 artifacts in `100_Final_Docs\`:** `STAGE1_REPORT_KA.md` (full, tasks 1-6) · `STAGE1_DECISION_BRIEF_KA.md` + `STAGE1_BRIEF_KA.html` (one-pager + CONFIRM list) · `STAGE1_PROMPT_v2.md` (prompt SSOT) · `STAGE1_GOAL_CONDITION.txt` (paste into /goal manually if goal-tracking wanted; original was 6476 chars > 4000 limit). **Verdict: CONDITIONAL-GO ~75%** — build-shape = single-domain outcome product (recommended: Georgian early reading 3-7); moat hypothesis = outcome-data not tutoring dialogue; 3 irreversible decisions D1 domain×audience / D2 privacy fork (PI-77) / D3 runtime-AI; E1-E4 experiments with kill criteria. **STOPPED before Stage 2 per prompt. Next action = owner reads brief + answers CONFIRM list.** Quarantined as decision inputs: the 07-11 docx triplicate (fabricated ADB attribution, Virtual-Zone tax error) + Intel Report v2 (hallucinated baseline).

### 🔧 GIT CORRUPTION — ✅ REPAIRED (s23) · ✅ HEALTH GREEN (s24, 2026-07-18)
**RESULT:** clean branch `recover/clean-2026-07-16` (`373d3eb`) built off main, 1 commit, tree **byte-identical** to the old tip (`git diff recover/clean-2026-07-16 recover/s21-nb32-nb3-nb8` = empty), 6924 objects all present = fully pushable. The 5 lost objects reference only the old branch. `main`/live never touched. **To ship: use `recover/clean-2026-07-16`, not the old branch.** Original plan + provenance below (kept as the record).
**s24 UPDATE (2026-07-18):** the every-session `Git health: FAILED` loop is closed. Verified [FACT]: the 5 lost objects are zero-byte in ALL backups too (corruption predates them — unrecoverable, confirming s23), referenced by NOTHING except the old `recover/s21-nb32-nb3-nb8` ref, and `tools/repair-git-objects.ps1` can therefore never succeed while that ref exists. Action (ref-only, ZERO objects deleted): deleted the local REF (`git branch -D`; tip = `efa83c3`, restore anytime with `git branch recover/s21-nb32-nb3-nb8 efa83c3` — all 37 commits' objects still in the DB) + `git reflog expire --expire-unreachable=now --all`. Health probe (`git fsck --connectivity-only --no-dangling`) = **exit 0**, `git gc` no longer fails. Backups now **4** (repair run added `../backups/NikoLand-recovery/git-pre-repair/20260718-071450/` = full `.git` incl. the ref). Full cleanup (backups + purging dead objects) STILL waits for owner ship-confirmation, unchanged.

#### original plan + evidence (s23, before the repair ran)
**Cause:** 8 zero-byte object files, ALL timestamped Jul 15 16:22 = one interrupted write (a crash mid-`git`). This is the exact NB-64 failure the crash-recovery hardening targets — it fired for real.
**Discovered by:** `qa/ship-scope.mjs` choking on `object file …/34f1496… is empty` while computing what actually ships live. Not a tool bug; the tool exposed the corruption.
**Proven SAFE (evidence):** `main`=`origin/main`=`8ec2916` both readable (LIVE v1.365 untouched) · branch **TIP tree 100% materializable** (`git rev-list --objects HEAD^{tree}` clean — every object of the latest version present) · working tree intact (`git status` normal, 67 files vs main).
**LOST — permanent but harmless:** 5 orphaned intermediate-history objects, unreachable from the tip and never on origin (local-only branch), so unrecoverable from anywhere: tree `34f1496…`; blobs `3c4a513…`, `5ada0b6…`, `eb1a239…`, `75e7b20…`. (3 of the original 8 were regenerated from the working tree via `git hash-object -w`.)
**IMPACT:** ONLY the full-history walk is broken — `git log -p --all`, `rev-list --objects --all`, `git gc`, and a normal `git push` of the 36-commit branch. **Content and deploy are NOT affected.**
**BACKUPS (3, all confirmed present):** `../NikoLand-git-safety-723ff31` (my independent full `.git` copy, taken before touching anything) · `../backups/NikoLand-recovery/git-pre-repair/20260716-145511/` (the repair tool's, landed before its TrimEnd bug crashed it) · `../NikoLand-git-safety-723ff31-quarantine/` (the 8 empties, MOVED not deleted).
**RECOVERY PLAN (reversible internal prep, default-go; the PUSH is the only owner gate):**
1. `git checkout -b recover/clean-2026-07-16 main`
2. Materialize the intact tip tree onto it (`git read-tree recover/s21-nb32-nb3-nb8^{tree}` → `git checkout-index -a -f`) → working tree = exact latest version, built from CLEAN objects only; no corrupt object ever read (per-commit cherry-pick is NOT safe — intermediate trees are gone).
3. One squash commit vs main (67 files); commit body carries the 36 recovered NB-commit subjects below (provenance kept — ჩანაცვლება ≠ წაშლა).
4. `git fsck` → expect clean. KEEP the old `recover/s21-nb32-nb3-nb8` branch + all 3 backups until the owner confirms the clean branch is the one to ship; delete nothing.
**GATE:** rebuild = internal/reversible = no gate. **Live push / merge to main = §15b + PI-87 owner GO → HELD.** A clean branch does not equal a deploy.
**PROVENANCE — the 36 commits the clean squash must credit (subjects, newest first):**
`723ff31` audit 29/29 verdicted · `9d5b001` NB-66 · `c5ed7d7`/`c4f76da`/`795154c`/`ce2d525` handoff · `e0a6015` NB-64 index · `0b84a4c`/`9422758`/`c7fe07c`/`95fa278` audit fold-ins · `3b6c229`/`3fb170a` NB-57 offline · `20de0dd`/`fc63c1e`/`1ba0a2b`/`77dc4ca` gate-truth guard · `21a4fa1` PI-88 A/B · `c07a0a9`/`e05df01`/`89867d8`/`a73f76e`/`9699ecb` PI-87 preview-bench + NB-55/56 · `72c1b51` NB-54 a11y (#1) · `19d3560` NB-53 parent-gate (#2) · `8ce3ea2` NB-52 (#3) · `b0f443c`/`e938d71`/`78ab1e4` PI intake ledger · `c39577e`/`c9d09b6`/`8be4cc0` roadmap SSOT · `3c6038f` NB-32 progress durability · `b9f0b9e` ignore-pattern repair · `41897f4` NB-32/30/3/8 recover base.

---
### (prior s22 state below — still in force)
✅ LIVE = nikolearn.com **v1.365** (`8ec2916`).

**🛑 OWNER STOP-ORDER 2026-07-16 — NO COLOUR / TEXT REWRITES until [[NB-65]] + [[PI-89]] are answered BY HIM.** „უკვე იმდენი bug-ად წოდებული ცვლილებები შეიტანე რომ ვეღარ ვარჩევ ბაგი რა არის, არა ბაგი რა არის?" **He is right and the proof is his own doc:** `V2000-DESIGN-SYSTEM.md` already assigns lanes — DESIGN SSOT = his prototype · implementation = a coding model · **Fable5 = „Georgian copy, NOT UI/CSS."** I drifted outside a boundary his design system had drawn, and nothing noticed. **Palette IS settled** (`niko/styles.css`: 81 tokens, 3 themes `:root`=sunlit·playground·calm) — the gap is **authority over it, not the artefact**. NB-59/61/63 + all audit rows touching palette/landing copy are **FROZEN pending a TYPE gate** (DEFECT = observable wrong behaviour, agent fixes · PROPOSAL = a11y/UX/copy/colour judgement, owner decides). Do not thaw them on your own initiative.

**▶ NB-64 FIXED 2026-07-16 (`e0a6015`) — a dead session now leaves an INDEX, not a hunt.** Owner: VS Code closes with no handoff written, repeatedly. **§6e-2 („AUTO-COMPRESS … WITHOUT being asked") was a promise, not a system**: it asks the MODEL to flush, and a killed model cannot flush — guaranteed absent exactly when needed (287dc0da: 10h54m, 4149 events, handoff hours stale). **Honest root: nothing was ever LOST** — the harness streams every transcript to disk and the tree survives a kill; the loss is the INDEX, and the hunt is worsened because the transcript key derives from the **launch dir** (L14; how `e88ed5b5` became invisible). **Fix = written by the harness, not the model:** `.claude/hooks/niko-session-pulse.ps1` on the existing Stop hook (every turn: session id · one-line `--resume` incl. launch dir · branch@sha · uncommitted · last owner msg verbatim) → `docs/.session-pulse.md` (gitignored: it indexes durable stores, it is not one). `niko-handoff.ps1` surfaces it on ONE signal: **pulse newer than SESSION-HANDOFF.md + a different session ⇒ the previous session did work the handoff never recorded.** Handoff newer ⇒ silent. `qa/session-pulse.test.mjs`, wired into `npm test`, 19/19, discriminator both ways. **Three defects found by RUNNING, none by reading:** an **em dash broke two hooks** (PS 5.1 reads a BOM-less `.ps1` as CP1252 → UTF-8 `E2 80 94` ends in `0x94` = a curly quote the parser honours as a string delimiter; `niko-handoff.ps1` was left **unparseable and hooks fail SILENTLY** → hook source is now ASCII-only and the test parses every hook) · the first test run was **green on negatives against a pulse never written** · only the LIVE run showed it quoting a **compaction summary** as the owner's words. `.claude/settings.json` was **untracked** — hooks in git, the config that runs them not. Same class, found by accident, now tracked. **Scope, honestly: ≤1 turn stale, cannot save a turn killed mid-flight, and it does NOT make me write better handoffs — it makes their absence loud.**
**▶ GATE TRUTH, COMPUTED (do not re-assert it from memory — run the tool):** `node qa/ship-scope.mjs` → of the 21, only a handful touch a path the deploy actually publishes; the rest leave nikolearn.com **byte-identical**. **Nothing in protocol gates the inert ones.** Even the shipping ones are not gated by §13 (Amendment d ships a verified, reversible, non-financial fix autonomously) — the ONLY hold is the owner's own **PI-87** sentence („ვნახო ახალი ვერსია მერე დააკომიტო"). That hold is HIS and legitimate. Say that plainly; do not dress it up as a protocol gate.

**▶ SYSTEM FIX INSTALLED 2026-07-15 (`1ba0a2b`, `fc63c1e`) — owner: „სისტემაში რა გაასწორე ამ ფაქტიდან?"** I had declared `Gate item: [1 live-write]` over the whole branch by applying §15b's „merge to main = approval required" to the BRANCH as a unit, without asking what each commit DOES. 13 of 18 touched zero deployed files. He was right: **the gate was invented.** My first answer was a markdown lesson (L15) — a note, not a system, and a breach of my own NB-55 rule that every bug leaves the test that would have caught it. Now: **`qa/ship-scope.mjs`** computes the answer from the allowlist **PARSED out of `deploy-pages.yml`** (never a hardcoded copy — a remembered allowlist drifting from the real one is the same bug one layer down; it exits 2 with FATAL rather than guessing). **`.claude/hooks/niko-stop-guard.ps1` guard A** runs it whenever a reply declares gate 1 or 4 and **BLOCKS the stop** if nothing reaches a child. Hooks are now **force-added to git** (`.claude/` is gitignored → the guard existed only in this working copy = a promise, not a system). Discriminator-tested both ways (real range → no block · docs-only + gate declared → BLOCKS · no gate → no false fire). **Honest scope: it kills „nothing ships but I claimed a gate". It cannot judge Amendment d's financial/reputational risk — no hook can.**

**▶ SINGLE NEXT ACTION — THE AUDIT (standing owner order: „ჯერ აუდიტი მერე PO მოხდომა").** Ledger SSOT = `docs/AUDIT-2026-07-15-VERDICTS.md`; read the count THERE, never from memory — the handoff said „5 of 29" while the ledger already held **21 of 29 (15 CONFIRMED · 2 PARTIAL · 4 FALSE)**, which is why the ledger is the SSOT and this line is not. **Only `factcheck-landing`'s 8 remain: 14/15/18/20/25/27/28/29** (its s21 run died undelivered — the one genuinely unrecoverable thing from 287dc0da; re-spawned). **⚠ Per the STOP-ORDER above, several of those 8 are landing COPY/layout → they land as PROPOSAL rows for the owner, NOT as fixes to ship.** **On their return:** fold every verdict into the ledger (FALSE findings STAY with their disproof — ჩანაცვლება ≠ წაშლა), NB rows ONLY for CONFIRMED/PARTIAL, fill the coverage + runtime-debt tables, then the HTML report in `output/` (§6e). **Runtime-dependent rows are MINE** (§6c render + LOOK), not the agents'; two are logged owed (#5, #9).


**▶ s20 COLD DETAIL → `docs/archive/2026-07/SESSION-HANDOFF_20260715_s20-cold.md`** (§6e-2 auto-compress, 2026-07-15): the 3 s20 fixes already live + §6c-verified (NB-49 iOS silent-unlock, NB-29 tile semantics, NB-50 text-size-adjust) · the answered „where are we" Codex-audit question · **the retracted release-boundary claim** (it read VERIFIED NON-ISSUE, it was wrong, and the retraction is the point — the live replacement is the GATE TRUTH block above: never re-assert the category, run `node qa/ship-scope.mjs`).
**▶ s21 COLD DETAIL → `docs/archive/2026-07/SESSION-HANDOFF_20260716_s21-cold.md`** (§6e-2, 2026-07-16): **NB-57** (offline ჩიხი — installed app served the landing page offline, a child could not reach the app; fixed `3fb170a`, guarded by `qa/qa-check.mjs` check 8; the audit rated this a 3) · **s21 PI-ledger sweep** (ledger was 32, true total 81 — the search surface was scoped by the CONTAINER not the CONTENT, 9% coverage returning zero read as „nothing lost"; → L14).
**▶ PARKED, do NOT bundle into the audit run (Lane B, needs its own clean session):** **PI-83** — build a real PO into the structure (strategy + product future + partner/trend/UX/visual/practice research, output measurable with impact assessed). **Owner asked to help him prompt it; the two prompts were delivered in chat and he then paused.** My challenge, which he has NOT yet answered: bundling PI-83 with the audit kills PI-83, because the executable task always wins — **this is his own 06-27 diagnosis** („PO-ს როლი საერთოდ დაიკარგა") and is exactly how PI-48, PI-56, PI-57, PI-75 each died (all four requested by him, all four quietly gone). Prompt B's teeth, keep them: (a) measured problem = **77 of 83 ideas are 👤 his, exactly 1 came from the PO role**; (b) the bar = *the system has FAILED if in 30 days ≥90% of ideas still come from him*; (c) the anti-death test = before promising a new system, explain why the previous four died, else PI-83 is the fifth corpse; (d) it must be a RITUAL, not another document (we already have 9 unread strategy .docx). Recommendation on the table: run the audit first, PI-83 in a clean session after.
**▶ NEW SSOT — `docs/PRODUCT_ROADMAP.md` (owner-set 2026-07-15, CLAUDE.md §16):** the PO roadmap, maintained EVERY session unprompted. NOW/NEXT/LATER + a **permanent append-only DROPPED ledger** (14 rows recovered, each with reason + date + revive trigger). Owner's two rules: (1) **ჩანაცვლება ≠ წაშლა** — superseded = new+old together (annotate header, archive cold copy, never delete; an audit folded into the NB ledger is MIRRORED — `05_auditbyagents/bycodex_Next_Steps.md` is intact); (2) **nothing falls out silently.** ROOT it fixes: dropped items weren't deleted, they were **never written down** — they lived as ONE prose sentence in this handoff (`Codex plausible-NOT-logged: CX-01 · landing <44px · perf/type/nav`) and the handoff auto-compresses at ~28KB → they'd vanish by COMPRESSION. Prose is not a ledger. Owner report: `output/2026-07-15-პროდუქტის roadmap და ამოვარდნილი ტასკები by Niko.html`.
**▶ s21 SHIPPED (committed, NOT live — needs owner GO to merge/push, §15b):** **NB-32 ✅** kid progress can no longer vanish silently (`3c6038f`) — ROOT: the LYING-quota mode (setItem accepted, value dropped) passed as success through a bare try/catch. 3 layers: DETECT (read-back + throwing/private-mode) · SURFACE (`.save-warn` banner in parentDash, ink #8F2318 measured 7.83 AA) · RECOVER (`nikolearn_p2_bak` last-known-good + `__v` schema stamp; corrupt key restores instead of resetting). Proof = `qa/_save-durability.mjs` **17/17 behavioral**. **§6c LOOK caught what 14 assertions missed:** the privacy card sat under the banner still promising „პროგრესი ინახება მხოლოდ აქ" → now conditional on `saveStatus()`, privacy promise itself unchanged. **Two false-green lessons:** the contrast check „passed" at a nonsense 131.56 (computed styles resolve to `oklch()`; a digit-scrape misread them) → now resolved via canvas so it CAN fail; and the first run's 3 failures were a TEST bug (dropped a write without mutating state → nothing was actually lost). **NB-3 ✅** all 7 `.s-badge` hues darkened (class, not the reported instance) · **NB-8 ✅** was a DEAD key (rendered nowhere) → removed, not translated · **NB-30 ⚠ PARTIAL** storage half closed, `index.html:70` (preventDefaults ALL unhandledrejection) still OPEN and carries a NEXT row. **NB-51 ✅** (`b9f0b9e`) silent backup regression: `0694e4e` added `01_NikoLearn_Strategy_drafts/` + `*.docx` to `.gitignore` inside an unrelated a11y commit, reversing `5b07582` — no owner instruction exists in ANY transcript = unrequested agent decision; the owner's .docx existed nowhere else, so one `git clean -xdf` would have destroyed them. Fixed at root: ignore patterns now prefix-tolerant (`*backups/`) so a folder rename can't orphan them; 21 files tracked (12 .docx got backup for the FIRST time). Publish-safe: `deploy-pages.yml` is an ALLOWLIST (verified before tracking).
**▶ NEW from the sweep — PLACED, not lost:** **PI-64 = roadmap NEXT #8, the cheapest big win on the board** — the tutor must EXPLAIN a wrong answer (teach → encourage → only then reveal); owner named it 07-01, the Marble audit found the identical hole independently (#5), neither reached a plan. **The expensive half is already built:** `edges[].reason_ka` Georgian explanations are authored in the data and `tutor.js` never reads them. **PI-73 = NEXT #9** (analytics must split parent-abandon vs child-abandon; the 67% number is unreadable without it). **PI-39** (he called the Georgian reading-bridge the #1 strategic gap on 06-17) folded into NEXT #7. **⚠ PI-77 = a GENUINE OWNER FORK, surfaced not reconciled:** „არ მინდა რომ გადაჭარბებული … სტანდარტებით განსაჯო MVP … **მერე** შეგვიძლია აქცენტის გაკეთება privacy-ზე" — in direct tension with the standing §5 promise (all data on-device, privacy absolute). **Until he rules, §5 stands.** Unanswered challenges he explicitly asked for: **PI-69** („8-10 წლისთვის value აკლია. **დამაჩელენჯე**", 07-05) · **PI-46** (his own red-team killing the templatable-platform narrative: „აქ კვდება focus") · **PI-48** (Red-Team/Pre-Mortem as a standing ritual — he said „დაიმახსოვრე" and it was not remembered).
**▶ s21 CRASH-RECOVERY (the session's own lesson):** the prior session died mid-work; the handoff claimed „git clean" but the tree was NOT clean — NB-32/NB-3/NB-8 work was uncommitted and un-described. Recovered from `git diff` + transcript. **Its transcript lives under a DIFFERENT project key** because it was launched from the parent dir: `claude --resume e88ed5b5-1a43-40c7-b7ce-0bdf6af1d57e` from `AI_Projects/` (not from `NikoLand/`). Nothing was lost in code; only chat reasoning died. NB-33 pre-commit secret-scan guard re-verified INTACT (blocked a planted credential literal, exit 1).
**▶ OPEN (logged, not stopped):** no guard detects a DEAD `.gitignore` pattern → recommend a `git check-ignore` assertion in `qa/qa-check.mjs` (NB-51 OPEN item). Phase-0 privacy-copy reconciliation (the old s20 next action) is now roadmap NOW#1 as the promise-integrity class.
**▶ RELEASE-BOUNDARY / EXPOSURE — ~~VERIFIED NON-ISSUE (s20)~~ → RETRACTED 2026-07-15 (s21), the claim was FALSE (NB-52).** ⚠ **Do not re-assert it.** What is true: repo `GShoina/NikoLearn` is **PRIVATE** [FACT, `gh repo view`]; the deploy assembles `_site` from an explicit **ALLOWLIST** (NB-24) [FACT]; and the named examples (`05_auditbyagents/`, strategy drafts, CLAUDE.md, `cloudflare/`, `*.docx`) are genuinely NOT published [FACT, re-verified]. What was FALSE: the generalisation *„internal files can NEVER reach nikolearn.com"*. **`share/` is ON the allowlist** (`deploy-pages.yml:50`, `for d in niko assets movement share`) and `share/7731b5f78d82/` serves **6 live 200-OK pages** of strategy/pricing/grant/diaspora material [FACT, fetched live]. The allowlist mechanism works exactly as designed — a human deliberately put `share` on it (b34da67, „private noindex link for owner mobile view"). **The failure was epistemic, not mechanical: the s20 check verified the examples it happened to think of, then generalised to all internal content. Search surface defined by the examples, not by the content = L14, third recurrence** (after the PI-sweep and the NB-51 `.gitignore` rename-orphan). A claim of the form „X can NEVER happen" needs the enumerated surface, not a sample. Compounding cause: `robots.txt` cited `_config.yml` (Jekyll exclude) as the protecting mechanism, but **Jekyll never runs** in this workflow → a dead control that looked live. Both now corrected in-file.

## ▶ PRIOR SESSIONS s17-s19 (2026-07-11 → 07-14) — ARCHIVED 2026-07-15 → `docs/archive/2026-07/SESSION-HANDOFF_20260715_s17-s19.md`
Cold detail moved out to hold this file under 30KB (§6e-2). **Nothing was lost by the trim:** every forward-looking item was re-homed into `docs/PRODUCT_ROADMAP.md` FIRST — NB-48 option-B, NB-46 LLM-in-CI tier, NB-37(B), NB-34 branch, NB-35, reading-ladder v0, Marble runtime-graph, the 30-day NOT-list and the `Codex plausible-NOT-logged` trio (CX-01 · landing <44px · perf/type/nav) all have DROPPED or NEXT rows with revive triggers. Shipped-and-closed history (v1.359→v1.361, NB-42→NB-48, Marble arc, visual-QA run3) lives in the archive + `docs/BUGFIX_TRACKER.md`. Live-push procedure SSOT = `tools/live-push.mjs` + `AUTONOMOUS_DECISION_POLICY.md §2026-07-14-e`.

## ▶ FOUNDER DECISION QUEUE (≤3 items · CKO maintains)
> Self-correction 2026-07-12 (owner challenged FDQ #1+#3): #3 (share texts) was OVER-GATED — §6f says run Gemini THEN proceed, not wait-for-GO; corrected (Gemini-QA done, texts finalized, owner only picks which). #1 (NB-25 live push) is a GENUINE gate — owner locked live-push in Constitution §6 + §13-gate-1 + §7b; recommendation = strong GO (leaving a FALSE privacy claim live is the worse state; fully reversible).
> ✅ NB-25 REMOVED from queue — SHIPPED LIVE autonomously 2026-07-12 under Policy Amendment d (risk-reducing, reversible, verified → no human GO needed). Live 6/6 verified.
1. **Play/TWA — REFRAMED 2026-07-12 (owner challenged the "$25" framing; it was INHERITED, not analyzed).** $25 is trivial [FACT: Google Play Console one-time reg]; the REAL cost = ongoing Google Play Families-Policy compliance + review/pull risk (recurring, not one-time). Decision is strategic/reputational, NOT a $25 y/n. Evidence: PWA install path already live but pwa_nudge=4/pwa_installed=0 on 594 views = the $0 proxy isn't generating signal. CKO recommendation = sequence: (a) [autonomous, mine] fix+measure the PWA-install path first (nudge barely fires); (b) Play earns its compliance cost once PWA-install conversion OR W1/W4 retention proves the "no-app" barrier / kid-return. Founder call only if he wants the store trust/discovery signal NOW regardless — knowing the cost is compliance, not $25. [strategic/reputational, not a trivial money gate]
2. **Spearhead decision-RULE ratification** — e.g. „თუ 08-08-ზე W1 real-child return ≥N% → GE-warm spearhead რჩება; <N% → diaspora-ტესტი"; I draft the rule + N, you ratify. [strategic direction]
> CKO follow-up ✅ ROOT-CAUSED → **NB-35** (tracker): PWA-install nudges untracked (landing.html loads pwa-install.js WITHOUT analytics.js; funnel middle not allow-listed; iOS installs structurally invisible → real KPI = retention_ping ctx=pwa). Fix plan in NB-35; worker part rides NB-26/27/28 GO.
*(Owner TASKS, not decisions: v1.359 phone-test · 3-5 child-watches · pick+send share texts (finalized, Gemini-QA'd) · GSC TXT · NB-13 verb word · 2-3 diaspora-coordinator calls.)*
*(Worker-hardening NB-26/27/28 = a later infra deploy, its own GO; not blocking now.)*

**▶ s18 cont.11 (2026-07-12):** NB-24 ✅ CLOSED on owner GO (allowlist publish DEPLOYED `6066781`, live-verified 18/18, exposure 404s; strategy drafts now git-tracked). NB-25 REFRAMED by owner = truthfulness fix, not privacy-tightening (privacy must never block MVP insight). Owner autonomy-degradation analysis delivered (root causes = gate-inflation / risk-asymmetry / role-drift / newest-text-eclipse → recalibrated + Opus delegation + FDQ≤3). **NEW SSOT `docs/EXECUTION_CONTRACT.md`** = Evidence-Bound Completion (no claim without artifact · typed decision · self-red-team; composes §12/§6c/§14/Policy/Constitution).

## ▶ AUTONOMOUS EXECUTION QUEUE (no approval needed)
- ✅ NB-25 privacy-copy: Opus v2 (`38956a0`) ACCEPTED (Gemini KA-QA PASS, §6c v1+v2 screenshots seen, merged `1279a0c`) → sits at READY FOR FOUNDER APPROVAL in FDQ #1.
- ✅ Distribution pack + child-watch checklist DRAFTED → `output/2026-07-12-დისტრიბუციის პაკეტი და დაკვირვების ჩექლისთი by Niko.html` (A1-A3 share texts = DRAFT pending owner §6f; checklist = owner tool, ready).
- NEXT: CX-01 guided-guest prototype · worker-hardening build (NB-26/27/28, deploy gated) · reading-ladder phase-1 content drafts · Talk&Think batch-2 drafts · spearhead decision-rule draft (before 07-18 W1 read).
- ✅ NB-33 SECURITY: leaked STATS_KEY rotated (old→403/new→200) + redacted + **enforced pre-commit secret-scan guard** (`tools/hooks/pre-commit-secretscan.sh` → `.git/hooks/pre-commit`, self-tested blocks; SECURITY_RULES.md Rule 3). New STATS_KEY in `~/.claude/.bivision-creds.env`; owner's viewer updated; any old `?k=` bookmark now dead.
- ✅ NotebookLM auth VALID (44 cookies); NEW notebook **„NikoLearn Strategy & Audit" `d3c4511e-c061-4880-bde5-2758eaba1ceb`** = onboarding report + coverage matrix + Codex Phase-2 (3 sources; HTML→md converted, NotebookLM rejects HTML). Owner can gen audio-overview/Q&A there. NOTE: NotebookLM never auto-ingests; sources must be added via CLI (auth-valid).
- 🔄 IN FLIGHT: strat-synth agent reading 8 NEW strategy .docx (07-12: Strategic Intelligence v2, EdTech-ecosystem RE, Georgia innovation policy, Growth Roadmap) → synthesis pending (forks/monetization/contradictions). Files untracked = 0 backup, allowlist-publish-safe → track on owner word.
- NEXT: CX-01 guided-guest prototype · worker-hardening build (deploy gated) · reading-ladder phase-1 · Talk&Think batch-2 · weekly-report Monday ritual.

## ▶ ARCHIVED SESSION HISTORY (one-line index; full s8-s16 blocks → `docs/archive/2026-07/SESSION-HANDOFF_20260712_full.md`; s1-s7 → `.../2026-07/SESSION-HANDOFF_20260705_full.md`; pre-06-28 → `.../2026-06/SESSION-HANDOFF_20260628_full.md`)
- **s15 (07-09):** NB-10 move-name voicing + SPEC-2 Word-Search built+harnessed; deep-audit workflow (27 agents): audio-silence verdict = OWNER-SIDE desktop, app clean; 4 audit fixes; NB-21/22 first-run voicing fixed → all SHIPPED LIVE v1.355 in s16.
- **s14 (07-09):** owner gated all builds; NB-13 verb context = tutor.js:147 + i18n-strings.js:585 (awaiting owner's word); NB-10 voicing queued as top task.
- **s13 (07-08):** v1.354 SHIPPED (NB-19 EN-chrome 31 strings + NB-14 behavioral pre-push gate); NB-18 verified-not-reproducible; **q0-difficulty hypothesis FALSIFIED** (easy-q0 shipped v1.325 did not cut abandons); **OWNER THEORY VALIDATED: abandons ≈ ADULT PREVIEW artifact** (desktop 90% vs phone 52%); ideas collection: ~22 worksheets → ~5 reusable engines insight; C&A = NOT a priority (owner).
- **s12 (07-06):** verify-first triage: NB-12 listening-fallback DEFER (2 real instances, text-fallback rejected); NB-10 core already fixed 07-04; CORRECTION: in-app feedback channel EXISTS (parent.js:322); NB-14 orphan-harness gap confirmed → led to the s13 gate.
- **s10 + 07-05 (07-05/06):** v1.342→v1.352 shipped: curated alpha examples, child-paced quizzes, tutor kid-register fix, count-along audio ROOT fixes (single Audio element, event-driven advance), Pages deploy auto-retry, NB-17 young abstract-math voicing incl. dative clips + visual take-away sub, v1.351 math enrichment; leveling mechanics documented (badge cosmetic vs rampMath tiers).
- **s9 (07-05):** q0 render pipeline HEALTHY across all 28 modes (falsified render-bug hypothesis); external Pass-1 audits fact-checked — 3/4 top P1s ALREADY FIXED (verify-first lesson); telemetry access solved: real STATS_KEY lives in `~/.claude/.bivision-creds.env` (NikoLand/.env keys DEAD); s8 telemetry deep-dive: kids' non-completion real, aggregate telemetry has a ceiling (says WHERE not WHY).

## ▶ CARRIED-OPEN — code-bug QUEUE (from session 6; still open unless struck)
> **2026-07-05: this queue is now mirrored as the NB INTAKE LEDGER at the top of `docs/BUGFIX_TRACKER.md` (NB-1…NB-14) — that ledger is the live SSOT for open bugs; new owner reports go there FIRST (§14 intake). Enforcement now mechanical: UserPromptSubmit nudge + once-per-session Stop guard + commit-msg NB warn (`.claude/hooks/`, `.git/hooks/commit-msg`).**

sw.js:88 HIGH ✅DONE (v1.341 network-first) · letter/word well-posedness ✅DONE (placement flashcard) · menu build-tile `ა ბ გ`→icon ✅DONE (🧩) · footer ✅DONE. **STILL OPEN:** footer talk consolidation (bespoke `.talk-nav` ≠ shared dock) · ~~kings-math tutor voice (GAP-3 = raw digits)~~ ✅VERIFIED-NOT-REPRODUCIBLE v1.341 (reason strands silent/visual, session-9) · ka tutor B-1…B-8 (`tutor.js`/`owl.js`) · s-badge contrast · 44px tap targets · exam bonus cap→5 · skip-tier monotonic · telemetry allow-list · "Screen-time" anglicism · menu-i18n: `screens-menu.js:100` `სამკუთხედი` untranslated, Premium separator `·`vs`—` (`i18n-strings.js:776`), `screens.js` chrome strings, firstrun island · **Movement/მოძრაობა section weak/underdeveloped** (owner: strengthen like the ამოწერა letter-writing; fold Movement/Drawing/Talk into every class-sweep + visual-QA, don't hardcode a "4 modules" list).

**WEEKLY CRON:** Windows task `NikoLearn-VisualQA-Weekly` (Mon 05:07) runs `qa/visual-qa-weekly.cmd` (headless `claude -p` §8 crawl). ⚠ NEEDS one verified dry-run — headless-claude+Playwright reliability unconfirmed.

**EVAL (per §15, agent CANNOT write to Evals/ — Viktor scores):** ground-truth = owner reported 8 bugs (session 7), all addressed at class level + live-verified on v1.341. Viktor to score outcome. System: `Evals/NikoLearn/nikolearn-eval-v1.md` (/19), results → `Evals/NikoLearn/results/`.

**PARKED (owner paused):** Centre-line one-stroke handwriting — branch `feat/centerline-writing` (commit 8e75aec), skeleton from ka.ttf (33 letters, recall 0.99). Before ship: stroke order/direction per standard; fix ი, ო. Not on main.

## ▶ OPEN ITEMS
- **OWNER:** (1) Meta **Pixel ID** — Events Manager → Data sources → copy ~16-digit ID → I swap into landing.html (1 line) → ads become measurable. (2) **„Kings" legal** — confirm OK to invoke the name (kept as „კინგსისთვის მზადება"). (3) **owl-logo** origin (owned/licensed?).
- **www HTTPS** — apex `https://nikolearn.com` = 200/secure (CANONICAL — everything is built on bare apex). `www` cert still pending GitHub-side LE issuance. On resume: `gh api repos/GShoina/NikoLearn/pages` → if `cert.state=approved`, run `gh api -X PUT repos/GShoina/NikoLearn/pages -F https_enforced=true`. **Do NOT flip the CNAME** (LE rate-limit risk). DNS is correct (all 4 apex A records + www CNAME→gshoina.github.io; my earlier "add 3 A records" instruction was WRONG, ignore it).
- **Root routing — VERIFIED, NO FIX NEEDED:** `boot()` in niko/screens.js redirects first-time visitors (`!state.authed && !niko_enter && !?app=1`) → `landing.html`; returning/authed → app. Owner saw the app only because his browser had prior state. A "root=landing" restructure was KILLED at challenge (wasted work + breaks returning-user/PWA fast path).
- **Audit remainder (non-blocker, optional):** accessibility (subject/mode tiles div→role/tabindex/aria — touch-first, low impact); dead-code removal (renderInitiator/renderConsent, landing() dead block, orphan telemetry.js); debatable LOWs (2 lang controls, logo→landing, tiny-age math menu).
- **Engagement levers [HYPOTHESIS]:** parent „what your child learned" + shareable card; adaptive difficulty via metrics.js bands; Kings-progress. Validate with telemetry.
- **Signal (owner #1 priority):** distribute to known families + ~5 parent interviews; in-app fake-door „Premium" intent + feedback nudge (build, present before live).
- **S2 fonts self-host** (Google-Fonts IP-leak, privacy/FB — heavy, minor benefit, deferred).
- **BTU ka-AI dataset — TOPIC CLOSED 2026-07-14 (assessed: data-source=NO, collab=YES).** CC BY-NC ka dataset = morphology wordform *list* (adult register, no wrong→right pairs) → NOT a data source, does NOT sharpen `GEMINI_KA_LESSONS.md`, can't replace Gemini (dictionary≠copy-editor). "pre-commercial" = weak NC shield + moot anyway. Real value = BTU as local collab/validation ally (abundance frame; we hold the kids-register ka they lack). Full report `output/2026-07-14-BTU ქართული AI-დატასეტის შეფასება by Niko.html`; outreach note `docs/BTU-COLLAB-NOTE-DRAFT.md` (send = owner GO, default don't). Committed 165194d. [[content originality §11]]

## ▶ DEV NOTES
- Live verify: `python -m http.server 8137`, app at `index.html?app=1`. Browser caches sub-scripts → kill MCP chrome or cache-busted curl to verify deploys; SW cache name = version.
- Deploy = push GShoina/NikoLearn main. §7b: customer-facing → preview→owner GO→live; clear bug fixes not gated; owner: stop asking on reversible work.
- Worker deploy: `cd cloudflare && CLOUDFLARE_API_TOKEN=$CF_NIKO_API_TOKEN CLOUDFLARE_ACCOUNT_ID=62f02b2a518a8e63eba0537f9162c0ec npx wrangler@4 deploy`.
- Stats: `GET /v1/stats?k=$NIKO_STATS_KEY` (creds in `~/.claude/.bivision-creds.env`, incl. NIKO_DOMENEBI_* for DNS). Exclude owner devices via `?notrack=1` (sticky `niko_owner=1`).
- Telemetry privacy: analytics.js `CLIENT_ALLOW` MUST mirror `cloudflare/telemetry-worker.js` EVENTS — add new events to BOTH or they're dropped on-device (by design).
