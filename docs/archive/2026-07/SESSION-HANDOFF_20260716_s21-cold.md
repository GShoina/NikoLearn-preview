# s21 cold detail — moved out of SESSION-HANDOFF.md 2026-07-16 (§6e-2 auto-compress)

> Moved, NOT deleted (ჩანაცვლება ≠ წაშლა). Both blocks are closed work whose lessons already
> live at their SSOT: the PI sweep → `docs/PRODUCT_IDEAS.md` + `REASONING-LESSONS.md` L14;
> NB-57 → `docs/BUGFIX_TRACKER.md` + `qa/qa-check.mjs` check 8. Kept here for the reasoning.

**✅ NB-57 CLOSED 2026-07-15 (`3fb170a` fix, `c7fe07c` ledger) — the audit's #10, and the worst thing found so far.** Live on nikolearn.com since v1.320: opening the installed app **offline** served the marketing landing page, and every way out led back to it. **ჩიხი** — offline, a child could not reach the app at all. Two mechanisms (`sw.js:90` cached every navigation under the literal key `'./index.html'`, and `screens.js:132` redirects to landing **by design** so the poisoning was the normal path; plus `manifest.json:5`'s queried start_url guaranteed the offline miss). Fixed both; §6c proven **both ways in a real browser** (old code fails the same test), screenshots in `output/`. Guard: `qa/qa-check.mjs` check 8, discriminator-proven 3 ways. **The lesson worth keeping:** static reading showed two innocent lines. Only running it showed the dead end — and the audit itself rated this a 3.

**▶ s21 PI-LEDGER SWEEP (2026-07-15) — the owner refused a false completeness claim and was RIGHT.** He asked for the full list of ideas he has given („მინდა სურლი სია"). I answered „32 ideas, nothing lost, verified" off a transcript sweep. He pushed back — „**ურიცხვი იდეა მომიწოდებია, სხვადასხვა ფორმატში, ივლისში**" — and the challenge held. **ROOT (reusable): the search surface was scoped by the CONTAINER, not the CONTENT** — it looked under the `NikoLand` project key, but **92 of 101 NikoLand transcripts live under other keys** (the key derives from the session's cwd). Coverage was **9 files / 132 msgs / 108,719 chars = 9%** of the real **101 / 1,282 / 1,179,212**. A 9% sample returning zero is not evidence of absence; the conclusion was backwards, not merely thin. Same class as the NB-51 `.gitignore` orphan: **a rule scoped to a name stops matching when the name is not where the thing is.** A real 6-agent full-corpus sweep then found **49 ideas that existed nowhere on disk → ledger was 32, true total is 81, i.e. it was 40% complete while calling itself the list.** Both loss modes are real, not one: (1) never written down = the 49; (2) written but never entering the plan = Marble. **Lesson locked → `REASONING-LESSONS.md` L14** (never say complete/nothing-lost/verified without stating the search surface, the coverage as a number, and what the method structurally CANNOT see). **Permanent blind spots, stated not hidden:** transcripts start 06-15 (06-04→06-14 survives only because it was written down at the time) · 14 of 42 days have no transcript · only **1** of 1,282 messages retains an image, so screenshot/voice ideas are unrecoverable at ANY coverage (PI-51/52/53 survive only because the surrounding text described the photos). **He had already told us:** 06-27 „ვისაუბრეთ მაგრამ **დაიკარგა თემა**" (now PI-60) — he named the loss on the day and it stayed lost 18 more days = a logged recurrence. Artifacts: `docs/PRODUCT_IDEAS.md` (81 rows) · `docs/PRODUCT_ROADMAP.md` §RECOVERED INTAKE · owner report `output/2026-07-15-პროდუქტის იდეების სრული სია by Niko.html`.
